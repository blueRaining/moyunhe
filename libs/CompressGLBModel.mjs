"use strict";
function e(s, r) {
  const n = t();
  return (e = function (e, t) {
    return n[(e -= 474)];
  })(s, r);
}
function t() {
  const e = [
    "getHandler",
    "678422svRPFC",
    "lastIndex",
    "addHandler",
    "resolveURL",
    "removeHandler",
    "1MwhcRC",
    "test",
    "1969844UiDdHC",
    "259645bRwdQs",
    "indexOf",
    "4879971CZSvIe",
    "setURLModifier",
    "6DsBqsb",
    "onLoad",
    "push",
    "onStart",
    "itemEnd",
    "553maSdoo",
    "itemError",
    "11PTiJLb",
    "25614228NWYNXt",
    "onProgress",
    "global",
    "21544ctxlue",
    "430590GjRJJM",
    "onError",
    "10GWzPZF",
  ];
  return (t = function () {
    return e;
  })();
}
!(function (s, r) {
  const n = e,
    i = t();
  for (;;)
    try {
      if (
        456280 ===
        (parseInt(n(490)) / 1) * (-parseInt(n(485)) / 2) +
          -parseInt(n(481)) / 3 +
          -parseInt(n(492)) / 4 +
          (-parseInt(n(493)) / 5) * (-parseInt(n(497)) / 6) +
          (parseInt(n(474)) / 7) * (-parseInt(n(480)) / 8) +
          (-parseInt(n(495)) / 9) * (parseInt(n(483)) / 10) +
          (-parseInt(n(476)) / 11) * (-parseInt(n(477)) / 12)
      )
        break;
      i.push(i.shift());
    } catch (e) {
      i.push(i.shift());
    }
})();
const s = new (class {
  constructor(t, s, r) {
    const n = e,
      i = this;
    let o,
      a = !1,
      c = 0,
      u = 0;
    const h = [];
    (this[n(500)] = void 0),
      (this[n(498)] = t),
      (this[n(478)] = s),
      (this[n(482)] = r),
      (this.itemStart = function (e) {
        const t = n;
        u++, !1 === a && void 0 !== i[t(500)] && i[t(500)](e, c, u), (a = !0);
      }),
      (this[n(501)] = function (e) {
        const t = n;
        c++,
          void 0 !== i[t(478)] && i[t(478)](e, c, u),
          c === u && ((a = !1), void 0 !== i[t(498)] && i[t(498)]());
      }),
      (this[n(475)] = function (e) {
        const t = n;
        void 0 !== i[t(482)] && i[t(482)](e);
      }),
      (this[n(488)] = function (e) {
        return o ? o(e) : e;
      }),
      (this[n(496)] = function (e) {
        return (o = e), this;
      }),
      (this[n(487)] = function (e, t) {
        return h[n(499)](e, t), this;
      }),
      (this[n(489)] = function (e) {
        const t = h[n(494)](e);
        return -1 !== t && h.splice(t, 2), this;
      }),
      (this[n(484)] = function (e) {
        const t = n;
        for (let s = 0, r = h.length; s < r; s += 2) {
          const r = h[s],
            n = h[s + 1];
          if ((r[t(479)] && (r[t(486)] = 0), r[t(491)](e))) return n;
        }
        return null;
      });
  }
})();
function r(e, t) {
  const s = i();
  return (r = function (e, t) {
    return s[(e -= 309)];
  })(e, t);
}
const n = r;
function i() {
  const e = [
    "3535788DyMDGf",
    "anonymous",
    "setRequestHeader",
    "2328549WefOyN",
    "__DEFAULT",
    "setWithCredentials",
    "setResourcePath",
    "withCredentials",
    "528299DOjHOq",
    "requestHeader",
    "6612435hbQoyY",
    "manager",
    "load",
    "parse",
    "path",
    "1074940QbrHhH",
    "crossOrigin",
    "7rkFBho",
    "resourcePath",
    "4850408PjesHh",
    "2135728TdOimF",
    "5ovUVhf",
  ];
  return (i = function () {
    return e;
  })();
}
!(function (e, t) {
  const s = r,
    n = i();
  for (;;)
    try {
      if (
        445570 ===
        parseInt(s(327)) / 1 +
          parseInt(s(312)) / 2 +
          parseInt(s(322)) / 3 +
          parseInt(s(317)) / 4 +
          (-parseInt(s(318)) / 5) * (parseInt(s(319)) / 6) +
          (-parseInt(s(314)) / 7) * (parseInt(s(316)) / 8) +
          -parseInt(s(329)) / 9
      )
        break;
      n.push(n.shift());
    } catch (e) {
      n.push(n.shift());
    }
})();
class o {
  constructor(e) {
    const t = r;
    (this[t(330)] = void 0 !== e ? e : s),
      (this[t(313)] = t(320)),
      (this.withCredentials = !1),
      (this.path = ""),
      (this[t(315)] = ""),
      (this[t(328)] = {});
  }
  [n(309)]() {}
  loadAsync(e, t) {
    const s = this;
    return new Promise(function (r, n) {
      s.load(e, r, t, n);
    });
  }
  [n(310)]() {}
  setCrossOrigin(e) {
    return (this[n(313)] = e), this;
  }
  [n(324)](e) {
    return (this[n(326)] = e), this;
  }
  setPath(e) {
    return (this[n(311)] = e), this;
  }
  [n(325)](e) {
    return (this[n(315)] = e), this;
  }
  [n(321)](e) {
    return (this[n(328)] = e), this;
  }
}
o.DEFAULT_MATERIAL_NAME = n(323);
const a = c;
function c(e, t) {
  const s = l();
  return (c = function (e, t) {
    return s[(e -= 267)];
  })(e, t);
}
!(function (e, t) {
  const s = c,
    r = l();
  for (;;)
    try {
      if (
        281143 ===
        parseInt(s(314)) / 1 +
          (-parseInt(s(276)) / 2) * (parseInt(s(278)) / 3) +
          parseInt(s(316)) / 4 +
          -parseInt(s(285)) / 5 +
          parseInt(s(308)) / 6 +
          -parseInt(s(319)) / 7 +
          parseInt(s(301)) / 8
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
const u = {};
class h extends Error {
  constructor(e, t) {
    const s = c;
    super(e), (this[s(320)] = t);
  }
}
class f extends o {
  constructor(e) {
    super(e);
  }
  [a(275)](e, t, s, r) {
    const n = a;
    if (
      (void 0 === e && (e = ""),
      void 0 !== this.path && (e = this[n(310)] + e),
      (e = this.manager.resolveURL(e)),
      void 0 !== u[e])
    )
      return void u[e][n(296)]({ onLoad: t, onProgress: s, onError: r });
    (u[e] = []), u[e][n(296)]({ onLoad: t, onProgress: s, onError: r });
    const i = new Request(e, {
        headers: new Headers(this[n(291)]),
        credentials: this[n(282)] ? n(307) : n(267),
      }),
      o = this.mimeType,
      f = this[n(302)];
    fetch(i)
      [n(295)]((t) => {
        const s = n;
        if (200 === t[s(287)] || 0 === t[s(287)]) {
          if (
            (0 === t[s(287)] && console[s(318)](s(294)),
            typeof ReadableStream === s(289) ||
              void 0 === t[s(274)] ||
              void 0 === t[s(274)][s(280)])
          )
            return t;
          const r = u[e],
            n = t.body[s(280)](),
            i = t[s(300)].get(s(311)) || t.headers[s(279)](s(270)),
            o = i ? parseInt(i) : 0,
            a = 0 !== o;
          let h = 0;
          const f = new ReadableStream({
            start(e) {
              !(function t() {
                n.read().then(({ done: s, value: n }) => {
                  const i = c;
                  if (s) e[i(297)]();
                  else {
                    h += n.byteLength;
                    const s = new ProgressEvent("progress", {
                      lengthComputable: a,
                      loaded: h,
                      total: o,
                    });
                    for (let e = 0, t = r[i(305)]; e < t; e++) {
                      const t = r[e];
                      t[i(277)] && t[i(277)](s);
                    }
                    e[i(303)](n), t();
                  }
                });
              })();
            },
          });
          return new Response(f);
        }
        throw new h(s(273) + t.url + s(313) + t[s(287)] + ": " + t[s(299)], t);
      })
      [n(295)]((e) => {
        const t = n;
        switch (f) {
          case t(281):
            return e[t(290)]();
          case t(283):
            return e[t(283)]();
          case t(309):
            return e[t(271)]().then((e) => {
              const s = t;
              return new DOMParser()[s(306)](e, o);
            });
          case t(268):
            return e[t(268)]();
          default:
            if (void 0 === o) return e[t(271)]();
            {
              const s = /charset="?([^;"\s]*)"?/i.exec(o),
                r = s && s[1] ? s[1][t(288)]() : void 0,
                n = new TextDecoder(r);
              return e.arrayBuffer().then((e) => n.decode(e));
            }
        }
      })
      [n(295)]((t) => {
        const s = n,
          r = u[e];
        delete u[e];
        for (let e = 0, n = r[s(305)]; e < n; e++) {
          const n = r[e];
          n[s(292)] && n[s(292)](t);
        }
      })
      [n(317)]((t) => {
        const s = n,
          r = u[e];
        if (void 0 === r) throw (this.manager[s(293)](e), t);
        delete u[e];
        for (let e = 0, n = r[s(305)]; e < n; e++) {
          const n = r[e];
          n[s(284)] && n[s(284)](t);
        }
        this.manager[s(293)](e);
      })
      [n(272)](() => {
        const t = n;
        this.manager[t(269)](e);
      }),
      this[n(304)][n(286)](e);
  }
  [a(298)](e) {
    return (this[a(302)] = e), this;
  }
  [a(312)](e) {
    return (this[a(315)] = e), this;
  }
}
function l() {
  const e = [
    "159myKpeC",
    "get",
    "getReader",
    "arraybuffer",
    "withCredentials",
    "blob",
    "onError",
    "751575tkdIlY",
    "itemStart",
    "status",
    "toLowerCase",
    "undefined",
    "arrayBuffer",
    "requestHeader",
    "onLoad",
    "itemError",
    "THREE.FileLoader: HTTP Status 0 received.",
    "then",
    "push",
    "close",
    "setResponseType",
    "statusText",
    "headers",
    "2769256smrdFh",
    "responseType",
    "enqueue",
    "manager",
    "length",
    "parseFromString",
    "include",
    "538974DDOPku",
    "document",
    "path",
    "Content-Length",
    "setMimeType",
    '" responded with ',
    "133397TAdliY",
    "mimeType",
    "1989656EtcCST",
    "catch",
    "warn",
    "1334312hUDrAa",
    "response",
    "same-origin",
    "json",
    "itemEnd",
    "X-File-Size",
    "text",
    "finally",
    'fetch for "',
    "body",
    "load",
    "16782nhRLMm",
    "onProgress",
  ];
  return (l = function () {
    return e;
  })();
}
function p(e, t) {
  const s = g();
  return (p = function (e, t) {
    return s[(e -= 390)];
  })(e, t);
}
function d() {
  let e, t;
  function s(e, t, s, r, n, i) {
    const o = p,
      a = i[o(406)](),
      c = s[o(440)]() * a,
      u = c * n.BYTES_PER_ELEMENT,
      h = (function (e, t) {
        const s = p;
        switch (t) {
          case Float32Array:
            return e[s(427)];
          case Int8Array:
            return e.DT_INT8;
          case Int16Array:
            return e[s(408)];
          case Int32Array:
            return e[s(407)];
          case Uint8Array:
            return e[s(410)];
          case Uint16Array:
            return e[s(426)];
          case Uint32Array:
            return e[s(413)];
        }
      })(e, n),
      f = e[o(402)](u);
    t[o(399)](s, i, h, u, f);
    const l = new n(e.HEAPF32[o(404)], f, c)[o(419)]();
    return e._free(f), { name: r, array: l, itemSize: a };
  }
  onmessage = function (r) {
    const n = p,
      i = r[n(423)];
    switch (i[n(420)]) {
      case "init":
        (e = i.decoderConfig),
          (t = new Promise(function (t) {
            (e[n(421)] = function (e) {
              t({ draco: e });
            }),
              DracoDecoderModule(e);
          }));
        break;
      case "decode":
        const r = i[n(404)],
          o = i.taskConfig;
        t[n(444)]((e) => {
          const t = n,
            a = e[t(429)],
            c = new a[t(415)]();
          try {
            const e = (function (e, t, r, n) {
                const i = p,
                  o = n[i(432)],
                  a = n.attributeTypes;
                let c, u;
                const h = t.GetEncodedGeometryType(r);
                if (h === e[i(416)])
                  (c = new e[i(396)]()), (u = t[i(409)](r, r[i(433)], c));
                else {
                  if (h !== e[i(422)])
                    throw new Error(
                      "THREE.DRACOLoader: Unexpected geometry type."
                    );
                  (c = new e[i(393)]()), (u = t[i(438)](r, r.byteLength, c));
                }
                if (!u.ok() || 0 === c.ptr)
                  throw new Error(i(417) + u[i(443)]());
                const f = { index: null, attributes: [] };
                for (const r in o) {
                  const u = self[a[r]];
                  let h, l;
                  if (n[i(405)]) (l = o[r]), (h = t[i(424)](c, l));
                  else {
                    if (((l = t.GetAttributeId(c, e[o[r]])), -1 === l))
                      continue;
                    h = t[i(434)](c, l);
                  }
                  const p = s(e, t, c, r, u, h);
                  r === i(430) && (p[i(442)] = n[i(442)]), f[i(400)][i(412)](p);
                }
                return (
                  h === e[i(416)] &&
                    (f[i(397)] = (function (e, t, s) {
                      const r = p,
                        n = 3 * s[r(418)](),
                        i = 4 * n,
                        o = e[r(402)](i);
                      t[r(411)](s, i, o);
                      const a = new Uint32Array(e[r(439)][r(404)], o, n)[
                        r(419)
                      ]();
                      return e._free(o), { array: a, itemSize: 1 };
                    })(e, t, c)),
                  e[i(398)](c),
                  f
                );
              })(a, c, new Int8Array(r), o),
              n = e.attributes[t(414)]((e) => e.array[t(404)]);
            e.index && n.push(e.index[t(394)][t(404)]),
              self[t(441)]({ type: "decode", id: i.id, geometry: e }, n);
          } catch (e) {
            console[t(436)](e),
              self[t(441)]({ type: "error", id: i.id, error: e[t(425)] });
          } finally {
            a[t(398)](c);
          }
        });
    }
  };
}
function g() {
  const e = [
    "attributeIDs",
    "byteLength",
    "GetAttribute",
    "78170mLVAaF",
    "error",
    "1227628PGKBhv",
    "DecodeArrayToPointCloud",
    "HEAPF32",
    "num_points",
    "postMessage",
    "vertexColorSpace",
    "error_msg",
    "then",
    "4717475uZJTGa",
    "12590540DMALWf",
    "694860sFypfW",
    "PointCloud",
    "array",
    "8tFcXEN",
    "Mesh",
    "index",
    "destroy",
    "GetAttributeDataArrayForAllPoints",
    "attributes",
    "30oekugD",
    "_malloc",
    "6028443zcipMw",
    "buffer",
    "useUniqueIDs",
    "num_components",
    "DT_INT32",
    "DT_INT16",
    "DecodeArrayToMesh",
    "DT_UINT8",
    "GetTrianglesUInt32Array",
    "push",
    "DT_UINT32",
    "map",
    "Decoder",
    "TRIANGULAR_MESH",
    "THREE.DRACOLoader: Decoding failed: ",
    "num_faces",
    "slice",
    "type",
    "onModuleLoaded",
    "POINT_CLOUD",
    "data",
    "GetAttributeByUniqueId",
    "message",
    "DT_UINT16",
    "DT_FLOAT32",
    "474CWIFox",
    "draco",
    "color",
    "15267DRuQkn",
  ];
  return (g = function () {
    return e;
  })();
}
!(function (e, t) {
  const s = p,
    r = g();
  for (;;)
    try {
      if (
        529464 ===
        -parseInt(s(392)) / 1 +
          (parseInt(s(435)) / 2) * (parseInt(s(401)) / 3) +
          parseInt(s(437)) / 4 +
          parseInt(s(390)) / 5 +
          (-parseInt(s(428)) / 6) * (-parseInt(s(431)) / 7) +
          (parseInt(s(395)) / 8) * (parseInt(s(403)) / 9) +
          -parseInt(s(391)) / 10
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
const m = x;
function x(e, t) {
  const s = T();
  return (x = function (e, t) {
    return s[(e -= 274)];
  })(e, t);
}
function T() {
  const e = [
    "/* worker */",
    "DRACO_LIBRARY_PATH",
    "1484805KatsFf",
    "draco_wasm_wrapper.js",
    "join",
    "setDecoderConfig",
    "decoderPath",
    "type",
    "withCredentials",
    "draco_encoder.wasm",
    "toString",
    "draco_decoder.wasm",
    "/* draco decoder */",
    "wasm",
    "arraybuffer",
    "object",
    "_initDecoder",
    "decoderPending",
    "decoderConfig",
    "wasmBinary",
    "360231XWJXZr",
    "unable to load decoder module",
    "\nDracoEncoderModule;",
    "push",
    "preload",
    "text",
    "initDecoder",
    "3789YNEAdD",
    "encoderPending",
    "indexOf",
    "setResponseType",
    "createObjectURL",
    "substring",
    "19420PqenhB",
    "initEncoder",
    "1912815VmmhDX",
    "17374xSFnDu",
    "8321208pXZPcV",
    "workerSourceURL",
    "8qWmOcx",
    "2936dtuGdR",
    "encoderConfig",
    "19812111uBiUHy",
    "4KEAECW",
    "setPath",
    "_loadLibrary",
    "then",
    "setDecoderPath",
    "\nDracoDecoderModule;",
    "load",
    "setWithCredentials",
  ];
  return (T = function () {
    return e;
  })();
}
!(function (e, t) {
  const s = x,
    r = T();
  for (;;)
    try {
      if (
        774644 ===
        (parseInt(s(321)) / 1) * (parseInt(s(293)) / 2) +
          -parseInt(s(303)) / 3 +
          (-parseInt(s(289)) / 4) * (parseInt(s(285)) / 5) +
          parseInt(s(287)) / 6 +
          (parseInt(s(286)) / 7) * (parseInt(s(290)) / 8) +
          (-parseInt(s(277)) / 9) * (-parseInt(s(283)) / 10) +
          -parseInt(s(292)) / 11
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
class E extends o {
  constructor() {
    const e = x;
    super(),
      (this[e(319)] = { type: e(314) }),
      (this.encoderPending = null),
      (this.encoderConfig = { type: e(314) }),
      (this.decoderPath = ""),
      (this.workerSourceURL = ""),
      (this[e(318)] = null),
      this[e(297)](E[e(302)]);
  }
  setDecoderPath(e) {
    return (this[x(307)] = e), this;
  }
  [m(274)](e = !0, t = !1) {
    const s = m;
    return e && this[s(317)](), t && this[s(284)](), this;
  }
  [m(306)](e) {
    return (this[m(319)] = e), this;
  }
  [m(295)](e, t) {
    const s = m,
      r = new f();
    return (
      r[s(294)](this[s(307)]),
      r[s(280)](t),
      r[s(300)](this[s(309)]),
      new Promise((t, n) => {
        r[s(299)](e, t, void 0, n);
      })
    );
  }
  async initEncoder() {
    const e = m;
    if (this[e(278)]) return this.encoderPending;
    const t = typeof WebAssembly !== e(316) || "js" === this[e(291)].type,
      s = [];
    return (
      t
        ? s[e(324)](this[e(295)]("draco_encoder.js", "text"))
        : (s[e(324)](this._loadLibrary(e(304), "text")),
          s[e(324)](this[e(295)](e(310), e(315)))),
      (this[e(278)] = Promise.all(s)[e(296)]((s) => {
        const r = e,
          n = s[0];
        !t && (this[r(291)][r(320)] = s[1]);
        const i = eval;
        return i(n + r(323))?.();
      })),
      this[e(278)]
    );
  }
  [m(317)]() {
    const e = m;
    if (this[e(318)]) return this[e(318)];
    const t = "object" != typeof WebAssembly || "js" === this[e(319)][e(308)],
      s = [];
    return (
      t
        ? s.push(this._loadLibrary("draco_decoder.js", e(275)))
        : (s[e(324)](this[e(295)]("draco_wasm_wrapper.js", e(275))),
          s.push(this._loadLibrary(e(312), "arraybuffer"))),
      (this.decoderPending = Promise.all(s)[e(296)]((s) => {
        const r = e,
          n = s[0];
        !t && (this.decoderConfig.wasmBinary = s[1]);
        const i = d[r(311)](),
          o = [
            r(313),
            n,
            "",
            r(301),
            i.substring(i[r(279)]("{") + 1, i.lastIndexOf("}")),
          ][r(305)]("\n");
        this[r(288)] = URL[r(281)](new Blob([o]));
      })),
      this[e(318)]
    );
  }
  async [m(276)]() {
    const e = m;
    await this[e(317)]();
    const t = await fetch(this[e(288)])
        [e(296)](async (e) => e.text())
        [e(296)]((t) => {
          const s = e,
            r = t[s(279)](s(301));
          if (r < 1) throw new Error(s(322));
          return t[s(282)](0, r - 1);
        }),
      s = eval;
    return s(t + e(298))?.();
  }
}
E.DRACO_LIBRARY_PATH = "./libs/";
class y {
  constructor() {
    this._listeners = {};
  }
  addEventListener(e, t) {
    const s = this._listeners;
    return (
      void 0 === s[e] && (s[e] = []),
      -1 === s[e].indexOf(t) && s[e].push(t),
      this
    );
  }
  removeEventListener(e, t) {
    if (void 0 === this._listeners) return this;
    const s = this._listeners[e];
    if (void 0 !== s) {
      const e = s.indexOf(t);
      -1 !== e && s.splice(e, 1);
    }
    return this;
  }
  dispatchEvent(e) {
    if (void 0 === this._listeners) return this;
    const t = this._listeners[e.type];
    if (void 0 !== t) {
      const s = t.slice(0);
      for (let t = 0, r = s.length; t < r; t++) s[t].call(this, e);
    }
    return this;
  }
  dispose() {
    for (const e in this._listeners) delete this._listeners[e];
  }
}
class I extends y {
  constructor(e, t, s, r = {}) {
    if (
      (super(),
      (this._name = void 0),
      (this._parent = void 0),
      (this._child = void 0),
      (this._attributes = void 0),
      (this._disposed = !1),
      (this._name = e),
      (this._parent = t),
      (this._child = s),
      (this._attributes = r),
      !t.isOnGraph(s))
    )
      throw new Error("Cannot connect disconnected graphs.");
  }
  getName() {
    return this._name;
  }
  getParent() {
    return this._parent;
  }
  getChild() {
    return this._child;
  }
  setChild(e) {
    return (this._child = e), this;
  }
  getAttributes() {
    return this._attributes;
  }
  dispose() {
    this._disposed ||
      ((this._disposed = !0),
      this.dispatchEvent({ type: "dispose", target: this }),
      super.dispose());
  }
  isDisposed() {
    return this._disposed;
  }
}
class R extends y {
  constructor(...e) {
    super(...e),
      (this._emptySet = new Set()),
      (this._edges = new Set()),
      (this._parentEdges = new Map()),
      (this._childEdges = new Map());
  }
  listEdges() {
    return Array.from(this._edges);
  }
  listParentEdges(e) {
    return Array.from(this._childEdges.get(e) || this._emptySet);
  }
  listParents(e) {
    return this.listParentEdges(e).map((e) => e.getParent());
  }
  listChildEdges(e) {
    return Array.from(this._parentEdges.get(e) || this._emptySet);
  }
  listChildren(e) {
    return this.listChildEdges(e).map((e) => e.getChild());
  }
  disconnectParents(e, t) {
    let s = this.listParentEdges(e);
    return (
      t && (s = s.filter((e) => t(e.getParent()))),
      s.forEach((e) => e.dispose()),
      this
    );
  }
  createEdge(e, t, s, r) {
    return this._registerEdge(new I(e, t, s, r));
  }
  _registerEdge(e) {
    this._edges.add(e);
    const t = e.getParent();
    this._parentEdges.has(t) || this._parentEdges.set(t, new Set()),
      this._parentEdges.get(t).add(e);
    const s = e.getChild();
    return (
      this._childEdges.has(s) || this._childEdges.set(s, new Set()),
      this._childEdges.get(s).add(e),
      e.addEventListener("dispose", () => this._removeEdge(e)),
      e
    );
  }
  _removeEdge(e) {
    return (
      this._edges.delete(e),
      this._parentEdges.get(e.getParent()).delete(e),
      this._childEdges.get(e.getChild()).delete(e),
      this
    );
  }
}
function N() {
  return (
    (N =
      Object.assign ||
      function (e) {
        for (var t = 1; t < arguments.length; t++) {
          var s = arguments[t];
          for (var r in s)
            Object.prototype.hasOwnProperty.call(s, r) && (e[r] = s[r]);
        }
        return e;
      }),
    N.apply(this, arguments)
  );
}
class A {
  constructor(e) {
    if (((this.list = []), e)) for (const t of e) this.list.push(t);
  }
  add(e) {
    this.list.push(e);
  }
  remove(e) {
    const t = this.list.indexOf(e);
    t >= 0 && this.list.splice(t, 1);
  }
  removeChild(e) {
    const t = [];
    for (const s of this.list) s.getChild() === e && t.push(s);
    for (const e of t) this.remove(e);
    return t;
  }
  listRefsByChild(e) {
    const t = [];
    for (const s of this.list) s.getChild() === e && t.push(s);
    return t;
  }
  values() {
    return this.list;
  }
}
class w {
  constructor(e) {
    if (((this.set = new Set()), (this.map = new Map()), e))
      for (const t of e) this.add(t);
  }
  add(e) {
    const t = e.getChild();
    this.removeChild(t), this.set.add(e), this.map.set(t, e);
  }
  remove(e) {
    this.set.delete(e), this.map.delete(e.getChild());
  }
  removeChild(e) {
    const t = this.map.get(e) || null;
    return t && this.remove(t), t;
  }
  getRefByChild(e) {
    return this.map.get(e) || null;
  }
  values() {
    return Array.from(this.set);
  }
}
class b {
  constructor(e) {
    (this.map = {}), e && Object.assign(this.map, e);
  }
  set(e, t) {
    this.map[e] = t;
  }
  delete(e) {
    delete this.map[e];
  }
  get(e) {
    return this.map[e] || null;
  }
  keys() {
    return Object.keys(this.map);
  }
  values() {
    return Object.values(this.map);
  }
}
const M = Symbol("attributes"),
  S = Symbol("immutableKeys");
class _ extends y {
  constructor(e) {
    super(),
      (this._disposed = !1),
      (this.graph = void 0),
      (this[M] = void 0),
      (this[S] = void 0),
      (this.graph = e),
      (this[S] = new Set()),
      (this[M] = this._createAttributes());
  }
  getDefaults() {
    return {};
  }
  _createAttributes() {
    const e = this.getDefaults(),
      t = {};
    for (const s in e) {
      const r = e[s];
      if (r instanceof _) {
        const e = this.graph.createEdge(s, this, r);
        e.addEventListener("dispose", () => r.dispose()),
          this[S].add(s),
          (t[s] = e);
      } else t[s] = r;
    }
    return t;
  }
  isOnGraph(e) {
    return this.graph === e.graph;
  }
  isDisposed() {
    return this._disposed;
  }
  dispose() {
    this._disposed ||
      (this.graph.listChildEdges(this).forEach((e) => e.dispose()),
      this.graph.disconnectParents(this),
      (this._disposed = !0),
      this.dispatchEvent({ type: "dispose" }));
  }
  detach() {
    return this.graph.disconnectParents(this), this;
  }
  swap(e, t) {
    for (const s in this[M]) {
      const r = this[M][s];
      if (r instanceof I) {
        const n = r;
        n.getChild() === e && this.setRef(s, t, n.getAttributes());
      } else if (r instanceof A)
        for (const n of r.listRefsByChild(e)) {
          const r = n.getAttributes();
          this.removeRef(s, e), this.addRef(s, t, r);
        }
      else if (r instanceof w) {
        const n = r.getRefByChild(e);
        if (n) {
          const r = n.getAttributes();
          this.removeRef(s, e), this.addRef(s, t, r);
        }
      } else if (r instanceof b)
        for (const n of r.keys()) {
          const i = r.get(n);
          i.getChild() === e && this.setRefMap(s, n, t, i.getAttributes());
        }
    }
    return this;
  }
  get(e) {
    return this[M][e];
  }
  set(e, t) {
    return (
      (this[M][e] = t), this.dispatchEvent({ type: "change", attribute: e })
    );
  }
  getRef(e) {
    const t = this[M][e];
    return t ? t.getChild() : null;
  }
  setRef(e, t, s) {
    if (this[S].has(e))
      throw new Error(`Cannot overwrite immutable attribute, "${e}".`);
    const r = this[M][e];
    if ((r && r.dispose(), !t)) return this;
    const n = this.graph.createEdge(e, this, t, s);
    return (
      n.addEventListener("dispose", () => {
        delete this[M][e], this.dispatchEvent({ type: "change", attribute: e });
      }),
      (this[M][e] = n),
      this.dispatchEvent({ type: "change", attribute: e })
    );
  }
  listRefs(e) {
    return this.assertRefList(e)
      .values()
      .map((e) => e.getChild());
  }
  addRef(e, t, s) {
    const r = this.graph.createEdge(e, this, t, s),
      n = this.assertRefList(e);
    return (
      n.add(r),
      r.addEventListener("dispose", () => {
        n.remove(r), this.dispatchEvent({ type: "change", attribute: e });
      }),
      this.dispatchEvent({ type: "change", attribute: e })
    );
  }
  removeRef(e, t) {
    const s = this.assertRefList(e);
    if (s instanceof A) for (const e of s.removeChild(t)) e.dispose();
    else {
      const e = s.removeChild(t);
      e && e.dispose();
    }
    return this;
  }
  assertRefList(e) {
    const t = this[M][e];
    if (t instanceof A || t instanceof w) return t;
    throw new Error(`Expected RefList or RefSet for attribute "${e}"`);
  }
  listRefMapKeys(e) {
    return this.assertRefMap(e).keys();
  }
  listRefMapValues(e) {
    return this.assertRefMap(e)
      .values()
      .map((e) => e.getChild());
  }
  getRefMap(e, t) {
    const s = this.assertRefMap(e).get(t);
    return s ? s.getChild() : null;
  }
  setRefMap(e, t, s, r) {
    const n = this.assertRefMap(e),
      i = n.get(t);
    if ((i && i.dispose(), !s)) return this;
    r = Object.assign(r || {}, { key: t });
    const o = this.graph.createEdge(e, this, s, N({}, r, { key: t }));
    return (
      o.addEventListener("dispose", () => {
        n.delete(t),
          this.dispatchEvent({ type: "change", attribute: e, key: t });
      }),
      n.set(t, o),
      this.dispatchEvent({ type: "change", attribute: e, key: t })
    );
  }
  assertRefMap(e) {
    const t = this[M][e];
    if (t instanceof b) return t;
    throw new Error(`Expected RefMap for attribute "${e}"`);
  }
  dispatchEvent(e) {
    return (
      super.dispatchEvent(N({}, e, { target: this })),
      this.graph.dispatchEvent(
        N({}, e, { target: this, type: `node:${e.type}` })
      ),
      this
    );
  }
}
const C = "v4.0.8",
  v = "@glb.bin";
var O, D, F, U, L;
!(function (e) {
  (e.ACCESSOR = "Accessor"),
    (e.ANIMATION = "Animation"),
    (e.ANIMATION_CHANNEL = "AnimationChannel"),
    (e.ANIMATION_SAMPLER = "AnimationSampler"),
    (e.BUFFER = "Buffer"),
    (e.CAMERA = "Camera"),
    (e.MATERIAL = "Material"),
    (e.MESH = "Mesh"),
    (e.PRIMITIVE = "Primitive"),
    (e.PRIMITIVE_TARGET = "PrimitiveTarget"),
    (e.NODE = "Node"),
    (e.ROOT = "Root"),
    (e.SCENE = "Scene"),
    (e.SKIN = "Skin"),
    (e.TEXTURE = "Texture"),
    (e.TEXTURE_INFO = "TextureInfo");
})(O || (O = {})),
  (function (e) {
    (e.INTERLEAVED = "interleaved"), (e.SEPARATE = "separate");
  })(D || (D = {})),
  (function (e) {
    (e.ARRAY_BUFFER = "ARRAY_BUFFER"),
      (e.ELEMENT_ARRAY_BUFFER = "ELEMENT_ARRAY_BUFFER"),
      (e.INVERSE_BIND_MATRICES = "INVERSE_BIND_MATRICES"),
      (e.OTHER = "OTHER"),
      (e.SPARSE = "SPARSE");
  })(F || (F = {})),
  (function (e) {
    (e[(e.R = 4096)] = "R"),
      (e[(e.G = 256)] = "G"),
      (e[(e.B = 16)] = "B"),
      (e[(e.A = 1)] = "A");
  })(U || (U = {})),
  (function (e) {
    (e.GLTF = "GLTF"), (e.GLB = "GLB");
  })(L || (L = {}));
const B = {
  5120: Int8Array,
  5121: Uint8Array,
  5122: Int16Array,
  5123: Uint16Array,
  5125: Uint32Array,
  5126: Float32Array,
};
var P,
  j,
  k,
  V = "undefined" != typeof Float32Array ? Float32Array : Array;
function G(e) {
  var t = e[0],
    s = e[1],
    r = e[2];
  return Math.hypot(t, s, r);
}
function H(e, t, s) {
  var r = t[0],
    n = t[1],
    i = t[2],
    o = s[3] * r + s[7] * n + s[11] * i + s[15];
  return (
    (o = o || 1),
    (e[0] = (s[0] * r + s[4] * n + s[8] * i + s[12]) / o),
    (e[1] = (s[1] * r + s[5] * n + s[9] * i + s[13]) / o),
    (e[2] = (s[2] * r + s[6] * n + s[10] * i + s[14]) / o),
    e
  );
}
function z(e, t) {
  const s = q();
  for (const r of e.listPrimitives()) {
    const e = r.getAttribute("POSITION"),
      n = r.getIndices();
    if (!e) continue;
    let i = [0, 0, 0],
      o = [0, 0, 0];
    for (let r = 0, a = n ? n.getCount() : e.getCount(); r < a; r++) {
      const a = n ? n.getScalar(r) : r;
      (i = e.getElement(a, i)), (o = H(o, i, t)), X(o, s);
    }
  }
  return s;
}
function X(e, t) {
  for (let s = 0; s < 3; s++)
    (t.min[s] = Math.min(e[s], t.min[s])),
      (t.max[s] = Math.max(e[s], t.max[s]));
}
function q() {
  return { min: [1 / 0, 1 / 0, 1 / 0], max: [-1 / 0, -1 / 0, -1 / 0] };
}
Math.hypot ||
  (Math.hypot = function () {
    for (var e = 0, t = arguments.length; t--; )
      e += arguments[t] * arguments[t];
    return Math.sqrt(e);
  }),
  (P = new V(3)),
  V != Float32Array && ((P[0] = 0), (P[1] = 0), (P[2] = 0));
class Y {
  static createBufferFromDataURI(e) {
    if ("undefined" == typeof Buffer) {
      const t = atob(e.split(",")[1]),
        s = new Uint8Array(t.length);
      for (let e = 0; e < t.length; e++) s[e] = t.charCodeAt(e);
      return s;
    }
    {
      const t = e.split(",")[1],
        s = e.indexOf("base64") >= 0;
      return Buffer.from(t, s ? "base64" : "utf8");
    }
  }
  static encodeText(e) {
    return "undefined" != typeof TextEncoder
      ? new TextEncoder().encode(e)
      : Buffer.from(e);
  }
  static decodeText(e) {
    return "undefined" != typeof TextDecoder
      ? new TextDecoder().decode(e)
      : Buffer.from(e).toString("utf8");
  }
  static concat(e) {
    let t = 0;
    for (const s of e) t += s.byteLength;
    const s = new Uint8Array(t);
    let r = 0;
    for (const t of e) s.set(t, r), (r += t.byteLength);
    return s;
  }
  static pad(e, t = 0) {
    const s = this.padNumber(e.byteLength);
    if (s === e.byteLength) return e;
    const r = new Uint8Array(s);
    if ((r.set(e), 0 !== t)) for (let n = e.byteLength; n < s; n++) r[n] = t;
    return r;
  }
  static padNumber(e) {
    return 4 * Math.ceil(e / 4);
  }
  static equals(e, t) {
    if (e === t) return !0;
    if (e.byteLength !== t.byteLength) return !1;
    let s = e.byteLength;
    for (; s--; ) if (e[s] !== t[s]) return !1;
    return !0;
  }
  static toView(e, t = 0, s = 1 / 0) {
    return new Uint8Array(
      e.buffer,
      e.byteOffset + t,
      Math.min(e.byteLength, s)
    );
  }
  static assertView(e) {
    if (e && !ArrayBuffer.isView(e))
      throw new Error(
        `Method requires Uint8Array parameter; received "${typeof e}".`
      );
    return e;
  }
}
class W {
  match(e) {
    return (
      e.length >= 8 &&
      137 === e[0] &&
      80 === e[1] &&
      78 === e[2] &&
      71 === e[3] &&
      13 === e[4] &&
      10 === e[5] &&
      26 === e[6] &&
      10 === e[7]
    );
  }
  getSize(e) {
    const t = new DataView(e.buffer, e.byteOffset);
    return Y.decodeText(e.slice(12, 16)) === W.PNG_FRIED_CHUNK_NAME
      ? [t.getUint32(32, !1), t.getUint32(36, !1)]
      : [t.getUint32(16, !1), t.getUint32(20, !1)];
  }
  getChannels(e) {
    return 4;
  }
}
W.PNG_FRIED_CHUNK_NAME = "CgBI";
class K {
  static registerFormat(e, t) {
    this.impls[e] = t;
  }
  static getMimeType(e) {
    for (const t in this.impls) if (this.impls[t].match(e)) return t;
    return null;
  }
  static getSize(e, t) {
    return this.impls[t] ? this.impls[t].getSize(e) : null;
  }
  static getChannels(e, t) {
    return this.impls[t] ? this.impls[t].getChannels(e) : null;
  }
  static getVRAMByteLength(e, t) {
    if (!this.impls[t]) return null;
    if (this.impls[t].getVRAMByteLength)
      return this.impls[t].getVRAMByteLength(e);
    let s = 0;
    const r = this.getSize(e, t);
    if (!r) return null;
    for (; r[0] > 1 || r[1] > 1; )
      (s += r[0] * r[1] * 4),
        (r[0] = Math.max(Math.floor(r[0] / 2), 1)),
        (r[1] = Math.max(Math.floor(r[1] / 2), 1));
    return (s += 4), s;
  }
  static mimeTypeToExtension(e) {
    return "image/jpeg" === e ? "jpg" : e.split("/").pop();
  }
  static extensionToMimeType(e) {
    return "jpg" === e ? "image/jpeg" : e ? `image/${e}` : "";
  }
}
function $(e, t) {
  if (t > e.byteLength)
    throw new TypeError("Corrupt JPG, exceeded buffer limits");
  if (255 !== e.getUint8(t))
    throw new TypeError("Invalid JPG, marker table corrupted");
  return e;
}
K.impls = {
  "image/jpeg": new (class {
    match(e) {
      return e.length >= 3 && 255 === e[0] && 216 === e[1] && 255 === e[2];
    }
    getSize(e) {
      let t,
        s,
        r = new DataView(e.buffer, e.byteOffset + 4);
      for (; r.byteLength; ) {
        if (
          ((t = r.getUint16(0, !1)),
          $(r, t),
          (s = r.getUint8(t + 1)),
          192 === s || 193 === s || 194 === s)
        )
          return [r.getUint16(t + 7, !1), r.getUint16(t + 5, !1)];
        r = new DataView(e.buffer, r.byteOffset + t + 2);
      }
      throw new TypeError("Invalid JPG, no size found");
    }
    getChannels(e) {
      return 3;
    }
  })(),
  "image/png": new W(),
};
class J {
  static basename(e) {
    const t = e.split(/[\\/]/).pop();
    return t.substring(0, t.lastIndexOf("."));
  }
  static extension(e) {
    if (e.startsWith("data:image/")) {
      const t = e.match(/data:(image\/\w+)/)[1];
      return K.mimeTypeToExtension(t);
    }
    return e.startsWith("data:model/gltf+json")
      ? "gltf"
      : e.startsWith("data:model/gltf-binary")
      ? "glb"
      : e.startsWith("data:application/")
      ? "bin"
      : e.split(/[\\/]/).pop().split(/[.]/).pop();
  }
}
function Q(e) {
  return "[object Object]" === Object.prototype.toString.call(e);
}
function Z(e) {
  if (!1 === Q(e)) return !1;
  const t = e.constructor;
  if (void 0 === t) return !0;
  const s = t.prototype;
  return (
    !1 !== Q(s) &&
    !1 !== Object.prototype.hasOwnProperty.call(s, "isPrototypeOf")
  );
}
!(function (e) {
  (e[(e.SILENT = 4)] = "SILENT"),
    (e[(e.ERROR = 3)] = "ERROR"),
    (e[(e.WARN = 2)] = "WARN"),
    (e[(e.INFO = 1)] = "INFO"),
    (e[(e.DEBUG = 0)] = "DEBUG");
})(k || (k = {}));
class ee {
  constructor(e) {
    (this.verbosity = void 0), (this.verbosity = e);
  }
  debug(e) {
    this.verbosity <= ee.Verbosity.DEBUG && console.debug(e);
  }
  info(e) {
    this.verbosity <= ee.Verbosity.INFO && console.info(e);
  }
  warn(e) {
    this.verbosity <= ee.Verbosity.WARN && console.warn(e);
  }
  error(e) {
    this.verbosity <= ee.Verbosity.ERROR && console.error(e);
  }
}
function te(e, t, s) {
  var r = t[0],
    n = t[1],
    i = t[2],
    o = t[3],
    a = t[4],
    c = t[5],
    u = t[6],
    h = t[7],
    f = t[8],
    l = t[9],
    p = t[10],
    d = t[11],
    g = t[12],
    m = t[13],
    x = t[14],
    T = t[15],
    E = s[0],
    y = s[1],
    I = s[2],
    R = s[3];
  return (
    (e[0] = E * r + y * a + I * f + R * g),
    (e[1] = E * n + y * c + I * l + R * m),
    (e[2] = E * i + y * u + I * p + R * x),
    (e[3] = E * o + y * h + I * d + R * T),
    (E = s[4]),
    (y = s[5]),
    (I = s[6]),
    (R = s[7]),
    (e[4] = E * r + y * a + I * f + R * g),
    (e[5] = E * n + y * c + I * l + R * m),
    (e[6] = E * i + y * u + I * p + R * x),
    (e[7] = E * o + y * h + I * d + R * T),
    (E = s[8]),
    (y = s[9]),
    (I = s[10]),
    (R = s[11]),
    (e[8] = E * r + y * a + I * f + R * g),
    (e[9] = E * n + y * c + I * l + R * m),
    (e[10] = E * i + y * u + I * p + R * x),
    (e[11] = E * o + y * h + I * d + R * T),
    (E = s[12]),
    (y = s[13]),
    (I = s[14]),
    (R = s[15]),
    (e[12] = E * r + y * a + I * f + R * g),
    (e[13] = E * n + y * c + I * l + R * m),
    (e[14] = E * i + y * u + I * p + R * x),
    (e[15] = E * o + y * h + I * d + R * T),
    e
  );
}
function se(e, t) {
  var s = new V(3);
  !(function (e, t) {
    var s = t[0],
      r = t[1],
      n = t[2],
      i = t[4],
      o = t[5],
      a = t[6],
      c = t[8],
      u = t[9],
      h = t[10];
    (e[0] = Math.hypot(s, r, n)),
      (e[1] = Math.hypot(i, o, a)),
      (e[2] = Math.hypot(c, u, h));
  })(s, t);
  var r = 1 / s[0],
    n = 1 / s[1],
    i = 1 / s[2],
    o = t[0] * r,
    a = t[1] * n,
    c = t[2] * i,
    u = t[4] * r,
    h = t[5] * n,
    f = t[6] * i,
    l = t[8] * r,
    p = t[9] * n,
    d = t[10] * i,
    g = o + h + d,
    m = 0;
  return (
    g > 0
      ? ((m = 2 * Math.sqrt(g + 1)),
        (e[3] = 0.25 * m),
        (e[0] = (f - p) / m),
        (e[1] = (l - c) / m),
        (e[2] = (a - u) / m))
      : o > h && o > d
      ? ((m = 2 * Math.sqrt(1 + o - h - d)),
        (e[3] = (f - p) / m),
        (e[0] = 0.25 * m),
        (e[1] = (a + u) / m),
        (e[2] = (l + c) / m))
      : h > d
      ? ((m = 2 * Math.sqrt(1 + h - o - d)),
        (e[3] = (l - c) / m),
        (e[0] = (a + u) / m),
        (e[1] = 0.25 * m),
        (e[2] = (f + p) / m))
      : ((m = 2 * Math.sqrt(1 + d - o - h)),
        (e[3] = (a - u) / m),
        (e[0] = (l + c) / m),
        (e[1] = (f + p) / m),
        (e[2] = 0.25 * m)),
    e
  );
}
(j = ee), (ee.Verbosity = k), (ee.DEFAULT_INSTANCE = new j(j.Verbosity.INFO));
class re {
  static identity(e) {
    return e;
  }
  static eq(e, t, s = 1e-5) {
    if (e.length !== t.length) return !1;
    for (let r = 0; r < e.length; r++) if (Math.abs(e[r] - t[r]) > s) return !1;
    return !0;
  }
  static clamp(e, t, s) {
    return e < t ? t : e > s ? s : e;
  }
  static decodeNormalizedInt(e, t) {
    switch (t) {
      case 5126:
        return e;
      case 5123:
        return e / 65535;
      case 5121:
        return e / 255;
      case 5122:
        return Math.max(e / 32767, -1);
      case 5120:
        return Math.max(e / 127, -1);
      default:
        throw new Error("Invalid component type.");
    }
  }
  static encodeNormalizedInt(e, t) {
    switch (t) {
      case 5126:
        return e;
      case 5123:
        return Math.round(65535 * re.clamp(e, 0, 1));
      case 5121:
        return Math.round(255 * re.clamp(e, 0, 1));
      case 5122:
        return Math.round(32767 * re.clamp(e, -1, 1));
      case 5120:
        return Math.round(127 * re.clamp(e, -1, 1));
      default:
        throw new Error("Invalid component type.");
    }
  }
  static decompose(e, t, s, r) {
    let n = G([e[0], e[1], e[2]]);
    const i = G([e[4], e[5], e[6]]),
      o = G([e[8], e[9], e[10]]);
    var a, c, u, h, f, l, p, d, g, m, x, T, E, y, I, R, N;
    ((c = (a = e)[0]),
    (u = a[1]),
    (h = a[2]),
    (f = a[3]),
    (l = a[4]),
    (p = a[5]),
    (d = a[6]),
    (g = a[7]),
    (m = a[8]),
    (x = a[9]),
    (T = a[10]),
    (E = a[11]),
    (y = a[12]),
    (I = a[13]),
    (R = a[14]),
    (N = a[15]),
    (c * p - u * l) * (T * N - E * R) -
      (c * d - h * l) * (x * N - E * I) +
      (c * g - f * l) * (x * R - T * I) +
      (u * d - h * p) * (m * N - E * y) -
      (u * g - f * p) * (m * R - T * y) +
      (h * g - f * d) * (m * I - x * y)) < 0 && (n = -n),
      (t[0] = e[12]),
      (t[1] = e[13]),
      (t[2] = e[14]);
    const A = e.slice(),
      w = 1 / n,
      b = 1 / i,
      M = 1 / o;
    (A[0] *= w),
      (A[1] *= w),
      (A[2] *= w),
      (A[4] *= b),
      (A[5] *= b),
      (A[6] *= b),
      (A[8] *= M),
      (A[9] *= M),
      (A[10] *= M),
      se(s, A),
      (r[0] = n),
      (r[1] = i),
      (r[2] = o);
  }
  static compose(e, t, s, r) {
    const n = r,
      i = t[0],
      o = t[1],
      a = t[2],
      c = t[3],
      u = i + i,
      h = o + o,
      f = a + a,
      l = i * u,
      p = i * h,
      d = i * f,
      g = o * h,
      m = o * f,
      x = a * f,
      T = c * u,
      E = c * h,
      y = c * f,
      I = s[0],
      R = s[1],
      N = s[2];
    return (
      (n[0] = (1 - (g + x)) * I),
      (n[1] = (p + y) * I),
      (n[2] = (d - E) * I),
      (n[3] = 0),
      (n[4] = (p - y) * R),
      (n[5] = (1 - (l + x)) * R),
      (n[6] = (m + T) * R),
      (n[7] = 0),
      (n[8] = (d + E) * N),
      (n[9] = (m - T) * N),
      (n[10] = (1 - (l + g)) * N),
      (n[11] = 0),
      (n[12] = e[0]),
      (n[13] = e[1]),
      (n[14] = e[2]),
      (n[15] = 1),
      n
    );
  }
}
function ne(e, t) {
  if (!!e != !!t) return !1;
  const s = e.getChild(),
    r = t.getChild();
  return s === r || s.equals(r);
}
function ie(e, t) {
  if (!!e != !!t) return !1;
  const s = e.values(),
    r = t.values();
  if (s.length !== r.length) return !1;
  for (let e = 0; e < s.length; e++) {
    const t = s[e],
      n = r[e];
    if (t.getChild() !== n.getChild() && !t.getChild().equals(n.getChild()))
      return !1;
  }
  return !0;
}
function oe(e, t) {
  if (!!e != !!t) return !1;
  const s = e.keys(),
    r = t.keys();
  if (s.length !== r.length) return !1;
  for (const r of s) {
    const s = e.get(r),
      n = t.get(r);
    if (!!s != !!n) return !1;
    const i = s.getChild(),
      o = n.getChild();
    if (i !== o && !i.equals(o)) return !1;
  }
  return !0;
}
function ae(e, t) {
  if (e === t) return !0;
  if (!!e != !!t || !e || !t) return !1;
  if (e.length !== t.length) return !1;
  for (let s = 0; s < e.length; s++) if (e[s] !== t[s]) return !1;
  return !0;
}
function ce(e, t) {
  if (e === t) return !0;
  if (!!e != !!t) return !1;
  if (!Z(e) || !Z(t)) return e === t;
  const s = e,
    r = t;
  let n,
    i = 0,
    o = 0;
  for (n in s) i++;
  for (n in r) o++;
  if (i !== o) return !1;
  for (n in s) {
    const e = s[n],
      t = r[n];
    if (ue(e) && ue(t)) {
      if (!ae(e, t)) return !1;
    } else if (Z(e) && Z(t)) {
      if (!ce(e, t)) return !1;
    } else if (e !== t) return !1;
  }
  return !0;
}
function ue(e) {
  return Array.isArray(e) || ArrayBuffer.isView(e);
}
const he = "23456789abdegjkmnpqrvwxyzABDEGJKMNPQRVWXYZ",
  fe = new Set(),
  le = function () {
    let e = "";
    for (let t = 0; t < 6; t++) e += he.charAt(Math.floor(42 * Math.random()));
    return e;
  },
  pe = "https://null.example";
class de {
  static dirname(e) {
    const t = e.lastIndexOf("/");
    return -1 === t ? "./" : e.substring(0, t + 1);
  }
  static basename(e) {
    return J.basename(new URL(e, pe).pathname);
  }
  static extension(e) {
    return J.extension(new URL(e, pe).pathname);
  }
  static resolve(e, t) {
    if (!this.isRelativePath(t)) return t;
    const s = e.split("/"),
      r = t.split("/");
    s.pop();
    for (let e = 0; e < r.length; e++)
      "." !== r[e] && (".." === r[e] ? s.pop() : s.push(r[e]));
    return s.join("/");
  }
  static isAbsoluteURL(e) {
    return this.PROTOCOL_REGEXP.test(e);
  }
  static isRelativePath(e) {
    return !/^(?:[a-zA-Z]+:)?\//.test(e);
  }
}
(de.DEFAULT_INIT = {}), (de.PROTOCOL_REGEXP = /^[a-zA-Z]+:\/\//);
const ge = (e) => e,
  me = new Set();
class xe extends _ {
  constructor(e, t = "") {
    super(e),
      (this[M].name = t),
      this.init(),
      this.dispatchEvent({ type: "create" });
  }
  getGraph() {
    return this.graph;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { name: "", extras: {} });
  }
  set(e, t) {
    return Array.isArray(t) && (t = t.slice()), super.set(e, t);
  }
  getName() {
    return this.get("name");
  }
  setName(e) {
    return this.set("name", e);
  }
  getExtras() {
    return this.get("extras");
  }
  setExtras(e) {
    return this.set("extras", e);
  }
  clone() {
    return new (0, this.constructor)(this.graph).copy(this, ge);
  }
  copy(e, t = ge) {
    for (const e in this[M]) {
      const t = this[M][e];
      if (t instanceof I) this[S].has(e) || t.dispose();
      else if (t instanceof A || t instanceof w)
        for (const e of t.values()) e.dispose();
      else if (t instanceof b) for (const e of t.values()) e.dispose();
    }
    for (const s in e[M]) {
      const r = this[M][s],
        n = e[M][s];
      if (n instanceof I)
        if (this[S].has(s)) {
          r.getChild().copy(t(n.getChild()), t);
        } else this.setRef(s, t(n.getChild()), n.getAttributes());
      else if (n instanceof w || n instanceof A)
        for (const e of n.values())
          this.addRef(s, t(e.getChild()), e.getAttributes());
      else if (n instanceof b)
        for (const e of n.keys()) {
          const r = n.get(e);
          this.setRefMap(s, e, t(r.getChild()), r.getAttributes());
        }
      else
        Z(n)
          ? (this[M][s] = JSON.parse(JSON.stringify(n)))
          : Array.isArray(n) ||
            n instanceof ArrayBuffer ||
            ArrayBuffer.isView(n)
          ? (this[M][s] = n.slice())
          : (this[M][s] = n);
    }
    return this;
  }
  equals(e, t = me) {
    if (this === e) return !0;
    if (this.propertyType !== e.propertyType) return !1;
    for (const s in this[M]) {
      if (t.has(s)) continue;
      const r = this[M][s],
        n = e[M][s];
      if (r instanceof I || n instanceof I) {
        if (!ne(r, n)) return !1;
      } else if (
        r instanceof w ||
        n instanceof w ||
        r instanceof A ||
        n instanceof A
      ) {
        if (!ie(r, n)) return !1;
      } else if (r instanceof b || n instanceof b) {
        if (!oe(r, n)) return !1;
      } else if (Z(r) || Z(n)) {
        if (!ce(r, n)) return !1;
      } else if (ue(r) || ue(n)) {
        if (!ae(r, n)) return !1;
      } else if (r !== n) return !1;
    }
    return !0;
  }
  detach() {
    return (
      this.graph.disconnectParents(this, (e) => "Root" !== e.propertyType), this
    );
  }
  listParents() {
    return this.graph.listParents(this);
  }
}
class Te extends xe {
  getDefaults() {
    return Object.assign(super.getDefaults(), { extensions: new b() });
  }
  getExtension(e) {
    return this.getRefMap("extensions", e);
  }
  setExtension(e, t) {
    return t && t._validateParent(this), this.setRefMap("extensions", e, t);
  }
  listExtensions() {
    return this.listRefMapValues("extensions");
  }
}
class Ee extends Te {
  init() {
    this.propertyType = O.ACCESSOR;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      array: null,
      type: Ee.Type.SCALAR,
      componentType: Ee.ComponentType.FLOAT,
      normalized: !1,
      sparse: !1,
      buffer: null,
    });
  }
  static getElementSize(e) {
    switch (e) {
      case Ee.Type.SCALAR:
        return 1;
      case Ee.Type.VEC2:
        return 2;
      case Ee.Type.VEC3:
        return 3;
      case Ee.Type.VEC4:
      case Ee.Type.MAT2:
        return 4;
      case Ee.Type.MAT3:
        return 9;
      case Ee.Type.MAT4:
        return 16;
      default:
        throw new Error("Unexpected type: " + e);
    }
  }
  static getComponentSize(e) {
    switch (e) {
      case Ee.ComponentType.BYTE:
      case Ee.ComponentType.UNSIGNED_BYTE:
        return 1;
      case Ee.ComponentType.SHORT:
      case Ee.ComponentType.UNSIGNED_SHORT:
        return 2;
      case Ee.ComponentType.UNSIGNED_INT:
      case Ee.ComponentType.FLOAT:
        return 4;
      default:
        throw new Error("Unexpected component type: " + e);
    }
  }
  getMinNormalized(e) {
    const t = this.getNormalized(),
      s = this.getElementSize(),
      r = this.getComponentType();
    if ((this.getMin(e), t))
      for (let t = 0; t < s; t++) e[t] = re.decodeNormalizedInt(e[t], r);
    return e;
  }
  getMin(e) {
    const t = this.getArray(),
      s = this.getCount(),
      r = this.getElementSize();
    for (let t = 0; t < r; t++) e[t] = 1 / 0;
    for (let n = 0; n < s * r; n += r)
      for (let s = 0; s < r; s++) {
        const r = t[n + s];
        Number.isFinite(r) && (e[s] = Math.min(e[s], r));
      }
    return e;
  }
  getMaxNormalized(e) {
    const t = this.getNormalized(),
      s = this.getElementSize(),
      r = this.getComponentType();
    if ((this.getMax(e), t))
      for (let t = 0; t < s; t++) e[t] = re.decodeNormalizedInt(e[t], r);
    return e;
  }
  getMax(e) {
    const t = this.get("array"),
      s = this.getCount(),
      r = this.getElementSize();
    for (let t = 0; t < r; t++) e[t] = -1 / 0;
    for (let n = 0; n < s * r; n += r)
      for (let s = 0; s < r; s++) {
        const r = t[n + s];
        Number.isFinite(r) && (e[s] = Math.max(e[s], r));
      }
    return e;
  }
  getCount() {
    const e = this.get("array");
    return e ? e.length / this.getElementSize() : 0;
  }
  getType() {
    return this.get("type");
  }
  setType(e) {
    return this.set("type", e);
  }
  getElementSize() {
    return Ee.getElementSize(this.get("type"));
  }
  getComponentSize() {
    return this.get("array").BYTES_PER_ELEMENT;
  }
  getComponentType() {
    return this.get("componentType");
  }
  getNormalized() {
    return this.get("normalized");
  }
  setNormalized(e) {
    return this.set("normalized", e);
  }
  getScalar(e) {
    const t = this.getElementSize(),
      s = this.getComponentType(),
      r = this.getArray();
    return this.getNormalized()
      ? re.decodeNormalizedInt(r[e * t], s)
      : r[e * t];
  }
  setScalar(e, t) {
    const s = this.getElementSize(),
      r = this.getComponentType(),
      n = this.getArray();
    return (
      this.getNormalized()
        ? (n[e * s] = re.encodeNormalizedInt(t, r))
        : (n[e * s] = t),
      this
    );
  }
  getElement(e, t) {
    const s = this.getNormalized(),
      r = this.getElementSize(),
      n = this.getComponentType(),
      i = this.getArray();
    for (let o = 0; o < r; o++)
      t[o] = s ? re.decodeNormalizedInt(i[e * r + o], n) : i[e * r + o];
    return t;
  }
  setElement(e, t) {
    const s = this.getNormalized(),
      r = this.getElementSize(),
      n = this.getComponentType(),
      i = this.getArray();
    for (let o = 0; o < r; o++)
      i[e * r + o] = s ? re.encodeNormalizedInt(t[o], n) : t[o];
    return this;
  }
  getSparse() {
    return this.get("sparse");
  }
  setSparse(e) {
    return this.set("sparse", e);
  }
  getBuffer() {
    return this.getRef("buffer");
  }
  setBuffer(e) {
    return this.setRef("buffer", e);
  }
  getArray() {
    return this.get("array");
  }
  setArray(e) {
    return (
      this.set(
        "componentType",
        e
          ? (function (e) {
              switch (e.constructor) {
                case Float32Array:
                  return Ee.ComponentType.FLOAT;
                case Uint32Array:
                  return Ee.ComponentType.UNSIGNED_INT;
                case Uint16Array:
                  return Ee.ComponentType.UNSIGNED_SHORT;
                case Uint8Array:
                  return Ee.ComponentType.UNSIGNED_BYTE;
                case Int16Array:
                  return Ee.ComponentType.SHORT;
                case Int8Array:
                  return Ee.ComponentType.BYTE;
                default:
                  throw new Error("Unknown accessor componentType.");
              }
            })(e)
          : Ee.ComponentType.FLOAT
      ),
      this.set("array", e),
      this
    );
  }
  getByteLength() {
    const e = this.get("array");
    return e ? e.byteLength : 0;
  }
}
(Ee.Type = {
  SCALAR: "SCALAR",
  VEC2: "VEC2",
  VEC3: "VEC3",
  VEC4: "VEC4",
  MAT2: "MAT2",
  MAT3: "MAT3",
  MAT4: "MAT4",
}),
  (Ee.ComponentType = {
    BYTE: 5120,
    UNSIGNED_BYTE: 5121,
    SHORT: 5122,
    UNSIGNED_SHORT: 5123,
    UNSIGNED_INT: 5125,
    FLOAT: 5126,
  });
class ye extends Te {
  init() {
    this.propertyType = O.ANIMATION;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      channels: new w(),
      samplers: new w(),
    });
  }
  addChannel(e) {
    return this.addRef("channels", e);
  }
  removeChannel(e) {
    return this.removeRef("channels", e);
  }
  listChannels() {
    return this.listRefs("channels");
  }
  addSampler(e) {
    return this.addRef("samplers", e);
  }
  removeSampler(e) {
    return this.removeRef("samplers", e);
  }
  listSamplers() {
    return this.listRefs("samplers");
  }
}
class Ie extends Te {
  init() {
    this.propertyType = O.ANIMATION_CHANNEL;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      targetPath: null,
      targetNode: null,
      sampler: null,
    });
  }
  getTargetPath() {
    return this.get("targetPath");
  }
  setTargetPath(e) {
    return this.set("targetPath", e);
  }
  getTargetNode() {
    return this.getRef("targetNode");
  }
  setTargetNode(e) {
    return this.setRef("targetNode", e);
  }
  getSampler() {
    return this.getRef("sampler");
  }
  setSampler(e) {
    return this.setRef("sampler", e);
  }
}
Ie.TargetPath = {
  TRANSLATION: "translation",
  ROTATION: "rotation",
  SCALE: "scale",
  WEIGHTS: "weights",
};
class Re extends Te {
  init() {
    this.propertyType = O.ANIMATION_SAMPLER;
  }
  getDefaultAttributes() {
    return Object.assign(super.getDefaults(), {
      interpolation: Re.Interpolation.LINEAR,
      input: null,
      output: null,
    });
  }
  getInterpolation() {
    return this.get("interpolation");
  }
  setInterpolation(e) {
    return this.set("interpolation", e);
  }
  getInput() {
    return this.getRef("input");
  }
  setInput(e) {
    return this.setRef("input", e, { usage: F.OTHER });
  }
  getOutput() {
    return this.getRef("output");
  }
  setOutput(e) {
    return this.setRef("output", e, { usage: F.OTHER });
  }
}
Re.Interpolation = {
  LINEAR: "LINEAR",
  STEP: "STEP",
  CUBICSPLINE: "CUBICSPLINE",
};
class Ne extends Te {
  init() {
    this.propertyType = O.BUFFER;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { uri: "" });
  }
  getURI() {
    return this.get("uri");
  }
  setURI(e) {
    return this.set("uri", e);
  }
}
class Ae extends Te {
  init() {
    this.propertyType = O.CAMERA;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      type: Ae.Type.PERSPECTIVE,
      znear: 0.1,
      zfar: 100,
      aspectRatio: null,
      yfov: (2 * Math.PI * 50) / 360,
      xmag: 1,
      ymag: 1,
    });
  }
  getType() {
    return this.get("type");
  }
  setType(e) {
    return this.set("type", e);
  }
  getZNear() {
    return this.get("znear");
  }
  setZNear(e) {
    return this.set("znear", e);
  }
  getZFar() {
    return this.get("zfar");
  }
  setZFar(e) {
    return this.set("zfar", e);
  }
  getAspectRatio() {
    return this.get("aspectRatio");
  }
  setAspectRatio(e) {
    return this.set("aspectRatio", e);
  }
  getYFov() {
    return this.get("yfov");
  }
  setYFov(e) {
    return this.set("yfov", e);
  }
  getXMag() {
    return this.get("xmag");
  }
  setXMag(e) {
    return this.set("xmag", e);
  }
  getYMag() {
    return this.get("ymag");
  }
  setYMag(e) {
    return this.set("ymag", e);
  }
}
Ae.Type = { PERSPECTIVE: "perspective", ORTHOGRAPHIC: "orthographic" };
class we extends xe {
  _validateParent(e) {
    if (!this.parentTypes.includes(e.propertyType))
      throw new Error(
        `Parent "${e.propertyType}" invalid for child "${this.propertyType}".`
      );
  }
}
we.EXTENSION_NAME = void 0;
class be extends Te {
  init() {
    this.propertyType = O.TEXTURE_INFO;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      texCoord: 0,
      magFilter: null,
      minFilter: null,
      wrapS: be.WrapMode.REPEAT,
      wrapT: be.WrapMode.REPEAT,
    });
  }
  getTexCoord() {
    return this.get("texCoord");
  }
  setTexCoord(e) {
    return this.set("texCoord", e);
  }
  getMagFilter() {
    return this.get("magFilter");
  }
  setMagFilter(e) {
    return this.set("magFilter", e);
  }
  getMinFilter() {
    return this.get("minFilter");
  }
  setMinFilter(e) {
    return this.set("minFilter", e);
  }
  getWrapS() {
    return this.get("wrapS");
  }
  setWrapS(e) {
    return this.set("wrapS", e);
  }
  getWrapT() {
    return this.get("wrapT");
  }
  setWrapT(e) {
    return this.set("wrapT", e);
  }
}
(be.WrapMode = { CLAMP_TO_EDGE: 33071, MIRRORED_REPEAT: 33648, REPEAT: 10497 }),
  (be.MagFilter = { NEAREST: 9728, LINEAR: 9729 }),
  (be.MinFilter = {
    NEAREST: 9728,
    LINEAR: 9729,
    NEAREST_MIPMAP_NEAREST: 9984,
    LINEAR_MIPMAP_NEAREST: 9985,
    NEAREST_MIPMAP_LINEAR: 9986,
    LINEAR_MIPMAP_LINEAR: 9987,
  });
const { R: Me, G: Se, B: _e, A: Ce } = U;
class ve extends Te {
  init() {
    this.propertyType = O.MATERIAL;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      alphaMode: ve.AlphaMode.OPAQUE,
      alphaCutoff: 0.5,
      doubleSided: !1,
      baseColorFactor: [1, 1, 1, 1],
      baseColorTexture: null,
      baseColorTextureInfo: new be(this.graph, "baseColorTextureInfo"),
      emissiveFactor: [0, 0, 0],
      emissiveTexture: null,
      emissiveTextureInfo: new be(this.graph, "emissiveTextureInfo"),
      normalScale: 1,
      normalTexture: null,
      normalTextureInfo: new be(this.graph, "normalTextureInfo"),
      occlusionStrength: 1,
      occlusionTexture: null,
      occlusionTextureInfo: new be(this.graph, "occlusionTextureInfo"),
      roughnessFactor: 1,
      metallicFactor: 1,
      metallicRoughnessTexture: null,
      metallicRoughnessTextureInfo: new be(
        this.graph,
        "metallicRoughnessTextureInfo"
      ),
    });
  }
  getDoubleSided() {
    return this.get("doubleSided");
  }
  setDoubleSided(e) {
    return this.set("doubleSided", e);
  }
  getAlpha() {
    return this.get("baseColorFactor")[3];
  }
  setAlpha(e) {
    const t = this.get("baseColorFactor").slice();
    return (t[3] = e), this.set("baseColorFactor", t);
  }
  getAlphaMode() {
    return this.get("alphaMode");
  }
  setAlphaMode(e) {
    return this.set("alphaMode", e);
  }
  getAlphaCutoff() {
    return this.get("alphaCutoff");
  }
  setAlphaCutoff(e) {
    return this.set("alphaCutoff", e);
  }
  getBaseColorFactor() {
    return this.get("baseColorFactor");
  }
  setBaseColorFactor(e) {
    return this.set("baseColorFactor", e);
  }
  getBaseColorTexture() {
    return this.getRef("baseColorTexture");
  }
  getBaseColorTextureInfo() {
    return this.getRef("baseColorTexture")
      ? this.getRef("baseColorTextureInfo")
      : null;
  }
  setBaseColorTexture(e) {
    return this.setRef("baseColorTexture", e, {
      channels: Me | Se | _e | Ce,
      isColor: !0,
    });
  }
  getEmissiveFactor() {
    return this.get("emissiveFactor");
  }
  setEmissiveFactor(e) {
    return this.set("emissiveFactor", e);
  }
  getEmissiveTexture() {
    return this.getRef("emissiveTexture");
  }
  getEmissiveTextureInfo() {
    return this.getRef("emissiveTexture")
      ? this.getRef("emissiveTextureInfo")
      : null;
  }
  setEmissiveTexture(e) {
    return this.setRef("emissiveTexture", e, {
      channels: Me | Se | _e,
      isColor: !0,
    });
  }
  getNormalScale() {
    return this.get("normalScale");
  }
  setNormalScale(e) {
    return this.set("normalScale", e);
  }
  getNormalTexture() {
    return this.getRef("normalTexture");
  }
  getNormalTextureInfo() {
    return this.getRef("normalTexture")
      ? this.getRef("normalTextureInfo")
      : null;
  }
  setNormalTexture(e) {
    return this.setRef("normalTexture", e, { channels: Me | Se | _e });
  }
  getOcclusionStrength() {
    return this.get("occlusionStrength");
  }
  setOcclusionStrength(e) {
    return this.set("occlusionStrength", e);
  }
  getOcclusionTexture() {
    return this.getRef("occlusionTexture");
  }
  getOcclusionTextureInfo() {
    return this.getRef("occlusionTexture")
      ? this.getRef("occlusionTextureInfo")
      : null;
  }
  setOcclusionTexture(e) {
    return this.setRef("occlusionTexture", e, { channels: Me });
  }
  getRoughnessFactor() {
    return this.get("roughnessFactor");
  }
  setRoughnessFactor(e) {
    return this.set("roughnessFactor", e);
  }
  getMetallicFactor() {
    return this.get("metallicFactor");
  }
  setMetallicFactor(e) {
    return this.set("metallicFactor", e);
  }
  getMetallicRoughnessTexture() {
    return this.getRef("metallicRoughnessTexture");
  }
  getMetallicRoughnessTextureInfo() {
    return this.getRef("metallicRoughnessTexture")
      ? this.getRef("metallicRoughnessTextureInfo")
      : null;
  }
  setMetallicRoughnessTexture(e) {
    return this.setRef("metallicRoughnessTexture", e, { channels: Se | _e });
  }
}
ve.AlphaMode = { OPAQUE: "OPAQUE", MASK: "MASK", BLEND: "BLEND" };
class Oe extends Te {
  init() {
    this.propertyType = O.MESH;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      weights: [],
      primitives: new w(),
    });
  }
  addPrimitive(e) {
    return this.addRef("primitives", e);
  }
  removePrimitive(e) {
    return this.removeRef("primitives", e);
  }
  listPrimitives() {
    return this.listRefs("primitives");
  }
  getWeights() {
    return this.get("weights");
  }
  setWeights(e) {
    return this.set("weights", e);
  }
}
class De extends Te {
  init() {
    this.propertyType = O.NODE;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      translation: [0, 0, 0],
      rotation: [0, 0, 0, 1],
      scale: [1, 1, 1],
      weights: [],
      camera: null,
      mesh: null,
      skin: null,
      children: new w(),
    });
  }
  copy(e, t = ge) {
    if (t === ge) throw new Error("Node cannot be copied.");
    return super.copy(e, t);
  }
  getTranslation() {
    return this.get("translation");
  }
  getRotation() {
    return this.get("rotation");
  }
  getScale() {
    return this.get("scale");
  }
  setTranslation(e) {
    return this.set("translation", e);
  }
  setRotation(e) {
    return this.set("rotation", e);
  }
  setScale(e) {
    return this.set("scale", e);
  }
  getMatrix() {
    return re.compose(
      this.get("translation"),
      this.get("rotation"),
      this.get("scale"),
      []
    );
  }
  setMatrix(e) {
    const t = this.get("translation").slice(),
      s = this.get("rotation").slice(),
      r = this.get("scale").slice();
    return (
      re.decompose(e, t, s, r),
      this.set("translation", t).set("rotation", s).set("scale", r)
    );
  }
  getWorldTranslation() {
    const e = [0, 0, 0];
    return re.decompose(this.getWorldMatrix(), e, [0, 0, 0, 1], [1, 1, 1]), e;
  }
  getWorldRotation() {
    const e = [0, 0, 0, 1];
    return re.decompose(this.getWorldMatrix(), [0, 0, 0], e, [1, 1, 1]), e;
  }
  getWorldScale() {
    const e = [1, 1, 1];
    return re.decompose(this.getWorldMatrix(), [0, 0, 0], [0, 0, 0, 1], e), e;
  }
  getWorldMatrix() {
    const e = [];
    for (let t = this; null != t; t = t.getParentNode()) e.push(t);
    let t;
    const s = e.pop().getMatrix();
    for (; (t = e.pop()); ) te(s, s, t.getMatrix());
    return s;
  }
  addChild(e) {
    const t = e.getParentNode();
    t && t.removeChild(e);
    for (const t of e.listParents())
      t.propertyType === O.SCENE && t.removeChild(e);
    return this.addRef("children", e);
  }
  removeChild(e) {
    return this.removeRef("children", e);
  }
  listChildren() {
    return this.listRefs("children");
  }
  getParentNode() {
    for (const e of this.listParents()) if (e.propertyType === O.NODE) return e;
    return null;
  }
  getMesh() {
    return this.getRef("mesh");
  }
  setMesh(e) {
    return this.setRef("mesh", e);
  }
  getCamera() {
    return this.getRef("camera");
  }
  setCamera(e) {
    return this.setRef("camera", e);
  }
  getSkin() {
    return this.getRef("skin");
  }
  setSkin(e) {
    return this.setRef("skin", e);
  }
  getWeights() {
    return this.get("weights");
  }
  setWeights(e) {
    return this.set("weights", e);
  }
  traverse(e) {
    e(this);
    for (const t of this.listChildren()) t.traverse(e);
    return this;
  }
}
class Fe extends Te {
  init() {
    this.propertyType = O.PRIMITIVE;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      mode: Fe.Mode.TRIANGLES,
      material: null,
      indices: null,
      attributes: new b(),
      targets: new w(),
    });
  }
  getIndices() {
    return this.getRef("indices");
  }
  setIndices(e) {
    return this.setRef("indices", e, { usage: F.ELEMENT_ARRAY_BUFFER });
  }
  getAttribute(e) {
    return this.getRefMap("attributes", e);
  }
  setAttribute(e, t) {
    return this.setRefMap("attributes", e, t, { usage: F.ARRAY_BUFFER });
  }
  listAttributes() {
    return this.listRefMapValues("attributes");
  }
  listSemantics() {
    return this.listRefMapKeys("attributes");
  }
  getMaterial() {
    return this.getRef("material");
  }
  setMaterial(e) {
    return this.setRef("material", e);
  }
  getMode() {
    return this.get("mode");
  }
  setMode(e) {
    return this.set("mode", e);
  }
  listTargets() {
    return this.listRefs("targets");
  }
  addTarget(e) {
    return this.addRef("targets", e);
  }
  removeTarget(e) {
    return this.removeRef("targets", e);
  }
}
Fe.Mode = {
  POINTS: 0,
  LINES: 1,
  LINE_LOOP: 2,
  LINE_STRIP: 3,
  TRIANGLES: 4,
  TRIANGLE_STRIP: 5,
  TRIANGLE_FAN: 6,
};
class Ue extends xe {
  init() {
    this.propertyType = O.PRIMITIVE_TARGET;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { attributes: new b() });
  }
  getAttribute(e) {
    return this.getRefMap("attributes", e);
  }
  setAttribute(e, t) {
    return this.setRefMap("attributes", e, t, { usage: F.ARRAY_BUFFER });
  }
  listAttributes() {
    return this.listRefMapValues("attributes");
  }
  listSemantics() {
    return this.listRefMapKeys("attributes");
  }
}
function Le() {
  return (
    (Le = Object.assign
      ? Object.assign.bind()
      : function (e) {
          for (var t = 1; t < arguments.length; t++) {
            var s = arguments[t];
            for (var r in s)
              Object.prototype.hasOwnProperty.call(s, r) && (e[r] = s[r]);
          }
          return e;
        }),
    Le.apply(this, arguments)
  );
}
class Be extends Te {
  init() {
    this.propertyType = O.SCENE;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { children: new w() });
  }
  copy(e, t = ge) {
    if (t === ge) throw new Error("Scene cannot be copied.");
    return super.copy(e, t);
  }
  addChild(e) {
    const t = e.getParentNode();
    return t && t.removeChild(e), this.addRef("children", e);
  }
  removeChild(e) {
    return this.removeRef("children", e);
  }
  listChildren() {
    return this.listRefs("children");
  }
  traverse(e) {
    for (const t of this.listChildren()) t.traverse(e);
    return this;
  }
}
class Pe extends Te {
  init() {
    this.propertyType = O.SKIN;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      skeleton: null,
      inverseBindMatrices: null,
      joints: new w(),
    });
  }
  getSkeleton() {
    return this.getRef("skeleton");
  }
  setSkeleton(e) {
    return this.setRef("skeleton", e);
  }
  getInverseBindMatrices() {
    return this.getRef("inverseBindMatrices");
  }
  setInverseBindMatrices(e) {
    return this.setRef("inverseBindMatrices", e, {
      usage: F.INVERSE_BIND_MATRICES,
    });
  }
  addJoint(e) {
    return this.addRef("joints", e);
  }
  removeJoint(e) {
    return this.removeRef("joints", e);
  }
  listJoints() {
    return this.listRefs("joints");
  }
}
class je extends Te {
  init() {
    this.propertyType = O.TEXTURE;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      image: null,
      mimeType: "",
      uri: "",
    });
  }
  getMimeType() {
    return (
      this.get("mimeType") ||
      K.extensionToMimeType(J.extension(this.get("uri")))
    );
  }
  setMimeType(e) {
    return this.set("mimeType", e);
  }
  getURI() {
    return this.get("uri");
  }
  setURI(e) {
    this.set("uri", e);
    const t = K.extensionToMimeType(J.extension(e));
    return t && this.set("mimeType", t), this;
  }
  getImage() {
    return this.get("image");
  }
  setImage(e) {
    return this.set("image", Y.assertView(e));
  }
  getSize() {
    const e = this.get("image");
    return e ? K.getSize(e, this.getMimeType()) : null;
  }
}
class ke extends Te {
  init() {
    this.propertyType = O.ROOT;
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      asset: { generator: `glTF-Transform ${C}`, version: "2.0" },
      defaultScene: null,
      accessors: new w(),
      animations: new w(),
      buffers: new w(),
      cameras: new w(),
      materials: new w(),
      meshes: new w(),
      nodes: new w(),
      scenes: new w(),
      skins: new w(),
      textures: new w(),
    });
  }
  constructor(e) {
    super(e),
      (this._extensions = new Set()),
      e.addEventListener("node:create", (e) => {
        this._addChildOfRoot(e.target);
      });
  }
  clone() {
    throw new Error("Root cannot be cloned.");
  }
  copy(e, t = ge) {
    if (t === ge) throw new Error("Root cannot be copied.");
    this.set("asset", Le({}, e.get("asset"))),
      this.setName(e.getName()),
      this.setExtras(Le({}, e.getExtras())),
      this.setDefaultScene(e.getDefaultScene() ? t(e.getDefaultScene()) : null);
    for (const s of e.listRefMapKeys("extensions")) {
      const r = e.getExtension(s);
      this.setExtension(s, t(r));
    }
    return this;
  }
  _addChildOfRoot(e) {
    return (
      e instanceof Be
        ? this.addRef("scenes", e)
        : e instanceof De
        ? this.addRef("nodes", e)
        : e instanceof Ae
        ? this.addRef("cameras", e)
        : e instanceof Pe
        ? this.addRef("skins", e)
        : e instanceof Oe
        ? this.addRef("meshes", e)
        : e instanceof ve
        ? this.addRef("materials", e)
        : e instanceof je
        ? this.addRef("textures", e)
        : e instanceof ye
        ? this.addRef("animations", e)
        : e instanceof Ee
        ? this.addRef("accessors", e)
        : e instanceof Ne && this.addRef("buffers", e),
      this
    );
  }
  getAsset() {
    return this.get("asset");
  }
  listExtensionsUsed() {
    return Array.from(this._extensions);
  }
  listExtensionsRequired() {
    return this.listExtensionsUsed().filter((e) => e.isRequired());
  }
  _enableExtension(e) {
    return this._extensions.add(e), this;
  }
  _disableExtension(e) {
    return this._extensions.delete(e), this;
  }
  listScenes() {
    return this.listRefs("scenes");
  }
  setDefaultScene(e) {
    return this.setRef("defaultScene", e);
  }
  getDefaultScene() {
    return this.getRef("defaultScene");
  }
  listNodes() {
    return this.listRefs("nodes");
  }
  listCameras() {
    return this.listRefs("cameras");
  }
  listSkins() {
    return this.listRefs("skins");
  }
  listMeshes() {
    return this.listRefs("meshes");
  }
  listMaterials() {
    return this.listRefs("materials");
  }
  listTextures() {
    return this.listRefs("textures");
  }
  listAnimations() {
    return this.listRefs("animations");
  }
  listAccessors() {
    return this.listRefs("accessors");
  }
  listBuffers() {
    return this.listRefs("buffers");
  }
}
class Ve {
  static fromGraph(e) {
    return Ve._GRAPH_DOCUMENTS.get(e) || null;
  }
  constructor() {
    (this._graph = new R()),
      (this._root = new ke(this._graph)),
      (this._logger = ee.DEFAULT_INSTANCE),
      Ve._GRAPH_DOCUMENTS.set(this._graph, this);
  }
  getRoot() {
    return this._root;
  }
  getGraph() {
    return this._graph;
  }
  getLogger() {
    return this._logger;
  }
  setLogger(e) {
    return (this._logger = e), this;
  }
  clone() {
    throw new Error(
      "Use 'cloneDocument(source)' from '@gltf-transform/functions'."
    );
  }
  merge(e) {
    throw new Error(
      "Use 'mergeDocuments(target, source)' from '@gltf-transform/functions'."
    );
  }
  async transform(...e) {
    const t = e.map((e) => e.name);
    for (const s of e) await s(this, { stack: t });
    return this;
  }
  createExtension(e) {
    const t = e.EXTENSION_NAME;
    return (
      this.getRoot()
        .listExtensionsUsed()
        .find((e) => e.extensionName === t) || new e(this)
    );
  }
  createScene(e = "") {
    return new Be(this._graph, e);
  }
  createNode(e = "") {
    return new De(this._graph, e);
  }
  createCamera(e = "") {
    return new Ae(this._graph, e);
  }
  createSkin(e = "") {
    return new Pe(this._graph, e);
  }
  createMesh(e = "") {
    return new Oe(this._graph, e);
  }
  createPrimitive() {
    return new Fe(this._graph);
  }
  createPrimitiveTarget(e = "") {
    return new Ue(this._graph, e);
  }
  createMaterial(e = "") {
    return new ve(this._graph, e);
  }
  createTexture(e = "") {
    return new je(this._graph, e);
  }
  createAnimation(e = "") {
    return new ye(this._graph, e);
  }
  createAnimationChannel(e = "") {
    return new Ie(this._graph, e);
  }
  createAnimationSampler(e = "") {
    return new Re(this._graph, e);
  }
  createAccessor(e = "", t = null) {
    return (
      t || (t = this.getRoot().listBuffers()[0]),
      new Ee(this._graph, e).setBuffer(t)
    );
  }
  createBuffer(e = "") {
    return new Ne(this._graph, e);
  }
}
Ve._GRAPH_DOCUMENTS = new WeakMap();
class Ge {
  constructor(e) {
    (this.extensionName = ""),
      (this.prereadTypes = []),
      (this.prewriteTypes = []),
      (this.readDependencies = []),
      (this.writeDependencies = []),
      (this.document = void 0),
      (this.required = !1),
      (this.properties = new Set()),
      (this._listener = void 0),
      (this.document = e),
      e.getRoot()._enableExtension(this),
      (this._listener = (e) => {
        const t = e,
          s = t.target;
        s instanceof we &&
          s.extensionName === this.extensionName &&
          ("node:create" === t.type && this._addExtensionProperty(s),
          "node:dispose" === t.type && this._removeExtensionProperty(s));
      });
    const t = e.getGraph();
    t.addEventListener("node:create", this._listener),
      t.addEventListener("node:dispose", this._listener);
  }
  dispose() {
    this.document.getRoot()._disableExtension(this);
    const e = this.document.getGraph();
    e.removeEventListener("node:create", this._listener),
      e.removeEventListener("node:dispose", this._listener);
    for (const e of this.properties) e.dispose();
  }
  static register() {}
  isRequired() {
    return this.required;
  }
  setRequired(e) {
    return (this.required = e), this;
  }
  listProperties() {
    return Array.from(this.properties);
  }
  _addExtensionProperty(e) {
    return this.properties.add(e), this;
  }
  _removeExtensionProperty(e) {
    return this.properties.delete(e), this;
  }
  install(e, t) {
    return this;
  }
  preread(e, t) {
    return this;
  }
  prewrite(e, t) {
    return this;
  }
}
Ge.EXTENSION_NAME = void 0;
class He {
  constructor(e) {
    (this.jsonDoc = void 0),
      (this.buffers = []),
      (this.bufferViews = []),
      (this.bufferViewBuffers = []),
      (this.accessors = []),
      (this.textures = []),
      (this.textureInfos = new Map()),
      (this.materials = []),
      (this.meshes = []),
      (this.cameras = []),
      (this.nodes = []),
      (this.skins = []),
      (this.animations = []),
      (this.scenes = []),
      (this.jsonDoc = e);
  }
  setTextureInfo(e, t) {
    this.textureInfos.set(e, t),
      void 0 !== t.texCoord && e.setTexCoord(t.texCoord),
      void 0 !== t.extras && e.setExtras(t.extras);
    const s = this.jsonDoc.json.textures[t.index];
    if (void 0 === s.sampler) return;
    const r = this.jsonDoc.json.samplers[s.sampler];
    void 0 !== r.magFilter && e.setMagFilter(r.magFilter),
      void 0 !== r.minFilter && e.setMinFilter(r.minFilter),
      void 0 !== r.wrapS && e.setWrapS(r.wrapS),
      void 0 !== r.wrapT && e.setWrapT(r.wrapT);
  }
}
const ze = { logger: ee.DEFAULT_INSTANCE, extensions: [], dependencies: {} },
  Xe = new Set([
    O.BUFFER,
    O.TEXTURE,
    O.MATERIAL,
    O.MESH,
    O.PRIMITIVE,
    O.NODE,
    O.SCENE,
  ]);
class qe {
  static read(e, t = ze) {
    const s = Le({}, ze, t),
      { json: r } = e,
      n = new Ve().setLogger(s.logger);
    this.validate(e, s);
    const i = new He(e),
      o = r.asset,
      a = n.getRoot().getAsset();
    o.copyright && (a.copyright = o.copyright),
      o.extras && (a.extras = o.extras),
      void 0 !== r.extras && n.getRoot().setExtras(Le({}, r.extras));
    const c = r.extensionsUsed || [],
      u = r.extensionsRequired || [];
    s.extensions.sort((e, t) => (e.EXTENSION_NAME > t.EXTENSION_NAME ? 1 : -1));
    for (const e of s.extensions)
      if (c.includes(e.EXTENSION_NAME)) {
        const t = n
            .createExtension(e)
            .setRequired(u.includes(e.EXTENSION_NAME)),
          r = t.prereadTypes.filter((e) => !Xe.has(e));
        r.length &&
          s.logger.warn(
            `Preread hooks for some types (${r.join()}), requested by extension ${
              t.extensionName
            }, are unsupported. Please file an issue or a PR.`
          );
        for (const e of t.readDependencies) t.install(e, s.dependencies[e]);
      }
    const h = r.buffers || [];
    n
      .getRoot()
      .listExtensionsUsed()
      .filter((e) => e.prereadTypes.includes(O.BUFFER))
      .forEach((e) => e.preread(i, O.BUFFER)),
      (i.buffers = h.map((e) => {
        const t = n.createBuffer(e.name);
        return (
          e.extras && t.setExtras(e.extras),
          e.uri && 0 !== e.uri.indexOf("__") && t.setURI(e.uri),
          t
        );
      }));
    const f = r.bufferViews || [];
    i.bufferViewBuffers = f.map((t, s) => {
      if (!i.bufferViews[s]) {
        const r = e.json.buffers[t.buffer],
          n = r.uri ? e.resources[r.uri] : e.resources[v],
          o = t.byteOffset || 0;
        i.bufferViews[s] = Y.toView(n, o, t.byteLength);
      }
      return i.buffers[t.buffer];
    });
    const l = r.accessors || [];
    i.accessors = l.map((e) => {
      const t = i.bufferViewBuffers[e.bufferView],
        s = n.createAccessor(e.name, t).setType(e.type);
      return (
        e.extras && s.setExtras(e.extras),
        void 0 !== e.normalized && s.setNormalized(e.normalized),
        void 0 === e.bufferView || s.setArray(Ye(e, i)),
        s
      );
    });
    const p = r.images || [],
      d = r.textures || [];
    n
      .getRoot()
      .listExtensionsUsed()
      .filter((e) => e.prereadTypes.includes(O.TEXTURE))
      .forEach((e) => e.preread(i, O.TEXTURE)),
      (i.textures = p.map((t) => {
        const s = n.createTexture(t.name);
        if ((t.extras && s.setExtras(t.extras), void 0 !== t.bufferView)) {
          const n = r.bufferViews[t.bufferView],
            i = e.json.buffers[n.buffer],
            o = i.uri ? e.resources[i.uri] : e.resources[v],
            a = n.byteOffset || 0,
            c = n.byteLength,
            u = o.slice(a, a + c);
          s.setImage(u);
        } else
          void 0 !== t.uri &&
            (s.setImage(e.resources[t.uri]),
            0 !== t.uri.indexOf("__") && s.setURI(t.uri));
        if (void 0 !== t.mimeType) s.setMimeType(t.mimeType);
        else if (t.uri) {
          const e = J.extension(t.uri);
          s.setMimeType(K.extensionToMimeType(e));
        }
        return s;
      })),
      n
        .getRoot()
        .listExtensionsUsed()
        .filter((e) => e.prereadTypes.includes(O.MATERIAL))
        .forEach((e) => e.preread(i, O.MATERIAL));
    const g = r.materials || [];
    (i.materials = g.map((e) => {
      const t = n.createMaterial(e.name);
      e.extras && t.setExtras(e.extras),
        void 0 !== e.alphaMode && t.setAlphaMode(e.alphaMode),
        void 0 !== e.alphaCutoff && t.setAlphaCutoff(e.alphaCutoff),
        void 0 !== e.doubleSided && t.setDoubleSided(e.doubleSided);
      const s = e.pbrMetallicRoughness || {};
      if (
        (void 0 !== s.baseColorFactor &&
          t.setBaseColorFactor(s.baseColorFactor),
        void 0 !== e.emissiveFactor && t.setEmissiveFactor(e.emissiveFactor),
        void 0 !== s.metallicFactor && t.setMetallicFactor(s.metallicFactor),
        void 0 !== s.roughnessFactor && t.setRoughnessFactor(s.roughnessFactor),
        void 0 !== s.baseColorTexture)
      ) {
        const e = s.baseColorTexture,
          r = i.textures[d[e.index].source];
        t.setBaseColorTexture(r),
          i.setTextureInfo(t.getBaseColorTextureInfo(), e);
      }
      if (void 0 !== e.emissiveTexture) {
        const s = e.emissiveTexture,
          r = i.textures[d[s.index].source];
        t.setEmissiveTexture(r),
          i.setTextureInfo(t.getEmissiveTextureInfo(), s);
      }
      if (void 0 !== e.normalTexture) {
        const s = e.normalTexture,
          r = i.textures[d[s.index].source];
        t.setNormalTexture(r),
          i.setTextureInfo(t.getNormalTextureInfo(), s),
          void 0 !== e.normalTexture.scale &&
            t.setNormalScale(e.normalTexture.scale);
      }
      if (void 0 !== e.occlusionTexture) {
        const s = e.occlusionTexture,
          r = i.textures[d[s.index].source];
        t.setOcclusionTexture(r),
          i.setTextureInfo(t.getOcclusionTextureInfo(), s),
          void 0 !== e.occlusionTexture.strength &&
            t.setOcclusionStrength(e.occlusionTexture.strength);
      }
      if (void 0 !== s.metallicRoughnessTexture) {
        const e = s.metallicRoughnessTexture,
          r = i.textures[d[e.index].source];
        t.setMetallicRoughnessTexture(r),
          i.setTextureInfo(t.getMetallicRoughnessTextureInfo(), e);
      }
      return t;
    })),
      n
        .getRoot()
        .listExtensionsUsed()
        .filter((e) => e.prereadTypes.includes(O.MESH))
        .forEach((e) => e.preread(i, O.MESH));
    const m = r.meshes || [];
    n
      .getRoot()
      .listExtensionsUsed()
      .filter((e) => e.prereadTypes.includes(O.PRIMITIVE))
      .forEach((e) => e.preread(i, O.PRIMITIVE)),
      (i.meshes = m.map((e) => {
        const t = n.createMesh(e.name);
        e.extras && t.setExtras(e.extras),
          void 0 !== e.weights && t.setWeights(e.weights);
        return (
          (e.primitives || []).forEach((s) => {
            const r = n.createPrimitive();
            s.extras && r.setExtras(s.extras),
              void 0 !== s.material && r.setMaterial(i.materials[s.material]),
              void 0 !== s.mode && r.setMode(s.mode);
            for (const [e, t] of Object.entries(s.attributes || {}))
              r.setAttribute(e, i.accessors[t]);
            void 0 !== s.indices && r.setIndices(i.accessors[s.indices]);
            const o = (e.extras && e.extras.targetNames) || [];
            (s.targets || []).forEach((e, t) => {
              const s = o[t] || t.toString(),
                a = n.createPrimitiveTarget(s);
              for (const [t, s] of Object.entries(e))
                a.setAttribute(t, i.accessors[s]);
              r.addTarget(a);
            }),
              t.addPrimitive(r);
          }),
          t
        );
      }));
    const x = r.cameras || [];
    i.cameras = x.map((e) => {
      const t = n.createCamera(e.name).setType(e.type);
      if ((e.extras && t.setExtras(e.extras), e.type === Ae.Type.PERSPECTIVE)) {
        const s = e.perspective;
        t.setYFov(s.yfov),
          t.setZNear(s.znear),
          void 0 !== s.zfar && t.setZFar(s.zfar),
          void 0 !== s.aspectRatio && t.setAspectRatio(s.aspectRatio);
      } else {
        const s = e.orthographic;
        t.setZNear(s.znear).setZFar(s.zfar).setXMag(s.xmag).setYMag(s.ymag);
      }
      return t;
    });
    const T = r.nodes || [];
    n
      .getRoot()
      .listExtensionsUsed()
      .filter((e) => e.prereadTypes.includes(O.NODE))
      .forEach((e) => e.preread(i, O.NODE)),
      (i.nodes = T.map((e) => {
        const t = n.createNode(e.name);
        if (
          (e.extras && t.setExtras(e.extras),
          void 0 !== e.translation && t.setTranslation(e.translation),
          void 0 !== e.rotation && t.setRotation(e.rotation),
          void 0 !== e.scale && t.setScale(e.scale),
          void 0 !== e.matrix)
        ) {
          const s = [0, 0, 0],
            r = [0, 0, 0, 1],
            n = [1, 1, 1];
          re.decompose(e.matrix, s, r, n),
            t.setTranslation(s),
            t.setRotation(r),
            t.setScale(n);
        }
        return void 0 !== e.weights && t.setWeights(e.weights), t;
      }));
    const E = r.skins || [];
    (i.skins = E.map((e) => {
      const t = n.createSkin(e.name);
      e.extras && t.setExtras(e.extras),
        void 0 !== e.inverseBindMatrices &&
          t.setInverseBindMatrices(i.accessors[e.inverseBindMatrices]),
        void 0 !== e.skeleton && t.setSkeleton(i.nodes[e.skeleton]);
      for (const s of e.joints) t.addJoint(i.nodes[s]);
      return t;
    })),
      T.map((e, t) => {
        const s = i.nodes[t];
        (e.children || []).forEach((e) => s.addChild(i.nodes[e])),
          void 0 !== e.mesh && s.setMesh(i.meshes[e.mesh]),
          void 0 !== e.camera && s.setCamera(i.cameras[e.camera]),
          void 0 !== e.skin && s.setSkin(i.skins[e.skin]);
      });
    const y = r.animations || [];
    i.animations = y.map((e) => {
      const t = n.createAnimation(e.name);
      e.extras && t.setExtras(e.extras);
      const s = (e.samplers || []).map((e) => {
        const s = n
          .createAnimationSampler()
          .setInput(i.accessors[e.input])
          .setOutput(i.accessors[e.output])
          .setInterpolation(e.interpolation || Re.Interpolation.LINEAR);
        return e.extras && s.setExtras(e.extras), t.addSampler(s), s;
      });
      return (
        (e.channels || []).forEach((e) => {
          const r = n
            .createAnimationChannel()
            .setSampler(s[e.sampler])
            .setTargetPath(e.target.path);
          void 0 !== e.target.node && r.setTargetNode(i.nodes[e.target.node]),
            e.extras && r.setExtras(e.extras),
            t.addChannel(r);
        }),
        t
      );
    });
    const I = r.scenes || [];
    return (
      n
        .getRoot()
        .listExtensionsUsed()
        .filter((e) => e.prereadTypes.includes(O.SCENE))
        .forEach((e) => e.preread(i, O.SCENE)),
      (i.scenes = I.map((e) => {
        const t = n.createScene(e.name);
        e.extras && t.setExtras(e.extras);
        return (
          (e.nodes || []).map((e) => i.nodes[e]).forEach((e) => t.addChild(e)),
          t
        );
      })),
      void 0 !== r.scene && n.getRoot().setDefaultScene(i.scenes[r.scene]),
      n
        .getRoot()
        .listExtensionsUsed()
        .forEach((e) => e.read(i)),
      l.forEach((e, t) => {
        const s = i.accessors[t],
          r = !!e.sparse,
          n = !e.bufferView && !s.getArray();
        (r || n) &&
          s.setSparse(!0).setArray(
            (function (e, t) {
              const s = B[e.componentType],
                r = Ee.getElementSize(e.type);
              let n;
              n = void 0 !== e.bufferView ? Ye(e, t) : new s(e.count * r);
              const i = e.sparse;
              if (!i) return n;
              const o = i.count,
                a = Le({}, e, i.indices, { count: o, type: "SCALAR" }),
                c = Le({}, e, i.values, { count: o }),
                u = Ye(a, t),
                h = Ye(c, t);
              for (let e = 0; e < a.count; e++)
                for (let t = 0; t < r; t++) n[u[e] * r + t] = h[e * r + t];
              return n;
            })(e, i)
          );
      }),
      n
    );
  }
  static validate(e, t) {
    const s = e.json;
    if ("2.0" !== s.asset.version)
      throw new Error(`Unsupported glTF version, "${s.asset.version}".`);
    if (s.extensionsRequired)
      for (const e of s.extensionsRequired)
        if (!t.extensions.find((t) => t.EXTENSION_NAME === e))
          throw new Error(`Missing required extension, "${e}".`);
    if (s.extensionsUsed)
      for (const e of s.extensionsUsed)
        t.extensions.find((t) => t.EXTENSION_NAME === e) ||
          t.logger.warn(`Missing optional extension, "${e}".`);
  }
}
function Ye(e, t) {
  const s = t.jsonDoc,
    r = t.bufferViews[e.bufferView],
    n = s.json.bufferViews[e.bufferView],
    i = B[e.componentType],
    o = Ee.getElementSize(e.type),
    a = i.BYTES_PER_ELEMENT,
    c = o * a;
  if (void 0 !== n.byteStride && n.byteStride !== c)
    return (function (e, t) {
      const s = t.jsonDoc,
        r = t.bufferViews[e.bufferView],
        n = s.json.bufferViews[e.bufferView],
        i = B[e.componentType],
        o = Ee.getElementSize(e.type),
        a = i.BYTES_PER_ELEMENT,
        c = e.byteOffset || 0,
        u = new i(e.count * o),
        h = new DataView(r.buffer, r.byteOffset, r.byteLength),
        f = n.byteStride;
      for (let t = 0; t < e.count; t++)
        for (let s = 0; s < o; s++) {
          const r = c + t * f + s * a;
          let n;
          switch (e.componentType) {
            case Ee.ComponentType.FLOAT:
              n = h.getFloat32(r, !0);
              break;
            case Ee.ComponentType.UNSIGNED_INT:
              n = h.getUint32(r, !0);
              break;
            case Ee.ComponentType.UNSIGNED_SHORT:
              n = h.getUint16(r, !0);
              break;
            case Ee.ComponentType.UNSIGNED_BYTE:
              n = h.getUint8(r);
              break;
            case Ee.ComponentType.SHORT:
              n = h.getInt16(r, !0);
              break;
            case Ee.ComponentType.BYTE:
              n = h.getInt8(r);
              break;
            default:
              throw new Error(`Unexpected componentType "${e.componentType}".`);
          }
          u[t * o + s] = n;
        }
      return u;
    })(e, t);
  const u = r.byteOffset + (e.byteOffset || 0),
    h = e.count * o * a;
  return new i(r.buffer.slice(u, u + h));
}
var We;
!(function (e) {
  (e[(e.ARRAY_BUFFER = 34962)] = "ARRAY_BUFFER"),
    (e[(e.ELEMENT_ARRAY_BUFFER = 34963)] = "ELEMENT_ARRAY_BUFFER");
})(We || (We = {}));
class Ke {
  constructor(e, t, s) {
    (this._doc = void 0),
      (this.jsonDoc = void 0),
      (this.options = void 0),
      (this.accessorIndexMap = new Map()),
      (this.animationIndexMap = new Map()),
      (this.bufferIndexMap = new Map()),
      (this.cameraIndexMap = new Map()),
      (this.skinIndexMap = new Map()),
      (this.materialIndexMap = new Map()),
      (this.meshIndexMap = new Map()),
      (this.nodeIndexMap = new Map()),
      (this.imageIndexMap = new Map()),
      (this.textureDefIndexMap = new Map()),
      (this.textureInfoDefMap = new Map()),
      (this.samplerDefIndexMap = new Map()),
      (this.sceneIndexMap = new Map()),
      (this.imageBufferViews = []),
      (this.otherBufferViews = new Map()),
      (this.otherBufferViewsIndexMap = new Map()),
      (this.extensionData = {}),
      (this.bufferURIGenerator = void 0),
      (this.imageURIGenerator = void 0),
      (this.logger = void 0),
      (this._accessorUsageMap = new Map()),
      (this.accessorUsageGroupedByParent = new Set(["ARRAY_BUFFER"])),
      (this.accessorParents = new Map()),
      (this._doc = e),
      (this.jsonDoc = t),
      (this.options = s);
    const r = e.getRoot(),
      n = r.listBuffers().length,
      i = r.listTextures().length;
    (this.bufferURIGenerator = new $e(n > 1, () => s.basename || "buffer")),
      (this.imageURIGenerator = new $e(
        i > 1,
        (t) =>
          (function (e, t) {
            const s = e
              .getGraph()
              .listParentEdges(t)
              .find((t) => t.getParent() !== e.getRoot());
            return s ? s.getName().replace(/texture$/i, "") : "";
          })(e, t) ||
          s.basename ||
          "texture"
      )),
      (this.logger = e.getLogger());
  }
  createTextureInfoDef(e, t) {
    const s = {
        magFilter: t.getMagFilter() || void 0,
        minFilter: t.getMinFilter() || void 0,
        wrapS: t.getWrapS(),
        wrapT: t.getWrapT(),
      },
      r = JSON.stringify(s);
    this.samplerDefIndexMap.has(r) ||
      (this.samplerDefIndexMap.set(r, this.jsonDoc.json.samplers.length),
      this.jsonDoc.json.samplers.push(s));
    const n = {
        source: this.imageIndexMap.get(e),
        sampler: this.samplerDefIndexMap.get(r),
      },
      i = JSON.stringify(n);
    this.textureDefIndexMap.has(i) ||
      (this.textureDefIndexMap.set(i, this.jsonDoc.json.textures.length),
      this.jsonDoc.json.textures.push(n));
    const o = { index: this.textureDefIndexMap.get(i) };
    return (
      0 !== t.getTexCoord() && (o.texCoord = t.getTexCoord()),
      Object.keys(t.getExtras()).length > 0 && (o.extras = t.getExtras()),
      this.textureInfoDefMap.set(t, o),
      o
    );
  }
  createPropertyDef(e) {
    const t = {};
    return (
      e.getName() && (t.name = e.getName()),
      Object.keys(e.getExtras()).length > 0 && (t.extras = e.getExtras()),
      t
    );
  }
  createAccessorDef(e) {
    const t = this.createPropertyDef(e);
    (t.type = e.getType()),
      (t.componentType = e.getComponentType()),
      (t.count = e.getCount());
    return (
      this._doc
        .getGraph()
        .listParentEdges(e)
        .some(
          (e) =>
            ("attributes" === e.getName() &&
              "POSITION" === e.getAttributes().key) ||
            "input" === e.getName()
        ) &&
        ((t.max = e.getMax([]).map(Math.fround)),
        (t.min = e.getMin([]).map(Math.fround))),
      e.getNormalized() && (t.normalized = e.getNormalized()),
      t
    );
  }
  createImageData(e, t, s) {
    if (this.options.format === L.GLB)
      this.imageBufferViews.push(t),
        (e.bufferView = this.jsonDoc.json.bufferViews.length),
        this.jsonDoc.json.bufferViews.push({
          buffer: 0,
          byteOffset: -1,
          byteLength: t.byteLength,
        });
    else {
      const r = K.mimeTypeToExtension(s.getMimeType());
      (e.uri = this.imageURIGenerator.createURI(s, r)),
        (this.jsonDoc.resources[e.uri] = t);
    }
  }
  getAccessorUsage(e) {
    const t = this._accessorUsageMap.get(e);
    if (t) return t;
    if (e.getSparse()) return F.SPARSE;
    for (const t of this._doc.getGraph().listParentEdges(e)) {
      const { usage: e } = t.getAttributes();
      if (e) return e;
      t.getParent().propertyType !== O.ROOT &&
        this.logger.warn(
          `Missing attribute ".usage" on edge, "${t.getName()}".`
        );
    }
    return F.OTHER;
  }
  addAccessorToUsageGroup(e, t) {
    const s = this._accessorUsageMap.get(e);
    if (s && s !== t)
      throw new Error(`Accessor with usage "${s}" cannot be reused as "${t}".`);
    return this._accessorUsageMap.set(e, t), this;
  }
}
(Ke.BufferViewTarget = We),
  (Ke.BufferViewUsage = F),
  (Ke.USAGE_TO_TARGET = {
    [F.ARRAY_BUFFER]: We.ARRAY_BUFFER,
    [F.ELEMENT_ARRAY_BUFFER]: We.ELEMENT_ARRAY_BUFFER,
  });
class $e {
  constructor(e, t) {
    (this.multiple = void 0),
      (this.basename = void 0),
      (this.counter = {}),
      (this.multiple = e),
      (this.basename = t);
  }
  createURI(e, t) {
    if (e.getURI()) return e.getURI();
    if (this.multiple) {
      const s = this.basename(e);
      return (
        (this.counter[s] = this.counter[s] || 1),
        `${s}_${this.counter[s]++}.${t}`
      );
    }
    return `${this.basename(e)}.${t}`;
  }
}
const { BufferViewUsage: Je } = Ke,
  {
    UNSIGNED_INT: Qe,
    UNSIGNED_SHORT: Ze,
    UNSIGNED_BYTE: et,
  } = Ee.ComponentType,
  tt = new Set([O.ACCESSOR, O.BUFFER, O.MATERIAL, O.MESH]);
class st {
  static write(e, t) {
    const s = e.getGraph(),
      r = e.getRoot(),
      n = {
        asset: Le({ generator: `glTF-Transform ${C}` }, r.getAsset()),
        extras: Le({}, r.getExtras()),
      },
      i = { json: n, resources: {} },
      o = new Ke(e, i, t),
      a = t.logger || ee.DEFAULT_INSTANCE,
      c = new Set(t.extensions.map((e) => e.EXTENSION_NAME)),
      u = e
        .getRoot()
        .listExtensionsUsed()
        .filter((e) => c.has(e.extensionName))
        .sort((e, t) => (e.extensionName > t.extensionName ? 1 : -1)),
      h = e
        .getRoot()
        .listExtensionsRequired()
        .filter((e) => c.has(e.extensionName))
        .sort((e, t) => (e.extensionName > t.extensionName ? 1 : -1));
    u.length < e.getRoot().listExtensionsUsed().length &&
      a.warn(
        "Some extensions were not registered for I/O, and will not be written."
      );
    for (const e of u) {
      const s = e.prewriteTypes.filter((e) => !tt.has(e));
      s.length &&
        a.warn(
          `Prewrite hooks for some types (${s.join()}), requested by extension ${
            e.extensionName
          }, are unsupported. Please file an issue or a PR.`
        );
      for (const s of e.writeDependencies) e.install(s, t.dependencies[s]);
    }
    function f(e, t, s, r) {
      const i = [];
      let a = 0;
      for (const t of e) {
        const e = o.createAccessorDef(t);
        e.bufferView = n.bufferViews.length;
        const s = t.getArray(),
          r = Y.pad(Y.toView(s));
        (e.byteOffset = a),
          (a += r.byteLength),
          i.push(r),
          o.accessorIndexMap.set(t, n.accessors.length),
          n.accessors.push(e);
      }
      const c = {
        buffer: t,
        byteOffset: s,
        byteLength: Y.concat(i).byteLength,
      };
      return (
        r && (c.target = r),
        n.bufferViews.push(c),
        { buffers: i, byteLength: a }
      );
    }
    function l(e, t, s) {
      const r = e[0].getCount();
      let i = 0;
      for (const t of e) {
        const e = o.createAccessorDef(t);
        (e.bufferView = n.bufferViews.length), (e.byteOffset = i);
        const s = t.getElementSize(),
          r = t.getComponentSize();
        (i += Y.padNumber(s * r)),
          o.accessorIndexMap.set(t, n.accessors.length),
          n.accessors.push(e);
      }
      const a = r * i,
        c = new ArrayBuffer(a),
        u = new DataView(c);
      for (let t = 0; t < r; t++) {
        let s = 0;
        for (const r of e) {
          const e = r.getElementSize(),
            n = r.getComponentSize(),
            o = r.getComponentType(),
            a = r.getArray();
          for (let r = 0; r < e; r++) {
            const c = t * i + s + r * n,
              h = a[t * e + r];
            switch (o) {
              case Ee.ComponentType.FLOAT:
                u.setFloat32(c, h, !0);
                break;
              case Ee.ComponentType.BYTE:
                u.setInt8(c, h);
                break;
              case Ee.ComponentType.SHORT:
                u.setInt16(c, h, !0);
                break;
              case Ee.ComponentType.UNSIGNED_BYTE:
                u.setUint8(c, h);
                break;
              case Ee.ComponentType.UNSIGNED_SHORT:
                u.setUint16(c, h, !0);
                break;
              case Ee.ComponentType.UNSIGNED_INT:
                u.setUint32(c, h, !0);
                break;
              default:
                throw new Error("Unexpected component type: " + o);
            }
          }
          s += Y.padNumber(e * n);
        }
      }
      const h = {
        buffer: t,
        byteOffset: s,
        byteLength: a,
        byteStride: i,
        target: Ke.BufferViewTarget.ARRAY_BUFFER,
      };
      return (
        n.bufferViews.push(h), { byteLength: a, buffers: [new Uint8Array(c)] }
      );
    }
    function p(e, t, s) {
      const r = [];
      let i = 0;
      const c = new Map();
      let u = -1 / 0,
        h = !1;
      for (const t of e) {
        const e = o.createAccessorDef(t);
        n.accessors.push(e), o.accessorIndexMap.set(t, n.accessors.length - 1);
        const s = [],
          r = [],
          i = [],
          a = new Array(t.getElementSize()).fill(0);
        for (let e = 0, n = t.getCount(); e < n; e++)
          if ((t.getElement(e, i), !re.eq(i, a, 0))) {
            (u = Math.max(e, u)), s.push(e);
            for (let e = 0; e < i.length; e++) r.push(i[e]);
          }
        const f = s.length,
          l = { accessorDef: e, count: f };
        if ((c.set(t, l), 0 === f)) continue;
        f > t.getCount() / 2 && (h = !0);
        const p = B[t.getComponentType()];
        (l.indices = s), (l.values = new p(r));
      }
      if (!Number.isFinite(u)) return { buffers: r, byteLength: i };
      h &&
        a.warn(
          "Some sparse accessors have >50% non-zero elements, which may increase file size."
        );
      const f = u < 255 ? Uint8Array : u < 65535 ? Uint16Array : Uint32Array,
        l = u < 255 ? et : u < 65535 ? Ze : Qe,
        p = { buffer: t, byteOffset: s + i, byteLength: 0 };
      for (const t of e) {
        const e = c.get(t);
        if (0 === e.count) continue;
        e.indicesByteOffset = p.byteLength;
        const s = Y.pad(Y.toView(new f(e.indices)));
        r.push(s), (i += s.byteLength), (p.byteLength += s.byteLength);
      }
      n.bufferViews.push(p);
      const d = n.bufferViews.length - 1,
        g = { buffer: t, byteOffset: s + i, byteLength: 0 };
      for (const t of e) {
        const e = c.get(t);
        if (0 === e.count) continue;
        e.valuesByteOffset = g.byteLength;
        const s = Y.pad(Y.toView(e.values));
        r.push(s), (i += s.byteLength), (g.byteLength += s.byteLength);
      }
      n.bufferViews.push(g);
      const m = n.bufferViews.length - 1;
      for (const t of e) {
        const e = c.get(t);
        0 !== e.count &&
          (e.accessorDef.sparse = {
            count: e.count,
            indices: {
              bufferView: d,
              byteOffset: e.indicesByteOffset,
              componentType: l,
            },
            values: { bufferView: m, byteOffset: e.valuesByteOffset },
          });
      }
      return { buffers: r, byteLength: i };
    }
    (n.accessors = []),
      (n.bufferViews = []),
      (n.samplers = []),
      (n.textures = []),
      (n.images = r.listTextures().map((e, t) => {
        const s = o.createPropertyDef(e);
        e.getMimeType() && (s.mimeType = e.getMimeType());
        const r = e.getImage();
        return r && o.createImageData(s, r, e), o.imageIndexMap.set(e, t), s;
      })),
      u
        .filter((e) => e.prewriteTypes.includes(O.ACCESSOR))
        .forEach((e) => e.prewrite(o, O.ACCESSOR)),
      r.listAccessors().forEach((e) => {
        const t = o.accessorUsageGroupedByParent,
          r = o.accessorParents;
        if (o.accessorIndexMap.has(e)) return;
        const n = o.getAccessorUsage(e);
        if ((o.addAccessorToUsageGroup(e, n), t.has(n))) {
          const t = s.listParents(e).find((e) => e.propertyType !== O.ROOT);
          r.set(e, t);
        }
      }),
      u
        .filter((e) => e.prewriteTypes.includes(O.BUFFER))
        .forEach((e) => e.prewrite(o, O.BUFFER));
    if (
      (r.listAccessors().length > 0 ||
        r.listTextures().length > 0 ||
        o.otherBufferViews.size > 0) &&
      0 === r.listBuffers().length
    )
      throw new Error(
        "Buffer required for Document resources, but none was found."
      );
    (n.buffers = []),
      r.listBuffers().forEach((e, s) => {
        const r = o.createPropertyDef(e),
          a = o.accessorUsageGroupedByParent,
          c = e.listParents().filter((e) => e instanceof Ee),
          u = new Set(c.map((e) => o.accessorParents.get(e))),
          h = new Map(Array.from(u).map((e, t) => [e, t])),
          d = {};
        for (const e of c) {
          var g;
          if (o.accessorIndexMap.has(e)) continue;
          const t = o.getAccessorUsage(e);
          let s = t;
          if (a.has(t)) {
            const t = o.accessorParents.get(e);
            s += `:${h.get(t)}`;
          }
          d[(g = s)] || (d[g] = { usage: t, accessors: [] }),
            d[s].accessors.push(e);
        }
        const m = [],
          x = n.buffers.length;
        let T = 0;
        for (const { usage: e, accessors: s } of Object.values(d))
          if (e === Je.ARRAY_BUFFER && t.vertexLayout === D.INTERLEAVED) {
            const e = l(s, x, T);
            (T += e.byteLength), m.push(...e.buffers);
          } else if (e === Je.ARRAY_BUFFER)
            for (const e of s) {
              const t = l([e], x, T);
              (T += t.byteLength), m.push(...t.buffers);
            }
          else if (e === Je.SPARSE) {
            const e = p(s, x, T);
            (T += e.byteLength), m.push(...e.buffers);
          } else if (e === Je.ELEMENT_ARRAY_BUFFER) {
            const e = f(s, x, T, Ke.BufferViewTarget.ELEMENT_ARRAY_BUFFER);
            (T += e.byteLength), m.push(...e.buffers);
          } else {
            const e = f(s, x, T);
            (T += e.byteLength), m.push(...e.buffers);
          }
        if (o.imageBufferViews.length && 0 === s)
          for (let e = 0; e < o.imageBufferViews.length; e++)
            if (
              ((n.bufferViews[n.images[e].bufferView].byteOffset = T),
              (T += o.imageBufferViews[e].byteLength),
              m.push(o.imageBufferViews[e]),
              T % 8)
            ) {
              const e = 8 - (T % 8);
              (T += e), m.push(new Uint8Array(e));
            }
        if (o.otherBufferViews.has(e))
          for (const t of o.otherBufferViews.get(e))
            n.bufferViews.push({
              buffer: x,
              byteOffset: T,
              byteLength: t.byteLength,
            }),
              o.otherBufferViewsIndexMap.set(t, n.bufferViews.length - 1),
              (T += t.byteLength),
              m.push(t);
        if (T) {
          let s;
          t.format === L.GLB
            ? (s = v)
            : ((s = o.bufferURIGenerator.createURI(e, "bin")), (r.uri = s)),
            (r.byteLength = T),
            (i.resources[s] = Y.concat(m));
        }
        n.buffers.push(r), o.bufferIndexMap.set(e, s);
      }),
      r.listAccessors().find((e) => !e.getBuffer()) &&
        a.warn("Skipped writing one or more Accessors: no Buffer assigned."),
      u
        .filter((e) => e.prewriteTypes.includes(O.MATERIAL))
        .forEach((e) => e.prewrite(o, O.MATERIAL)),
      (n.materials = r.listMaterials().map((e, t) => {
        const s = o.createPropertyDef(e);
        if (
          (e.getAlphaMode() !== ve.AlphaMode.OPAQUE &&
            (s.alphaMode = e.getAlphaMode()),
          e.getAlphaMode() === ve.AlphaMode.MASK &&
            (s.alphaCutoff = e.getAlphaCutoff()),
          e.getDoubleSided() && (s.doubleSided = !0),
          (s.pbrMetallicRoughness = {}),
          re.eq(e.getBaseColorFactor(), [1, 1, 1, 1]) ||
            (s.pbrMetallicRoughness.baseColorFactor = e.getBaseColorFactor()),
          re.eq(e.getEmissiveFactor(), [0, 0, 0]) ||
            (s.emissiveFactor = e.getEmissiveFactor()),
          1 !== e.getRoughnessFactor() &&
            (s.pbrMetallicRoughness.roughnessFactor = e.getRoughnessFactor()),
          1 !== e.getMetallicFactor() &&
            (s.pbrMetallicRoughness.metallicFactor = e.getMetallicFactor()),
          e.getBaseColorTexture())
        ) {
          const t = e.getBaseColorTexture(),
            r = e.getBaseColorTextureInfo();
          s.pbrMetallicRoughness.baseColorTexture = o.createTextureInfoDef(
            t,
            r
          );
        }
        if (e.getEmissiveTexture()) {
          const t = e.getEmissiveTexture(),
            r = e.getEmissiveTextureInfo();
          s.emissiveTexture = o.createTextureInfoDef(t, r);
        }
        if (e.getNormalTexture()) {
          const t = e.getNormalTexture(),
            r = e.getNormalTextureInfo(),
            n = o.createTextureInfoDef(t, r);
          1 !== e.getNormalScale() && (n.scale = e.getNormalScale()),
            (s.normalTexture = n);
        }
        if (e.getOcclusionTexture()) {
          const t = e.getOcclusionTexture(),
            r = e.getOcclusionTextureInfo(),
            n = o.createTextureInfoDef(t, r);
          1 !== e.getOcclusionStrength() &&
            (n.strength = e.getOcclusionStrength()),
            (s.occlusionTexture = n);
        }
        if (e.getMetallicRoughnessTexture()) {
          const t = e.getMetallicRoughnessTexture(),
            r = e.getMetallicRoughnessTextureInfo();
          s.pbrMetallicRoughness.metallicRoughnessTexture =
            o.createTextureInfoDef(t, r);
        }
        return o.materialIndexMap.set(e, t), s;
      })),
      u
        .filter((e) => e.prewriteTypes.includes(O.MESH))
        .forEach((e) => e.prewrite(o, O.MESH)),
      (n.meshes = r.listMeshes().map((e, t) => {
        const s = o.createPropertyDef(e);
        let r = null;
        return (
          (s.primitives = e.listPrimitives().map((e) => {
            const t = { attributes: {} };
            t.mode = e.getMode();
            const s = e.getMaterial();
            s && (t.material = o.materialIndexMap.get(s)),
              Object.keys(e.getExtras()).length && (t.extras = e.getExtras());
            const n = e.getIndices();
            n && (t.indices = o.accessorIndexMap.get(n));
            for (const s of e.listSemantics())
              t.attributes[s] = o.accessorIndexMap.get(e.getAttribute(s));
            for (const s of e.listTargets()) {
              const e = {};
              for (const t of s.listSemantics())
                e[t] = o.accessorIndexMap.get(s.getAttribute(t));
              (t.targets = t.targets || []), t.targets.push(e);
            }
            return (
              e.listTargets().length &&
                !r &&
                (r = e.listTargets().map((e) => e.getName())),
              t
            );
          })),
          e.getWeights().length && (s.weights = e.getWeights()),
          r && ((s.extras = s.extras || {}), (s.extras.targetNames = r)),
          o.meshIndexMap.set(e, t),
          s
        );
      })),
      (n.cameras = r.listCameras().map((e, t) => {
        const s = o.createPropertyDef(e);
        if (((s.type = e.getType()), s.type === Ae.Type.PERSPECTIVE)) {
          s.perspective = {
            znear: e.getZNear(),
            zfar: e.getZFar(),
            yfov: e.getYFov(),
          };
          const t = e.getAspectRatio();
          null !== t && (s.perspective.aspectRatio = t);
        } else
          s.orthographic = {
            znear: e.getZNear(),
            zfar: e.getZFar(),
            xmag: e.getXMag(),
            ymag: e.getYMag(),
          };
        return o.cameraIndexMap.set(e, t), s;
      })),
      (n.nodes = r.listNodes().map((e, t) => {
        const s = o.createPropertyDef(e);
        return (
          re.eq(e.getTranslation(), [0, 0, 0]) ||
            (s.translation = e.getTranslation()),
          re.eq(e.getRotation(), [0, 0, 0, 1]) ||
            (s.rotation = e.getRotation()),
          re.eq(e.getScale(), [1, 1, 1]) || (s.scale = e.getScale()),
          e.getWeights().length && (s.weights = e.getWeights()),
          o.nodeIndexMap.set(e, t),
          s
        );
      })),
      (n.skins = r.listSkins().map((e, t) => {
        const s = o.createPropertyDef(e),
          r = e.getInverseBindMatrices();
        r && (s.inverseBindMatrices = o.accessorIndexMap.get(r));
        const n = e.getSkeleton();
        return (
          n && (s.skeleton = o.nodeIndexMap.get(n)),
          (s.joints = e.listJoints().map((e) => o.nodeIndexMap.get(e))),
          o.skinIndexMap.set(e, t),
          s
        );
      })),
      r.listNodes().forEach((e, t) => {
        const s = n.nodes[t],
          r = e.getMesh();
        r && (s.mesh = o.meshIndexMap.get(r));
        const i = e.getCamera();
        i && (s.camera = o.cameraIndexMap.get(i));
        const a = e.getSkin();
        a && (s.skin = o.skinIndexMap.get(a)),
          e.listChildren().length > 0 &&
            (s.children = e.listChildren().map((e) => o.nodeIndexMap.get(e)));
      }),
      (n.animations = r.listAnimations().map((e, t) => {
        const s = o.createPropertyDef(e),
          r = new Map();
        return (
          (s.samplers = e.listSamplers().map((e, t) => {
            const s = o.createPropertyDef(e);
            return (
              (s.input = o.accessorIndexMap.get(e.getInput())),
              (s.output = o.accessorIndexMap.get(e.getOutput())),
              (s.interpolation = e.getInterpolation()),
              r.set(e, t),
              s
            );
          })),
          (s.channels = e.listChannels().map((e) => {
            const t = o.createPropertyDef(e);
            return (
              (t.sampler = r.get(e.getSampler())),
              (t.target = {
                node: o.nodeIndexMap.get(e.getTargetNode()),
                path: e.getTargetPath(),
              }),
              t
            );
          })),
          o.animationIndexMap.set(e, t),
          s
        );
      })),
      (n.scenes = r.listScenes().map((e, t) => {
        const s = o.createPropertyDef(e);
        return (
          (s.nodes = e.listChildren().map((e) => o.nodeIndexMap.get(e))),
          o.sceneIndexMap.set(e, t),
          s
        );
      }));
    const d = r.getDefaultScene();
    return (
      d && (n.scene = r.listScenes().indexOf(d)),
      (n.extensionsUsed = u.map((e) => e.extensionName)),
      (n.extensionsRequired = h.map((e) => e.extensionName)),
      u.forEach((e) => e.write(o)),
      (function (e) {
        const t = [];
        for (const s in e) {
          const r = e[s];
          ((Array.isArray(r) && 0 === r.length) ||
            null === r ||
            "" === r ||
            (r && "object" == typeof r && 0 === Object.keys(r).length)) &&
            t.push(s);
        }
        for (const s of t) delete e[s];
      })(n),
      i
    );
  }
}
var rt;
!(function (e) {
  (e[(e.JSON = 1313821514)] = "JSON"), (e[(e.BIN = 5130562)] = "BIN");
})(rt || (rt = {}));
class nt {
  constructor() {
    (this._logger = ee.DEFAULT_INSTANCE),
      (this._extensions = new Set()),
      (this._dependencies = {}),
      (this._vertexLayout = D.INTERLEAVED),
      (this.lastReadBytes = 0),
      (this.lastWriteBytes = 0);
  }
  setLogger(e) {
    return (this._logger = e), this;
  }
  registerExtensions(e) {
    for (const t of e) this._extensions.add(t), t.register();
    return this;
  }
  registerDependencies(e) {
    return Object.assign(this._dependencies, e), this;
  }
  setVertexLayout(e) {
    return (this._vertexLayout = e), this;
  }
  async read(e) {
    return await this.readJSON(await this.readAsJSON(e));
  }
  async readAsJSON(e) {
    const t = await this.readURI(e, "view");
    this.lastReadBytes = t.byteLength;
    const s = it(t)
      ? this._binaryToJSON(t)
      : { json: JSON.parse(Y.decodeText(t)), resources: {} };
    return (
      await this._readResourcesExternal(s, this.dirname(e)),
      this._readResourcesInternal(s),
      s
    );
  }
  async readJSON(e) {
    return (
      (e = this._copyJSON(e)),
      this._readResourcesInternal(e),
      qe.read(e, {
        extensions: Array.from(this._extensions),
        dependencies: this._dependencies,
        logger: this._logger,
      })
    );
  }
  async binaryToJSON(e) {
    const t = this._binaryToJSON(Y.assertView(e));
    this._readResourcesInternal(t);
    const s = t.json;
    if (
      s.buffers &&
      s.buffers.some((e) =>
        (function (e, t) {
          return void 0 !== t.uri && !(t.uri in e.resources);
        })(t, e)
      )
    )
      throw new Error("Cannot resolve external buffers with binaryToJSON().");
    if (
      s.images &&
      s.images.some((e) =>
        (function (e, t) {
          return (
            void 0 !== t.uri &&
            !(t.uri in e.resources) &&
            void 0 === t.bufferView
          );
        })(t, e)
      )
    )
      throw new Error("Cannot resolve external images with binaryToJSON().");
    return t;
  }
  async readBinary(e) {
    return this.readJSON(await this.binaryToJSON(Y.assertView(e)));
  }
  async writeJSON(e, t = {}) {
    if (t.format === L.GLB && e.getRoot().listBuffers().length > 1)
      throw new Error("GLB must have 0–1 buffers.");
    return st.write(e, {
      format: t.format || L.GLTF,
      basename: t.basename || "",
      logger: this._logger,
      vertexLayout: this._vertexLayout,
      dependencies: Le({}, this._dependencies),
      extensions: Array.from(this._extensions),
    });
  }
  async writeBinary(e) {
    const { json: t, resources: s } = await this.writeJSON(e, {
        format: L.GLB,
      }),
      r = new Uint32Array([1179937895, 2, 12]),
      n = JSON.stringify(t),
      i = Y.pad(Y.encodeText(n), 32),
      o = Y.toView(new Uint32Array([i.byteLength, 1313821514])),
      a = Y.concat([o, i]);
    r[r.length - 1] += a.byteLength;
    const c = Object.values(s)[0];
    if (!c || !c.byteLength) return Y.concat([Y.toView(r), a]);
    const u = Y.pad(c, 0),
      h = Y.toView(new Uint32Array([u.byteLength, 5130562])),
      f = Y.concat([h, u]);
    return (r[r.length - 1] += f.byteLength), Y.concat([Y.toView(r), a, f]);
  }
  async _readResourcesExternal(e, t) {
    var s = this;
    const r = [...(e.json.images || []), ...(e.json.buffers || [])].map(
      async function (r) {
        const n = r.uri;
        if (!n || n.match(/data:/)) return Promise.resolve();
        (e.resources[n] = await s.readURI(s.resolve(t, n), "view")),
          (s.lastReadBytes += e.resources[n].byteLength);
      }
    );
    await Promise.all(r);
  }
  _readResourcesInternal(e) {
    function t(t) {
      if (t.uri)
        if (t.uri in e.resources) Y.assertView(e.resources[t.uri]);
        else if (t.uri.match(/data:/)) {
          const s = `__${(function () {
            for (let e = 0; e < 999; e++) {
              const e = le();
              if (!fe.has(e)) return fe.add(e), e;
            }
            return "";
          })()}.${J.extension(t.uri)}`;
          (e.resources[s] = Y.createBufferFromDataURI(t.uri)), (t.uri = s);
        }
    }
    (e.json.images || []).forEach((e) => {
      if (void 0 === e.bufferView && void 0 === e.uri)
        throw new Error("Missing resource URI or buffer view.");
      t(e);
    });
    (e.json.buffers || []).forEach(t);
  }
  _copyJSON(e) {
    const { images: t, buffers: s } = e.json;
    return (
      (e = { json: Le({}, e.json), resources: Le({}, e.resources) }),
      t && (e.json.images = t.map((e) => Le({}, e))),
      s && (e.json.buffers = s.map((e) => Le({}, e))),
      e
    );
  }
  _binaryToJSON(e) {
    if (!it(e)) throw new Error("Invalid glTF 2.0 binary.");
    const t = new Uint32Array(e.buffer, e.byteOffset + 12, 2);
    if (t[1] !== rt.JSON) throw new Error("Missing required GLB JSON chunk.");
    const s = t[0],
      r = Y.decodeText(Y.toView(e, 20, s)),
      n = JSON.parse(r),
      i = 20 + s;
    if (e.byteLength <= i) return { json: n, resources: {} };
    const o = new Uint32Array(e.buffer, e.byteOffset + i, 2);
    if (o[1] !== rt.BIN) return { json: n, resources: {} };
    const a = o[0],
      c = Y.toView(e, i + 8, a);
    return { json: n, resources: { [v]: c } };
  }
}
function it(e) {
  if (e.byteLength < 3 * Uint32Array.BYTES_PER_ELEMENT) return !1;
  const t = new Uint32Array(e.buffer, e.byteOffset, 3);
  return 1179937895 === t[0] && 2 === t[1];
}
class ot extends nt {
  constructor(e = de.DEFAULT_INIT) {
    super(), (this._fetchConfig = void 0), (this._fetchConfig = e);
  }
  async readURI(e, t) {
    const s = await fetch(e, this._fetchConfig);
    switch (t) {
      case "view":
        return new Uint8Array(await s.arrayBuffer());
      case "text":
        return s.text();
    }
  }
  resolve(e, t) {
    return de.resolve(e, t);
  }
  dirname(e) {
    return de.dirname(e);
  }
}
class at {
  constructor() {
    (this.vkFormat = 0),
      (this.typeSize = 1),
      (this.pixelWidth = 0),
      (this.pixelHeight = 0),
      (this.pixelDepth = 0),
      (this.layerCount = 0),
      (this.faceCount = 1),
      (this.supercompressionScheme = 0),
      (this.levels = []),
      (this.dataFormatDescriptor = [
        {
          vendorId: 0,
          descriptorType: 0,
          descriptorBlockSize: 0,
          versionNumber: 2,
          colorModel: 0,
          colorPrimaries: 1,
          transferFunction: 2,
          flags: 0,
          texelBlockDimension: [0, 0, 0, 0],
          bytesPlane: [0, 0, 0, 0, 0, 0, 0, 0],
          samples: [],
        },
      ]),
      (this.keyValue = {}),
      (this.globalData = null);
  }
}
class ct {
  constructor(e, t, s, r) {
    (this._dataView = void 0),
      (this._littleEndian = void 0),
      (this._offset = void 0),
      (this._dataView = new DataView(e.buffer, e.byteOffset + t, s)),
      (this._littleEndian = r),
      (this._offset = 0);
  }
  _nextUint8() {
    const e = this._dataView.getUint8(this._offset);
    return (this._offset += 1), e;
  }
  _nextUint16() {
    const e = this._dataView.getUint16(this._offset, this._littleEndian);
    return (this._offset += 2), e;
  }
  _nextUint32() {
    const e = this._dataView.getUint32(this._offset, this._littleEndian);
    return (this._offset += 4), e;
  }
  _nextUint64() {
    const e =
      this._dataView.getUint32(this._offset, this._littleEndian) +
      2 ** 32 * this._dataView.getUint32(this._offset + 4, this._littleEndian);
    return (this._offset += 8), e;
  }
  _nextInt32() {
    const e = this._dataView.getInt32(this._offset, this._littleEndian);
    return (this._offset += 4), e;
  }
  _nextUint8Array(e) {
    const t = new Uint8Array(
      this._dataView.buffer,
      this._dataView.byteOffset + this._offset,
      e
    );
    return (this._offset += e), t;
  }
  _skip(e) {
    return (this._offset += e), this;
  }
  _scan(e, t = 0) {
    const s = this._offset;
    let r = 0;
    for (; this._dataView.getUint8(this._offset) !== t && r < e; )
      r++, this._offset++;
    return (
      r < e && this._offset++,
      new Uint8Array(this._dataView.buffer, this._dataView.byteOffset + s, r)
    );
  }
}
const ut = [171, 75, 84, 88, 32, 50, 48, 187, 13, 10, 26, 10];
function ht(e) {
  return "undefined" != typeof TextDecoder
    ? new TextDecoder().decode(e)
    : Buffer.from(e).toString("utf8");
}
function ft(e) {
  const t = new Uint8Array(e.buffer, e.byteOffset, ut.length);
  if (
    t[0] !== ut[0] ||
    t[1] !== ut[1] ||
    t[2] !== ut[2] ||
    t[3] !== ut[3] ||
    t[4] !== ut[4] ||
    t[5] !== ut[5] ||
    t[6] !== ut[6] ||
    t[7] !== ut[7] ||
    t[8] !== ut[8] ||
    t[9] !== ut[9] ||
    t[10] !== ut[10] ||
    t[11] !== ut[11]
  )
    throw new Error("Missing KTX 2.0 identifier.");
  const s = new at(),
    r = 17 * Uint32Array.BYTES_PER_ELEMENT,
    n = new ct(e, ut.length, r, !0);
  (s.vkFormat = n._nextUint32()),
    (s.typeSize = n._nextUint32()),
    (s.pixelWidth = n._nextUint32()),
    (s.pixelHeight = n._nextUint32()),
    (s.pixelDepth = n._nextUint32()),
    (s.layerCount = n._nextUint32()),
    (s.faceCount = n._nextUint32());
  const i = n._nextUint32();
  s.supercompressionScheme = n._nextUint32();
  const o = n._nextUint32(),
    a = n._nextUint32(),
    c = n._nextUint32(),
    u = n._nextUint32(),
    h = n._nextUint64(),
    f = n._nextUint64(),
    l = new ct(e, ut.length + r, 3 * i * 8, !0);
  for (let t = 0; t < i; t++)
    s.levels.push({
      levelData: new Uint8Array(
        e.buffer,
        e.byteOffset + l._nextUint64(),
        l._nextUint64()
      ),
      uncompressedByteLength: l._nextUint64(),
    });
  const p = new ct(e, o, a, !0),
    d = {
      vendorId: p._skip(4)._nextUint16(),
      descriptorType: p._nextUint16(),
      versionNumber: p._nextUint16(),
      descriptorBlockSize: p._nextUint16(),
      colorModel: p._nextUint8(),
      colorPrimaries: p._nextUint8(),
      transferFunction: p._nextUint8(),
      flags: p._nextUint8(),
      texelBlockDimension: [
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
      ],
      bytesPlane: [
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
      ],
      samples: [],
    },
    g = (d.descriptorBlockSize / 4 - 6) / 4;
  for (let e = 0; e < g; e++) {
    const t = {
      bitOffset: p._nextUint16(),
      bitLength: p._nextUint8(),
      channelType: p._nextUint8(),
      samplePosition: [
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
        p._nextUint8(),
      ],
      sampleLower: -1 / 0,
      sampleUpper: 1 / 0,
    };
    64 & t.channelType
      ? ((t.sampleLower = p._nextInt32()), (t.sampleUpper = p._nextInt32()))
      : ((t.sampleLower = p._nextUint32()), (t.sampleUpper = p._nextUint32())),
      (d.samples[e] = t);
  }
  (s.dataFormatDescriptor.length = 0), s.dataFormatDescriptor.push(d);
  const m = new ct(e, c, u, !0);
  for (; m._offset < u; ) {
    const e = m._nextUint32(),
      t = m._scan(e),
      r = ht(t);
    if (
      ((s.keyValue[r] = m._nextUint8Array(e - t.byteLength - 1)),
      r.match(/^ktx/i))
    ) {
      const e = ht(s.keyValue[r]);
      s.keyValue[r] = e.substring(0, e.lastIndexOf("\0"));
    }
    const n = e % 4 ? 4 - (e % 4) : 0;
    m._skip(n);
  }
  if (f <= 0) return s;
  const x = new ct(e, h, f, !0),
    T = x._nextUint16(),
    E = x._nextUint16(),
    y = x._nextUint32(),
    I = x._nextUint32(),
    R = x._nextUint32(),
    N = x._nextUint32(),
    A = [];
  for (let e = 0; e < i; e++)
    A.push({
      imageFlags: x._nextUint32(),
      rgbSliceByteOffset: x._nextUint32(),
      rgbSliceByteLength: x._nextUint32(),
      alphaSliceByteOffset: x._nextUint32(),
      alphaSliceByteLength: x._nextUint32(),
    });
  const w = h + x._offset,
    b = w + y,
    M = b + I,
    S = M + R,
    _ = new Uint8Array(e.buffer, e.byteOffset + w, y),
    C = new Uint8Array(e.buffer, e.byteOffset + b, I),
    v = new Uint8Array(e.buffer, e.byteOffset + M, R),
    O = new Uint8Array(e.buffer, e.byteOffset + S, N);
  return (
    (s.globalData = {
      endpointCount: T,
      selectorCount: E,
      imageDescs: A,
      endpointsData: _,
      selectorsData: C,
      tablesData: v,
      extendedData: O,
    }),
    s
  );
}
const lt = "EXT_mesh_gpu_instancing",
  pt = "EXT_meshopt_compression",
  dt = "KHR_draco_mesh_compression",
  gt = "KHR_lights_punctual",
  mt = "KHR_materials_anisotropy",
  xt = "KHR_materials_clearcoat",
  Tt = "KHR_materials_diffuse_transmission",
  Et = "KHR_materials_dispersion",
  yt = "KHR_materials_emissive_strength",
  It = "KHR_materials_ior",
  Rt = "KHR_materials_iridescence",
  Nt = "KHR_materials_pbrSpecularGlossiness",
  At = "KHR_materials_sheen",
  wt = "KHR_materials_specular",
  bt = "KHR_materials_transmission",
  Mt = "KHR_materials_unlit",
  St = "KHR_materials_volume",
  _t = "KHR_materials_variants",
  Ct = "KHR_texture_transform",
  vt = "KHR_xmp_json_ld",
  Ot = "INSTANCE_ATTRIBUTE";
class Dt extends we {
  init() {
    (this.extensionName = lt),
      (this.propertyType = "InstancedMesh"),
      (this.parentTypes = [O.NODE]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { attributes: new b() });
  }
  getAttribute(e) {
    return this.getRefMap("attributes", e);
  }
  setAttribute(e, t) {
    return this.setRefMap("attributes", e, t, { usage: Ot });
  }
  listAttributes() {
    return this.listRefMapValues("attributes");
  }
  listSemantics() {
    return this.listRefMapKeys("attributes");
  }
}
Dt.EXTENSION_NAME = lt;
const Ft = lt;
class Ut extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Ft),
      (this.provideTypes = [O.NODE]),
      (this.prewriteTypes = [O.ACCESSOR]);
  }
  createInstancedMesh() {
    return new Dt(this.document.getGraph());
  }
  read(e) {
    return (
      (e.jsonDoc.json.nodes || []).forEach((t, s) => {
        if (!t.extensions || !t.extensions[Ft]) return;
        const r = t.extensions[Ft],
          n = this.createInstancedMesh();
        for (const t in r.attributes)
          n.setAttribute(t, e.accessors[r.attributes[t]]);
        e.nodes[s].setExtension(Ft, n);
      }),
      this
    );
  }
  prewrite(e) {
    e.accessorUsageGroupedByParent.add(Ot);
    for (const t of this.properties)
      for (const s of t.listAttributes()) e.addAccessorToUsageGroup(s, Ot);
    return this;
  }
  write(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listNodes()
        .forEach((s) => {
          const r = s.getExtension(Ft);
          if (r) {
            const n = e.nodeIndexMap.get(s),
              i = t.json.nodes[n],
              o = { attributes: {} };
            r.listSemantics().forEach((t) => {
              const s = r.getAttribute(t);
              o.attributes[t] = e.accessorIndexMap.get(s);
            }),
              (i.extensions = i.extensions || {}),
              (i.extensions[Ft] = o);
          }
        }),
      this
    );
  }
}
function Lt() {
  return (
    (Lt = Object.assign
      ? Object.assign.bind()
      : function (e) {
          for (var t = 1; t < arguments.length; t++) {
            var s = arguments[t];
            for (var r in s)
              Object.prototype.hasOwnProperty.call(s, r) && (e[r] = s[r]);
          }
          return e;
        }),
    Lt.apply(this, arguments)
  );
}
var Bt, Pt, jt;
(Ut.EXTENSION_NAME = Ft),
  (function (e) {
    (e.QUANTIZE = "quantize"), (e.FILTER = "filter");
  })(Bt || (Bt = {})),
  (function (e) {
    (e.ATTRIBUTES = "ATTRIBUTES"),
      (e.TRIANGLES = "TRIANGLES"),
      (e.INDICES = "INDICES");
  })(Pt || (Pt = {})),
  (function (e) {
    (e.NONE = "NONE"),
      (e.OCTAHEDRAL = "OCTAHEDRAL"),
      (e.QUATERNION = "QUATERNION"),
      (e.EXPONENTIAL = "EXPONENTIAL");
  })(jt || (jt = {}));
const { BYTE: kt, SHORT: Vt, FLOAT: Gt } = Ee.ComponentType,
  { encodeNormalizedInt: Ht, decodeNormalizedInt: zt } = re;
function Xt(e, t, s, r) {
  const { filter: n, bits: i } = r,
    o = {
      array: e.getArray(),
      byteStride: e.getElementSize() * e.getComponentSize(),
      componentType: e.getComponentType(),
      normalized: e.getNormalized(),
    };
  if (s !== Pt.ATTRIBUTES) return o;
  if (n !== jt.NONE) {
    let s = e.getNormalized()
      ? (function (e) {
          const t = e.getComponentType(),
            s = e.getArray(),
            r = new Float32Array(s.length);
          for (let e = 0; e < s.length; e++) r[e] = zt(s[e], t);
          return r;
        })(e)
      : new Float32Array(o.array);
    switch (n) {
      case jt.EXPONENTIAL:
        (o.byteStride = 4 * e.getElementSize()),
          (o.componentType = Gt),
          (o.normalized = !1),
          (o.array = t.encodeFilterExp(s, e.getCount(), o.byteStride, i));
        break;
      case jt.OCTAHEDRAL:
        (o.byteStride = i > 8 ? 8 : 4),
          (o.componentType = i > 8 ? Vt : kt),
          (o.normalized = !0),
          (s =
            3 === e.getElementSize()
              ? (function (e) {
                  const t = new Float32Array((4 * e.length) / 3);
                  for (let s = 0, r = e.length / 3; s < r; s++)
                    (t[4 * s] = e[3 * s]),
                      (t[4 * s + 1] = e[3 * s + 1]),
                      (t[4 * s + 2] = e[3 * s + 2]);
                  return t;
                })(s)
              : s),
          (o.array = t.encodeFilterOct(s, e.getCount(), o.byteStride, i));
        break;
      case jt.QUATERNION:
        (o.byteStride = 8),
          (o.componentType = Vt),
          (o.normalized = !0),
          (o.array = t.encodeFilterQuat(s, e.getCount(), o.byteStride, i));
        break;
      default:
        throw new Error("Invalid filter.");
    }
    (o.min = e.getMin([])),
      (o.max = e.getMax([])),
      e.getNormalized() &&
        ((o.min = o.min.map((t) => zt(t, e.getComponentType()))),
        (o.max = o.max.map((t) => zt(t, e.getComponentType())))),
      o.normalized &&
        ((o.min = o.min.map((e) => Ht(e, o.componentType))),
        (o.max = o.max.map((e) => Ht(e, o.componentType))));
  } else
    o.byteStride % 4 &&
      ((o.array = (function (e, t) {
        const s = Y.padNumber(e.BYTES_PER_ELEMENT * t) / e.BYTES_PER_ELEMENT,
          r = e.length / t,
          n = new e.constructor(r * s);
        for (let r = 0; r * t < e.length; r++)
          for (let i = 0; i < t; i++) n[r * s + i] = e[r * t + i];
        return n;
      })(o.array, e.getElementSize())),
      (o.byteStride = o.array.byteLength / e.getCount()));
  return o;
}
function qt(e, t) {
  if (t === Ke.BufferViewUsage.ELEMENT_ARRAY_BUFFER) {
    return e
      .listParents()
      .some((e) => e instanceof Fe && e.getMode() === Fe.Mode.TRIANGLES)
      ? Pt.TRIANGLES
      : Pt.INDICES;
  }
  return Pt.ATTRIBUTES;
}
function Yt(e, t) {
  const s = t
    .getGraph()
    .listParentEdges(e)
    .filter((e) => !(e.getParent() instanceof ke));
  for (const t of s) {
    const s = t.getName(),
      r = t.getAttributes().key || "",
      n = t.getParent().propertyType === O.PRIMITIVE_TARGET;
    if ("indices" === s) return { filter: jt.NONE };
    if ("attributes" === s) {
      if ("POSITION" === r) return { filter: jt.NONE };
      if ("TEXCOORD_0" === r) return { filter: jt.NONE };
      if (r.startsWith("JOINTS_")) return { filter: jt.NONE };
      if (r.startsWith("WEIGHTS_")) return { filter: jt.NONE };
      if ("NORMAL" === r || "TANGENT" === r)
        return n ? { filter: jt.NONE } : { filter: jt.OCTAHEDRAL, bits: 8 };
    }
    if ("output" === s) {
      const t = Wt(e);
      return "rotation" === t
        ? { filter: jt.QUATERNION, bits: 16 }
        : "translation" === t || "scale" === t
        ? { filter: jt.EXPONENTIAL, bits: 12 }
        : { filter: jt.NONE };
    }
    if ("input" === s) return { filter: jt.NONE };
    if ("inverseBindMatrices" === s) return { filter: jt.NONE };
  }
  return { filter: jt.NONE };
}
function Wt(e) {
  for (const t of e.listParents())
    if (t instanceof Re)
      for (const e of t.listParents())
        if (e instanceof Ie) return e.getTargetPath();
  return null;
}
const Kt = pt,
  $t = { method: Bt.QUANTIZE };
class Jt extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Kt),
      (this.prereadTypes = [O.BUFFER, O.PRIMITIVE]),
      (this.prewriteTypes = [O.BUFFER, O.ACCESSOR]),
      (this.readDependencies = ["meshopt.decoder"]),
      (this.writeDependencies = ["meshopt.encoder"]),
      (this._decoder = null),
      (this._decoderFallbackBufferMap = new Map()),
      (this._encoder = null),
      (this._encoderOptions = $t),
      (this._encoderFallbackBuffer = null),
      (this._encoderBufferViews = {}),
      (this._encoderBufferViewData = {}),
      (this._encoderBufferViewAccessors = {});
  }
  install(e, t) {
    return (
      "meshopt.decoder" === e && (this._decoder = t),
      "meshopt.encoder" === e && (this._encoder = t),
      this
    );
  }
  setEncoderOptions(e) {
    return (this._encoderOptions = Lt({}, $t, e)), this;
  }
  preread(e, t) {
    if (!this._decoder) {
      if (!this.isRequired()) return this;
      throw new Error(
        `[${Kt}] Please install extension dependency, "meshopt.decoder".`
      );
    }
    if (!this._decoder.supported) {
      if (!this.isRequired()) return this;
      throw new Error(`[${Kt}]: Missing WASM support.`);
    }
    return (
      t === O.BUFFER
        ? this._prereadBuffers(e)
        : t === O.PRIMITIVE && this._prereadPrimitives(e),
      this
    );
  }
  _prereadBuffers(e) {
    const t = e.jsonDoc;
    (t.json.bufferViews || []).forEach((s, r) => {
      if (!s.extensions || !s.extensions[Kt]) return;
      const n = s.extensions[Kt],
        i = n.byteOffset || 0,
        o = n.byteLength || 0,
        a = n.count,
        c = n.byteStride,
        u = new Uint8Array(a * c),
        h = t.json.buffers[n.buffer],
        f = h.uri ? t.resources[h.uri] : t.resources[v],
        l = Y.toView(f, i, o);
      this._decoder.decodeGltfBuffer(u, a, c, l, n.mode, n.filter),
        (e.bufferViews[r] = u);
    });
  }
  _prereadPrimitives(e) {
    const t = e.jsonDoc;
    (t.json.bufferViews || []).forEach((s) => {
      if (!s.extensions || !s.extensions[Kt]) return;
      const r = s.extensions[Kt],
        n = e.buffers[r.buffer],
        i = e.buffers[s.buffer],
        o = t.json.buffers[s.buffer];
      var a;
      (a = o).extensions &&
        a.extensions[pt] &&
        a.extensions[pt].fallback &&
        this._decoderFallbackBufferMap.set(i, n);
    });
  }
  read(e) {
    if (!this.isRequired()) return this;
    for (const [e, t] of this._decoderFallbackBufferMap) {
      for (const s of e.listParents()) s instanceof Ee && s.swap(e, t);
      e.dispose();
    }
    return this;
  }
  prewrite(e, t) {
    return (
      t === O.ACCESSOR
        ? this._prewriteAccessors(e)
        : t === O.BUFFER && this._prewriteBuffers(e),
      this
    );
  }
  _prewriteAccessors(e) {
    const t = e.jsonDoc.json,
      s = this._encoder,
      r = this._encoderOptions,
      n = this.document.getGraph(),
      i = this.document.createBuffer(),
      o = this.document.getRoot().listBuffers().indexOf(i);
    let a = 1;
    const c = new Map(),
      u = (e) => {
        for (const t of n.listParents(e)) {
          if (t.propertyType === O.ROOT) continue;
          let s = c.get(e);
          return void 0 === s && c.set(e, (s = a++)), s;
        }
        return -1;
      };
    (this._encoderFallbackBuffer = i),
      (this._encoderBufferViews = {}),
      (this._encoderBufferViewData = {}),
      (this._encoderBufferViewAccessors = {});
    for (const n of this.document.getRoot().listAccessors()) {
      if ("weights" === Wt(n)) continue;
      if (n.getSparse()) continue;
      const i = e.getAccessorUsage(n),
        a = e.accessorUsageGroupedByParent.has(i) ? u(n) : null,
        c = qt(n, i),
        h = r.method === Bt.FILTER ? Yt(n, this.document) : { filter: jt.NONE },
        f = Xt(n, s, c, h),
        { array: l, byteStride: p } = f,
        d = n.getBuffer();
      if (!d) throw new Error(`${Kt}: Missing buffer for accessor.`);
      const g = this.document.getRoot().listBuffers().indexOf(d),
        m = [i, a, c, h.filter, p, g].join(":");
      let x = this._encoderBufferViews[m],
        T = this._encoderBufferViewData[m],
        E = this._encoderBufferViewAccessors[m];
      (x && T) ||
        ((E = this._encoderBufferViewAccessors[m] = []),
        (T = this._encoderBufferViewData[m] = []),
        (x = this._encoderBufferViews[m] =
          {
            buffer: o,
            target: Ke.USAGE_TO_TARGET[i],
            byteOffset: 0,
            byteLength: 0,
            byteStride: i === Ke.BufferViewUsage.ARRAY_BUFFER ? p : void 0,
            extensions: {
              [Kt]: {
                buffer: g,
                byteOffset: 0,
                byteLength: 0,
                mode: c,
                filter: h.filter !== jt.NONE ? h.filter : void 0,
                byteStride: p,
                count: 0,
              },
            },
          }));
      const y = e.createAccessorDef(n);
      (y.componentType = f.componentType),
        (y.normalized = f.normalized),
        (y.byteOffset = x.byteLength),
        y.min && f.min && (y.min = f.min),
        y.max && f.max && (y.max = f.max),
        e.accessorIndexMap.set(n, t.accessors.length),
        t.accessors.push(y),
        E.push(y),
        T.push(new Uint8Array(l.buffer, l.byteOffset, l.byteLength)),
        (x.byteLength += l.byteLength),
        (x.extensions.EXT_meshopt_compression.count += n.getCount());
    }
  }
  _prewriteBuffers(e) {
    const t = this._encoder;
    for (const s in this._encoderBufferViews) {
      const r = this._encoderBufferViews[s],
        n = this._encoderBufferViewData[s],
        i = this.document.getRoot().listBuffers()[r.extensions[Kt].buffer],
        o = e.otherBufferViews.get(i) || [],
        { count: a, byteStride: c, mode: u } = r.extensions[Kt],
        h = Y.concat(n),
        f = t.encodeGltfBuffer(h, a, c, u),
        l = Y.pad(f);
      (r.extensions[Kt].byteLength = f.byteLength),
        (n.length = 0),
        n.push(l),
        o.push(l),
        e.otherBufferViews.set(i, o);
    }
  }
  write(e) {
    let t = 0;
    for (const s in this._encoderBufferViews) {
      const r = this._encoderBufferViews[s],
        n = this._encoderBufferViewData[s][0],
        i = e.otherBufferViewsIndexMap.get(n),
        o = this._encoderBufferViewAccessors[s];
      for (const e of o) e.bufferView = i;
      const a = e.jsonDoc.json.bufferViews[i],
        c = a.byteOffset || 0;
      Object.assign(a, r), (a.byteOffset = t);
      (a.extensions[Kt].byteOffset = c), (t += Y.padNumber(r.byteLength));
    }
    const s = this._encoderFallbackBuffer,
      r = e.bufferIndexMap.get(s),
      n = e.jsonDoc.json.buffers[r];
    return (
      (n.byteLength = t),
      (n.extensions = { [Kt]: { fallback: !0 } }),
      s.dispose(),
      this
    );
  }
}
(Jt.EXTENSION_NAME = Kt), (Jt.EncoderMethod = Bt);
const Qt = "EXT_texture_avif";
class Zt {
  match(e) {
    return e.length >= 12 && "ftypavif" === Y.decodeText(e.slice(4, 12));
  }
  getSize(e) {
    if (!this.match(e)) return null;
    const t = new DataView(e.buffer, e.byteOffset, e.byteLength);
    let s = es(t, 0);
    if (!s) return null;
    let r = s.end;
    for (; (s = es(t, r)); )
      if ("meta" === s.type) r = s.start + 4;
      else if ("iprp" === s.type || "ipco" === s.type) r = s.start;
      else {
        if ("ispe" === s.type)
          return [t.getUint32(s.start + 4), t.getUint32(s.start + 8)];
        if ("mdat" === s.type) break;
        r = s.end;
      }
    return null;
  }
  getChannels(e) {
    return 4;
  }
}
function es(e, t) {
  if (e.byteLength < 4 + t) return null;
  const s = e.getUint32(t);
  return e.byteLength < s + t || s < 8
    ? null
    : {
        type: Y.decodeText(new Uint8Array(e.buffer, e.byteOffset + t + 4, 4)),
        start: t + 8,
        end: t + s,
      };
}
(class extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Qt), (this.prereadTypes = [O.TEXTURE]);
  }
  static register() {
    K.registerFormat("image/avif", new Zt());
  }
  preread(e) {
    return (
      (e.jsonDoc.json.textures || []).forEach((e) => {
        e.extensions &&
          e.extensions[Qt] &&
          (e.source = e.extensions[Qt].source);
      }),
      this
    );
  }
  read(e) {
    return this;
  }
  write(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listTextures()
        .forEach((s) => {
          if ("image/avif" === s.getMimeType()) {
            const r = e.imageIndexMap.get(s);
            (t.json.textures || []).forEach((e) => {
              e.source === r &&
                ((e.extensions = e.extensions || {}),
                (e.extensions[Qt] = { source: e.source }),
                delete e.source);
            });
          }
        }),
      this
    );
  }
}).EXTENSION_NAME = Qt;
const ts = "EXT_texture_webp";
class ss {
  match(e) {
    return (
      e.length >= 12 &&
      87 === e[8] &&
      69 === e[9] &&
      66 === e[10] &&
      80 === e[11]
    );
  }
  getSize(e) {
    const t = Y.decodeText(e.slice(0, 4)),
      s = Y.decodeText(e.slice(8, 12));
    if ("RIFF" !== t || "WEBP" !== s) return null;
    const r = new DataView(e.buffer, e.byteOffset);
    let n = 12;
    for (; n < r.byteLength; ) {
      const e = Y.decodeText(
          new Uint8Array([
            r.getUint8(n),
            r.getUint8(n + 1),
            r.getUint8(n + 2),
            r.getUint8(n + 3),
          ])
        ),
        t = r.getUint32(n + 4, !0);
      if ("VP8 " === e) {
        return [16383 & r.getInt16(n + 14, !0), 16383 & r.getInt16(n + 16, !0)];
      }
      if ("VP8L" === e) {
        const e = r.getUint8(n + 9),
          t = r.getUint8(n + 10),
          s = r.getUint8(n + 11);
        return [
          1 + (((63 & t) << 8) | e),
          1 + (((15 & r.getUint8(n + 12)) << 10) | (s << 2) | ((192 & t) >> 6)),
        ];
      }
      n += 8 + t + (t % 2);
    }
    return null;
  }
  getChannels(e) {
    return 4;
  }
}
(class extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = ts), (this.prereadTypes = [O.TEXTURE]);
  }
  static register() {
    K.registerFormat("image/webp", new ss());
  }
  preread(e) {
    return (
      (e.jsonDoc.json.textures || []).forEach((e) => {
        e.extensions &&
          e.extensions[ts] &&
          (e.source = e.extensions[ts].source);
      }),
      this
    );
  }
  read(e) {
    return this;
  }
  write(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listTextures()
        .forEach((s) => {
          if ("image/webp" === s.getMimeType()) {
            const r = e.imageIndexMap.get(s);
            (t.json.textures || []).forEach((e) => {
              e.source === r &&
                ((e.extensions = e.extensions || {}),
                (e.extensions[ts] = { source: e.source }),
                delete e.source);
            });
          }
        }),
      this
    );
  }
}).EXTENSION_NAME = ts;
const rs = dt;
let ns, is, os, as;
function cs(e, t) {
  const s = new ns.DecoderBuffer();
  try {
    s.Init(t, t.length);
    if (e.GetEncodedGeometryType(s) !== ns.TRIANGULAR_MESH)
      throw new Error(`[${rs}] Unknown geometry type.`);
    const r = new ns.Mesh();
    if (!e.DecodeBufferToMesh(s, r).ok() || 0 === r.ptr)
      throw new Error(`[${rs}] Decoding failure.`);
    return r;
  } finally {
    ns.destroy(s);
  }
}
function us(e, t) {
  const s = 3 * t.num_faces();
  let r, n;
  if (t.num_points() <= 65534) {
    const i = s * Uint16Array.BYTES_PER_ELEMENT;
    (r = ns._malloc(i)),
      e.GetTrianglesUInt16Array(t, i, r),
      (n = new Uint16Array(ns.HEAPU16.buffer, r, s).slice());
  } else {
    const i = s * Uint32Array.BYTES_PER_ELEMENT;
    (r = ns._malloc(i)),
      e.GetTrianglesUInt32Array(t, i, r),
      (n = new Uint32Array(ns.HEAPU32.buffer, r, s).slice());
  }
  return ns._free(r), n;
}
function hs(e, t, s, r) {
  const n = os[r.componentType],
    i = is[r.componentType],
    o = s.num_components(),
    a = t.num_points() * o,
    c = a * i.BYTES_PER_ELEMENT,
    u = ns._malloc(c);
  e.GetAttributeDataArrayForAllPoints(t, s, n, c, u);
  const h = new i(ns.HEAPF32.buffer, u, a).slice();
  return ns._free(u), h;
}
var fs, ls;
!(function (e) {
  (e[(e.EDGEBREAKER = 1)] = "EDGEBREAKER"),
    (e[(e.SEQUENTIAL = 0)] = "SEQUENTIAL");
})(fs || (fs = {})),
  (function (e) {
    (e.POSITION = "POSITION"),
      (e.NORMAL = "NORMAL"),
      (e.COLOR = "COLOR"),
      (e.TEX_COORD = "TEX_COORD"),
      (e.GENERIC = "GENERIC");
  })(ls || (ls = {}));
const ps = {
    [ls.POSITION]: 14,
    [ls.NORMAL]: 10,
    [ls.COLOR]: 8,
    [ls.TEX_COORD]: 12,
    [ls.GENERIC]: 12,
  },
  ds = {
    decodeSpeed: 5,
    encodeSpeed: 5,
    method: fs.EDGEBREAKER,
    quantizationBits: ps,
    quantizationVolume: "mesh",
  };
function gs(e, t = ds) {
  const s = Lt({}, ds, t);
  s.quantizationBits = Lt({}, ps, t.quantizationBits);
  const r = new as.MeshBuilder(),
    n = new as.Mesh(),
    i = new as.ExpertEncoder(n),
    o = {},
    a = new as.DracoInt8Array(),
    c = e.listTargets().length > 0;
  let u = !1;
  for (const t of e.listSemantics()) {
    const a = e.getAttribute(t);
    if (a.getSparse()) {
      u = !0;
      continue;
    }
    const c = ms(t),
      h = xs(
        r,
        a.getComponentType(),
        n,
        as[c],
        a.getCount(),
        a.getElementSize(),
        a.getArray()
      );
    if (-1 === h) throw new Error(`Error compressing "${t}" attribute.`);
    if (((o[t] = h), "mesh" === s.quantizationVolume || "POSITION" !== t))
      i.SetAttributeQuantization(h, s.quantizationBits[c]);
    else {
      if ("object" != typeof s.quantizationVolume)
        throw new Error("Invalid quantization volume state.");
      {
        const { quantizationVolume: e } = s,
          t = Math.max(
            e.max[0] - e.min[0],
            e.max[1] - e.min[1],
            e.max[2] - e.min[2]
          );
        i.SetAttributeExplicitQuantization(
          h,
          s.quantizationBits[c],
          a.getElementSize(),
          e.min,
          t
        );
      }
    }
  }
  const h = e.getIndices();
  if (!h) throw new Ts("Primitive must have indices.");
  r.AddFacesToMesh(n, h.getCount() / 3, h.getArray()),
    i.SetSpeedOptions(s.encodeSpeed, s.decodeSpeed),
    i.SetTrackEncodedProperties(!0),
    s.method === fs.SEQUENTIAL || c || u
      ? i.SetEncodingMethod(as.MESH_SEQUENTIAL_ENCODING)
      : i.SetEncodingMethod(as.MESH_EDGEBREAKER_ENCODING);
  const f = i.EncodeToDracoBuffer(!(c || u), a);
  if (f <= 0) throw new Ts("Error applying Draco compression.");
  const l = new Uint8Array(f);
  for (let e = 0; e < f; ++e) l[e] = a.GetValue(e);
  const p = i.GetNumberOfEncodedPoints(),
    d = 3 * i.GetNumberOfEncodedFaces();
  return (
    as.destroy(a),
    as.destroy(n),
    as.destroy(r),
    as.destroy(i),
    { numVertices: p, numIndices: d, data: l, attributeIDs: o }
  );
}
function ms(e) {
  return "POSITION" === e
    ? ls.POSITION
    : "NORMAL" === e
    ? ls.NORMAL
    : e.startsWith("COLOR_")
    ? ls.COLOR
    : e.startsWith("TEXCOORD_")
    ? ls.TEX_COORD
    : ls.GENERIC;
}
function xs(e, t, s, r, n, i, o) {
  switch (t) {
    case Ee.ComponentType.UNSIGNED_BYTE:
      return e.AddUInt8Attribute(s, r, n, i, o);
    case Ee.ComponentType.BYTE:
      return e.AddInt8Attribute(s, r, n, i, o);
    case Ee.ComponentType.UNSIGNED_SHORT:
      return e.AddUInt16Attribute(s, r, n, i, o);
    case Ee.ComponentType.SHORT:
      return e.AddInt16Attribute(s, r, n, i, o);
    case Ee.ComponentType.UNSIGNED_INT:
      return e.AddUInt32Attribute(s, r, n, i, o);
    case Ee.ComponentType.FLOAT:
      return e.AddFloatAttribute(s, r, n, i, o);
    default:
      throw new Error(`Unexpected component type, "${t}".`);
  }
}
class Ts extends Error {}
const Es = dt;
class ys extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Es),
      (this.prereadTypes = [O.PRIMITIVE]),
      (this.prewriteTypes = [O.ACCESSOR]),
      (this.readDependencies = ["draco3d.decoder"]),
      (this.writeDependencies = ["draco3d.encoder"]),
      (this._decoderModule = null),
      (this._encoderModule = null),
      (this._encoderOptions = {});
  }
  install(e, t) {
    var s, r;
    return (
      "draco3d.decoder" === e &&
        ((this._decoderModule = t),
        (s = this._decoderModule),
        (ns = s),
        (is = {
          [Ee.ComponentType.FLOAT]: Float32Array,
          [Ee.ComponentType.UNSIGNED_INT]: Uint32Array,
          [Ee.ComponentType.UNSIGNED_SHORT]: Uint16Array,
          [Ee.ComponentType.UNSIGNED_BYTE]: Uint8Array,
          [Ee.ComponentType.SHORT]: Int16Array,
          [Ee.ComponentType.BYTE]: Int8Array,
        }),
        (os = {
          [Ee.ComponentType.FLOAT]: ns.DT_FLOAT32,
          [Ee.ComponentType.UNSIGNED_INT]: ns.DT_UINT32,
          [Ee.ComponentType.UNSIGNED_SHORT]: ns.DT_UINT16,
          [Ee.ComponentType.UNSIGNED_BYTE]: ns.DT_UINT8,
          [Ee.ComponentType.SHORT]: ns.DT_INT16,
          [Ee.ComponentType.BYTE]: ns.DT_INT8,
        })),
      "draco3d.encoder" === e &&
        ((this._encoderModule = t), (r = this._encoderModule), (as = r)),
      this
    );
  }
  setEncoderOptions(e) {
    return (this._encoderOptions = e), this;
  }
  preread(e) {
    if (!this._decoderModule)
      throw new Error(
        `[${Es}] Please install extension dependency, "draco3d.decoder".`
      );
    const t = this.document.getLogger(),
      s = e.jsonDoc,
      r = new Map();
    try {
      const n = s.json.meshes || [];
      for (const i of n)
        for (const n of i.primitives) {
          if (!n.extensions || !n.extensions[Es]) continue;
          const i = n.extensions[Es];
          let [o, a] = r.get(i.bufferView) || [];
          if (!a || !o) {
            const e = s.json.bufferViews[i.bufferView],
              n = s.json.buffers[e.buffer],
              c = n.uri ? s.resources[n.uri] : s.resources[v],
              u = e.byteOffset || 0,
              h = e.byteLength,
              f = Y.toView(c, u, h);
            (o = new this._decoderModule.Decoder()),
              (a = cs(o, f)),
              r.set(i.bufferView, [o, a]),
              t.debug(`[${Es}] Decompressed ${f.byteLength} bytes.`);
          }
          for (const t in n.attributes) {
            const s = e.jsonDoc.json.accessors[n.attributes[t]],
              r = o.GetAttributeByUniqueId(a, i.attributes[t]),
              c = hs(o, a, r, s);
            e.accessors[n.attributes[t]].setArray(c);
          }
          void 0 !== n.indices && e.accessors[n.indices].setArray(us(o, a));
        }
    } finally {
      for (const [e, t] of Array.from(r.values()))
        this._decoderModule.destroy(e), this._decoderModule.destroy(t);
    }
    return this;
  }
  read(e) {
    return this;
  }
  prewrite(e, t) {
    if (!this._encoderModule)
      throw new Error(
        `[${Es}] Please install extension dependency, "draco3d.encoder".`
      );
    const s = this.document.getLogger();
    s.debug(
      `[${Es}] Compression options: ${JSON.stringify(this._encoderOptions)}`
    );
    const r = (function (e) {
        const t = e.getLogger(),
          s = new Set(),
          r = new Set();
        let n = 0,
          i = 0;
        for (const t of e.getRoot().listMeshes())
          for (const e of t.listPrimitives())
            e.getIndices()
              ? e.getMode() !== Fe.Mode.TRIANGLES
                ? (r.add(e), i++)
                : s.add(e)
              : (r.add(e), n++);
        n > 0 &&
          t.warn(
            `[${Es}] Skipping Draco compression of ${n} non-indexed primitives.`
          );
        i > 0 &&
          t.warn(
            `[${Es}] Skipping Draco compression of ${i} non-TRIANGLES primitives.`
          );
        const o = e.getRoot().listAccessors(),
          a = new Map();
        for (let e = 0; e < o.length; e++) a.set(o[e], e);
        const c = new Map(),
          u = new Set(),
          h = new Map();
        for (const t of Array.from(s)) {
          let s = Is(t, a);
          if (u.has(s)) h.set(t, s);
          else {
            if (c.has(t.getIndices())) {
              const s = t.getIndices(),
                r = s.clone();
              a.set(r, e.getRoot().listAccessors().length - 1), t.swap(s, r);
            }
            for (const s of t.listAttributes())
              if (c.has(s)) {
                const r = s.clone();
                a.set(r, e.getRoot().listAccessors().length - 1), t.swap(s, r);
              }
            (s = Is(t, a)), u.add(s), h.set(t, s), c.set(t.getIndices(), s);
            for (const e of t.listAttributes()) c.set(e, s);
          }
        }
        for (const e of Array.from(c.keys())) {
          const t = new Set(e.listParents().map((e) => e.propertyType));
          if (2 !== t.size || !t.has(O.PRIMITIVE) || !t.has(O.ROOT))
            throw new Error(
              `[${Es}] Compressed accessors must only be used as indices or vertex attributes.`
            );
        }
        for (const e of Array.from(s)) {
          const t = h.get(e),
            s = e.getIndices();
          if (c.get(s) !== t || e.listAttributes().some((e) => c.get(e) !== t))
            throw new Error(
              `[${Es}] Draco primitives must share all, or no, accessors.`
            );
        }
        for (const e of Array.from(r)) {
          const t = e.getIndices();
          if (c.has(t) || e.listAttributes().some((e) => c.has(e)))
            throw new Error(
              `[${Es}] Accessor cannot be shared by compressed and uncompressed primitives.`
            );
        }
        return h;
      })(this.document),
      n = new Map();
    let i = "mesh";
    "scene" === this._encoderOptions.quantizationVolume &&
      (1 !== this.document.getRoot().listScenes().length
        ? s.warn(`[${Es}]: quantizationVolume=scene requires exactly 1 scene.`)
        : (i = (function (e) {
            const t = {
                min: [1 / 0, 1 / 0, 1 / 0],
                max: [-1 / 0, -1 / 0, -1 / 0],
              },
              s = e.propertyType === O.NODE ? [e] : e.listChildren();
            for (const e of s)
              e.traverse((e) => {
                const s = e.getMesh();
                if (!s) return;
                const r = z(s, e.getWorldMatrix());
                r.min.every(isFinite) &&
                  r.max.every(isFinite) &&
                  (X(r.min, t), X(r.max, t));
              });
            return t;
          })(this.document.getRoot().listScenes().pop())));
    for (const t of Array.from(r.keys())) {
      const o = r.get(t);
      if (!o) throw new Error("Unexpected primitive.");
      if (n.has(o)) {
        n.set(o, n.get(o));
        continue;
      }
      const a = t.getIndices(),
        c = e.jsonDoc.json.accessors;
      let u;
      try {
        u = gs(t, Lt({}, this._encoderOptions, { quantizationVolume: i }));
      } catch (e) {
        if (e instanceof Ts) {
          s.warn(`[${Es}]: ${e.message} Skipping primitive compression.`);
          continue;
        }
        throw e;
      }
      n.set(o, u);
      const h = e.createAccessorDef(a);
      (h.count = u.numIndices),
        e.accessorIndexMap.set(a, c.length),
        c.push(h),
        u.numVertices > 65534 &&
          h.componentType !== Ee.ComponentType.UNSIGNED_INT &&
          (h.componentType = Ee.ComponentType.UNSIGNED_INT);
      for (const s of t.listSemantics()) {
        const r = t.getAttribute(s);
        if (void 0 === u.attributeIDs[s]) continue;
        const n = e.createAccessorDef(r);
        (n.count = u.numVertices),
          e.accessorIndexMap.set(r, c.length),
          c.push(n);
      }
      const f =
        t.getAttribute("POSITION").getBuffer() ||
        this.document.getRoot().listBuffers()[0];
      e.otherBufferViews.has(f) || e.otherBufferViews.set(f, []),
        e.otherBufferViews.get(f).push(u.data);
    }
    return (
      s.debug(`[${Es}] Compressed ${r.size} primitives.`),
      (e.extensionData[Es] = { primitiveHashMap: r, primitiveEncodingMap: n }),
      this
    );
  }
  write(e) {
    const t = e.extensionData[Es];
    for (const s of this.document.getRoot().listMeshes()) {
      const r = e.jsonDoc.json.meshes[e.meshIndexMap.get(s)];
      for (let n = 0; n < s.listPrimitives().length; n++) {
        const i = s.listPrimitives()[n],
          o = r.primitives[n],
          a = t.primitiveHashMap.get(i);
        if (!a) continue;
        const c = t.primitiveEncodingMap.get(a);
        c &&
          ((o.extensions = o.extensions || {}),
          (o.extensions[Es] = {
            bufferView: e.otherBufferViewsIndexMap.get(c.data),
            attributes: c.attributeIDs,
          }));
      }
    }
    if (!t.primitiveHashMap.size) {
      const t = e.jsonDoc.json;
      (t.extensionsUsed = (t.extensionsUsed || []).filter((e) => e !== Es)),
        (t.extensionsRequired = (t.extensionsRequired || []).filter(
          (e) => e !== Es
        ));
    }
    return this;
  }
}
function Is(e, t) {
  const s = [],
    r = e.getIndices();
  s.push(t.get(r));
  for (const r of e.listAttributes()) s.push(t.get(r));
  return s.sort().join("|");
}
(ys.EXTENSION_NAME = Es), (ys.EncoderMethod = fs);
class Rs extends we {
  init() {
    (this.extensionName = gt),
      (this.propertyType = "Light"),
      (this.parentTypes = [O.NODE]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      color: [1, 1, 1],
      intensity: 1,
      type: Rs.Type.POINT,
      range: null,
      innerConeAngle: 0,
      outerConeAngle: Math.PI / 4,
    });
  }
  getColor() {
    return this.get("color");
  }
  setColor(e) {
    return this.set("color", e);
  }
  getIntensity() {
    return this.get("intensity");
  }
  setIntensity(e) {
    return this.set("intensity", e);
  }
  getType() {
    return this.get("type");
  }
  setType(e) {
    return this.set("type", e);
  }
  getRange() {
    return this.get("range");
  }
  setRange(e) {
    return this.set("range", e);
  }
  getInnerConeAngle() {
    return this.get("innerConeAngle");
  }
  setInnerConeAngle(e) {
    return this.set("innerConeAngle", e);
  }
  getOuterConeAngle() {
    return this.get("outerConeAngle");
  }
  setOuterConeAngle(e) {
    return this.set("outerConeAngle", e);
  }
}
(Rs.EXTENSION_NAME = gt),
  (Rs.Type = { POINT: "point", SPOT: "spot", DIRECTIONAL: "directional" });
const Ns = gt;
class As extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Ns);
  }
  createLight(e = "") {
    return new Rs(this.document.getGraph(), e);
  }
  read(e) {
    const t = e.jsonDoc;
    if (!t.json.extensions || !t.json.extensions[Ns]) return this;
    const s = (t.json.extensions[Ns].lights || []).map((e) => {
      var t, s;
      const r = this.createLight()
        .setName(e.name || "")
        .setType(e.type);
      return (
        void 0 !== e.color && r.setColor(e.color),
        void 0 !== e.intensity && r.setIntensity(e.intensity),
        void 0 !== e.range && r.setRange(e.range),
        void 0 !== (null == (t = e.spot) ? void 0 : t.innerConeAngle) &&
          r.setInnerConeAngle(e.spot.innerConeAngle),
        void 0 !== (null == (s = e.spot) ? void 0 : s.outerConeAngle) &&
          r.setOuterConeAngle(e.spot.outerConeAngle),
        r
      );
    });
    return (
      t.json.nodes.forEach((t, r) => {
        if (!t.extensions || !t.extensions[Ns]) return;
        const n = t.extensions[Ns];
        e.nodes[r].setExtension(Ns, s[n.light]);
      }),
      this
    );
  }
  write(e) {
    const t = e.jsonDoc;
    if (0 === this.properties.size) return this;
    const s = [],
      r = new Map();
    for (const e of this.properties) {
      const t = e,
        n = { type: t.getType() };
      re.eq(t.getColor(), [1, 1, 1]) || (n.color = t.getColor()),
        1 !== t.getIntensity() && (n.intensity = t.getIntensity()),
        null != t.getRange() && (n.range = t.getRange()),
        t.getName() && (n.name = t.getName()),
        t.getType() === Rs.Type.SPOT &&
          (n.spot = {
            innerConeAngle: t.getInnerConeAngle(),
            outerConeAngle: t.getOuterConeAngle(),
          }),
        s.push(n),
        r.set(t, s.length - 1);
    }
    return (
      this.document
        .getRoot()
        .listNodes()
        .forEach((s) => {
          const n = s.getExtension(Ns);
          if (n) {
            const i = e.nodeIndexMap.get(s),
              o = t.json.nodes[i];
            (o.extensions = o.extensions || {}),
              (o.extensions[Ns] = { light: r.get(n) });
          }
        }),
      (t.json.extensions = t.json.extensions || {}),
      (t.json.extensions[Ns] = { lights: s }),
      this
    );
  }
}
As.EXTENSION_NAME = Ns;
const { R: ws, G: bs, B: Ms } = U;
class Ss extends we {
  init() {
    (this.extensionName = mt),
      (this.propertyType = "Anisotropy"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      anisotropyStrength: 0,
      anisotropyRotation: 0,
      anisotropyTexture: null,
      anisotropyTextureInfo: new be(this.graph, "anisotropyTextureInfo"),
    });
  }
  getAnisotropyStrength() {
    return this.get("anisotropyStrength");
  }
  setAnisotropyStrength(e) {
    return this.set("anisotropyStrength", e);
  }
  getAnisotropyRotation() {
    return this.get("anisotropyRotation");
  }
  setAnisotropyRotation(e) {
    return this.set("anisotropyRotation", e);
  }
  getAnisotropyTexture() {
    return this.getRef("anisotropyTexture");
  }
  getAnisotropyTextureInfo() {
    return this.getRef("anisotropyTexture")
      ? this.getRef("anisotropyTextureInfo")
      : null;
  }
  setAnisotropyTexture(e) {
    return this.setRef("anisotropyTexture", e, { channels: ws | bs | Ms });
  }
}
Ss.EXTENSION_NAME = mt;
const _s = mt;
class Cs extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = _s),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createAnisotropy() {
    return new Ss(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[_s]) {
          const n = this.createAnisotropy();
          e.materials[s].setExtension(_s, n);
          const i = t.extensions[_s];
          if (
            (void 0 !== i.anisotropyStrength &&
              n.setAnisotropyStrength(i.anisotropyStrength),
            void 0 !== i.anisotropyRotation &&
              n.setAnisotropyRotation(i.anisotropyRotation),
            void 0 !== i.anisotropyTexture)
          ) {
            const t = i.anisotropyTexture,
              s = e.textures[r[t.index].source];
            n.setAnisotropyTexture(s),
              e.setTextureInfo(n.getAnisotropyTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(_s);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[_s] = {});
            if (
              (r.getAnisotropyStrength() > 0 &&
                (o.anisotropyStrength = r.getAnisotropyStrength()),
              0 !== r.getAnisotropyRotation() &&
                (o.anisotropyRotation = r.getAnisotropyRotation()),
              r.getAnisotropyTexture())
            ) {
              const t = r.getAnisotropyTexture(),
                s = r.getAnisotropyTextureInfo();
              o.anisotropyTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
Cs.EXTENSION_NAME = _s;
const { R: vs, G: Os, B: Ds } = U;
class Fs extends we {
  init() {
    (this.extensionName = xt),
      (this.propertyType = "Clearcoat"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      clearcoatFactor: 0,
      clearcoatTexture: null,
      clearcoatTextureInfo: new be(this.graph, "clearcoatTextureInfo"),
      clearcoatRoughnessFactor: 0,
      clearcoatRoughnessTexture: null,
      clearcoatRoughnessTextureInfo: new be(
        this.graph,
        "clearcoatRoughnessTextureInfo"
      ),
      clearcoatNormalScale: 1,
      clearcoatNormalTexture: null,
      clearcoatNormalTextureInfo: new be(
        this.graph,
        "clearcoatNormalTextureInfo"
      ),
    });
  }
  getClearcoatFactor() {
    return this.get("clearcoatFactor");
  }
  setClearcoatFactor(e) {
    return this.set("clearcoatFactor", e);
  }
  getClearcoatTexture() {
    return this.getRef("clearcoatTexture");
  }
  getClearcoatTextureInfo() {
    return this.getRef("clearcoatTexture")
      ? this.getRef("clearcoatTextureInfo")
      : null;
  }
  setClearcoatTexture(e) {
    return this.setRef("clearcoatTexture", e, { channels: vs });
  }
  getClearcoatRoughnessFactor() {
    return this.get("clearcoatRoughnessFactor");
  }
  setClearcoatRoughnessFactor(e) {
    return this.set("clearcoatRoughnessFactor", e);
  }
  getClearcoatRoughnessTexture() {
    return this.getRef("clearcoatRoughnessTexture");
  }
  getClearcoatRoughnessTextureInfo() {
    return this.getRef("clearcoatRoughnessTexture")
      ? this.getRef("clearcoatRoughnessTextureInfo")
      : null;
  }
  setClearcoatRoughnessTexture(e) {
    return this.setRef("clearcoatRoughnessTexture", e, { channels: Os });
  }
  getClearcoatNormalScale() {
    return this.get("clearcoatNormalScale");
  }
  setClearcoatNormalScale(e) {
    return this.set("clearcoatNormalScale", e);
  }
  getClearcoatNormalTexture() {
    return this.getRef("clearcoatNormalTexture");
  }
  getClearcoatNormalTextureInfo() {
    return this.getRef("clearcoatNormalTexture")
      ? this.getRef("clearcoatNormalTextureInfo")
      : null;
  }
  setClearcoatNormalTexture(e) {
    return this.setRef("clearcoatNormalTexture", e, { channels: vs | Os | Ds });
  }
}
Fs.EXTENSION_NAME = xt;
const Us = xt;
class Ls extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Us),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createClearcoat() {
    return new Fs(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[Us]) {
          const n = this.createClearcoat();
          e.materials[s].setExtension(Us, n);
          const i = t.extensions[Us];
          if (
            (void 0 !== i.clearcoatFactor &&
              n.setClearcoatFactor(i.clearcoatFactor),
            void 0 !== i.clearcoatRoughnessFactor &&
              n.setClearcoatRoughnessFactor(i.clearcoatRoughnessFactor),
            void 0 !== i.clearcoatTexture)
          ) {
            const t = i.clearcoatTexture,
              s = e.textures[r[t.index].source];
            n.setClearcoatTexture(s),
              e.setTextureInfo(n.getClearcoatTextureInfo(), t);
          }
          if (void 0 !== i.clearcoatRoughnessTexture) {
            const t = i.clearcoatRoughnessTexture,
              s = e.textures[r[t.index].source];
            n.setClearcoatRoughnessTexture(s),
              e.setTextureInfo(n.getClearcoatRoughnessTextureInfo(), t);
          }
          if (void 0 !== i.clearcoatNormalTexture) {
            const t = i.clearcoatNormalTexture,
              s = e.textures[r[t.index].source];
            n.setClearcoatNormalTexture(s),
              e.setTextureInfo(n.getClearcoatNormalTextureInfo(), t),
              void 0 !== t.scale && n.setClearcoatNormalScale(t.scale);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(Us);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[Us] = {
              clearcoatFactor: r.getClearcoatFactor(),
              clearcoatRoughnessFactor: r.getClearcoatRoughnessFactor(),
            });
            if (r.getClearcoatTexture()) {
              const t = r.getClearcoatTexture(),
                s = r.getClearcoatTextureInfo();
              o.clearcoatTexture = e.createTextureInfoDef(t, s);
            }
            if (r.getClearcoatRoughnessTexture()) {
              const t = r.getClearcoatRoughnessTexture(),
                s = r.getClearcoatRoughnessTextureInfo();
              o.clearcoatRoughnessTexture = e.createTextureInfoDef(t, s);
            }
            if (r.getClearcoatNormalTexture()) {
              const t = r.getClearcoatNormalTexture(),
                s = r.getClearcoatNormalTextureInfo();
              (o.clearcoatNormalTexture = e.createTextureInfoDef(t, s)),
                1 !== r.getClearcoatNormalScale() &&
                  (o.clearcoatNormalTexture.scale =
                    r.getClearcoatNormalScale());
            }
          }
        }),
      this
    );
  }
}
Ls.EXTENSION_NAME = Us;
const { R: Bs, G: Ps, B: js, A: ks } = U;
class Vs extends we {
  init() {
    (this.extensionName = Tt),
      (this.propertyType = "DiffuseTransmission"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      diffuseTransmissionFactor: 0,
      diffuseTransmissionTexture: null,
      diffuseTransmissionTextureInfo: new be(
        this.graph,
        "diffuseTransmissionTextureInfo"
      ),
      diffuseTransmissionColorFactor: [1, 1, 1],
      diffuseTransmissionColorTexture: null,
      diffuseTransmissionColorTextureInfo: new be(
        this.graph,
        "diffuseTransmissionColorTextureInfo"
      ),
    });
  }
  getDiffuseTransmissionFactor() {
    return this.get("diffuseTransmissionFactor");
  }
  setDiffuseTransmissionFactor(e) {
    return this.set("diffuseTransmissionFactor", e);
  }
  getDiffuseTransmissionTexture() {
    return this.getRef("diffuseTransmissionTexture");
  }
  getDiffuseTransmissionTextureInfo() {
    return this.getRef("diffuseTransmissionTexture")
      ? this.getRef("diffuseTransmissionTextureInfo")
      : null;
  }
  setDiffuseTransmissionTexture(e) {
    return this.setRef("diffuseTransmissionTexture", e, { channels: ks });
  }
  getDiffuseTransmissionColorFactor() {
    return this.get("diffuseTransmissionColorFactor");
  }
  setDiffuseTransmissionColorFactor(e) {
    return this.set("diffuseTransmissionColorFactor", e);
  }
  getDiffuseTransmissionColorTexture() {
    return this.getRef("diffuseTransmissionColorTexture");
  }
  getDiffuseTransmissionColorTextureInfo() {
    return this.getRef("diffuseTransmissionColorTexture")
      ? this.getRef("diffuseTransmissionColorTextureInfo")
      : null;
  }
  setDiffuseTransmissionColorTexture(e) {
    return this.setRef("diffuseTransmissionColorTexture", e, {
      channels: Bs | Ps | js,
    });
  }
}
Vs.EXTENSION_NAME = Tt;
const Gs = Tt;
class Hs extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Gs);
  }
  createDiffuseTransmission() {
    return new Vs(this.document.getGraph());
  }
  read(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[Gs]) {
          const n = this.createDiffuseTransmission();
          e.materials[s].setExtension(Gs, n);
          const i = t.extensions[Gs];
          if (
            (void 0 !== i.diffuseTransmissionFactor &&
              n.setDiffuseTransmissionFactor(i.diffuseTransmissionFactor),
            void 0 !== i.diffuseTransmissionColorFactor &&
              n.setDiffuseTransmissionColorFactor(
                i.diffuseTransmissionColorFactor
              ),
            void 0 !== i.diffuseTransmissionTexture)
          ) {
            const t = i.diffuseTransmissionTexture,
              s = e.textures[r[t.index].source];
            n.setDiffuseTransmissionTexture(s),
              e.setTextureInfo(n.getDiffuseTransmissionTextureInfo(), t);
          }
          if (void 0 !== i.diffuseTransmissionColorTexture) {
            const t = i.diffuseTransmissionColorTexture,
              s = e.textures[r[t.index].source];
            n.setDiffuseTransmissionColorTexture(s),
              e.setTextureInfo(n.getDiffuseTransmissionColorTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  write(e) {
    const t = e.jsonDoc;
    for (const s of this.document.getRoot().listMaterials()) {
      const r = s.getExtension(Gs);
      if (!r) continue;
      const n = e.materialIndexMap.get(s),
        i = t.json.materials[n];
      i.extensions = i.extensions || {};
      const o = (i.extensions[Gs] = {
        diffuseTransmissionFactor: r.getDiffuseTransmissionFactor(),
        diffuseTransmissionColorFactor: r.getDiffuseTransmissionColorFactor(),
      });
      if (r.getDiffuseTransmissionTexture()) {
        const t = r.getDiffuseTransmissionTexture(),
          s = r.getDiffuseTransmissionTextureInfo();
        o.diffuseTransmissionTexture = e.createTextureInfoDef(t, s);
      }
      if (r.getDiffuseTransmissionColorTexture()) {
        const t = r.getDiffuseTransmissionColorTexture(),
          s = r.getDiffuseTransmissionColorTextureInfo();
        o.diffuseTransmissionColorTexture = e.createTextureInfoDef(t, s);
      }
    }
    return this;
  }
}
Hs.EXTENSION_NAME = Gs;
class zs extends we {
  init() {
    (this.extensionName = Et),
      (this.propertyType = "Dispersion"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { dispersion: 0 });
  }
  getDispersion() {
    return this.get("dispersion");
  }
  setDispersion(e) {
    return this.set("dispersion", e);
  }
}
zs.EXTENSION_NAME = Et;
const Xs = Et;
class qs extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Xs),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createDispersion() {
    return new zs(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    return (
      (e.jsonDoc.json.materials || []).forEach((t, s) => {
        if (t.extensions && t.extensions[Xs]) {
          const r = this.createDispersion();
          e.materials[s].setExtension(Xs, r);
          const n = t.extensions[Xs];
          void 0 !== n.dispersion && r.setDispersion(n.dispersion);
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(Xs);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            (i.extensions = i.extensions || {}),
              (i.extensions[Xs] = { dispersion: r.getDispersion() });
          }
        }),
      this
    );
  }
}
qs.EXTENSION_NAME = Xs;
class Ys extends we {
  init() {
    (this.extensionName = yt),
      (this.propertyType = "EmissiveStrength"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { emissiveStrength: 1 });
  }
  getEmissiveStrength() {
    return this.get("emissiveStrength");
  }
  setEmissiveStrength(e) {
    return this.set("emissiveStrength", e);
  }
}
Ys.EXTENSION_NAME = yt;
const Ws = yt;
class Ks extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Ws),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createEmissiveStrength() {
    return new Ys(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    return (
      (e.jsonDoc.json.materials || []).forEach((t, s) => {
        if (t.extensions && t.extensions[Ws]) {
          const r = this.createEmissiveStrength();
          e.materials[s].setExtension(Ws, r);
          const n = t.extensions[Ws];
          void 0 !== n.emissiveStrength &&
            r.setEmissiveStrength(n.emissiveStrength);
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(Ws);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            (i.extensions = i.extensions || {}),
              (i.extensions[Ws] = {
                emissiveStrength: r.getEmissiveStrength(),
              });
          }
        }),
      this
    );
  }
}
Ks.EXTENSION_NAME = Ws;
class $s extends we {
  init() {
    (this.extensionName = It),
      (this.propertyType = "IOR"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { ior: 1.5 });
  }
  getIOR() {
    return this.get("ior");
  }
  setIOR(e) {
    return this.set("ior", e);
  }
}
$s.EXTENSION_NAME = It;
const Js = It;
class Qs extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Js),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createIOR() {
    return new $s(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    return (
      (e.jsonDoc.json.materials || []).forEach((t, s) => {
        if (t.extensions && t.extensions[Js]) {
          const r = this.createIOR();
          e.materials[s].setExtension(Js, r);
          const n = t.extensions[Js];
          void 0 !== n.ior && r.setIOR(n.ior);
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(Js);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            (i.extensions = i.extensions || {}),
              (i.extensions[Js] = { ior: r.getIOR() });
          }
        }),
      this
    );
  }
}
Qs.EXTENSION_NAME = Js;
const { R: Zs, G: er } = U;
class tr extends we {
  init() {
    (this.extensionName = Rt),
      (this.propertyType = "Iridescence"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      iridescenceFactor: 0,
      iridescenceTexture: null,
      iridescenceTextureInfo: new be(this.graph, "iridescenceTextureInfo"),
      iridescenceIOR: 1.3,
      iridescenceThicknessMinimum: 100,
      iridescenceThicknessMaximum: 400,
      iridescenceThicknessTexture: null,
      iridescenceThicknessTextureInfo: new be(
        this.graph,
        "iridescenceThicknessTextureInfo"
      ),
    });
  }
  getIridescenceFactor() {
    return this.get("iridescenceFactor");
  }
  setIridescenceFactor(e) {
    return this.set("iridescenceFactor", e);
  }
  getIridescenceTexture() {
    return this.getRef("iridescenceTexture");
  }
  getIridescenceTextureInfo() {
    return this.getRef("iridescenceTexture")
      ? this.getRef("iridescenceTextureInfo")
      : null;
  }
  setIridescenceTexture(e) {
    return this.setRef("iridescenceTexture", e, { channels: Zs });
  }
  getIridescenceIOR() {
    return this.get("iridescenceIOR");
  }
  setIridescenceIOR(e) {
    return this.set("iridescenceIOR", e);
  }
  getIridescenceThicknessMinimum() {
    return this.get("iridescenceThicknessMinimum");
  }
  setIridescenceThicknessMinimum(e) {
    return this.set("iridescenceThicknessMinimum", e);
  }
  getIridescenceThicknessMaximum() {
    return this.get("iridescenceThicknessMaximum");
  }
  setIridescenceThicknessMaximum(e) {
    return this.set("iridescenceThicknessMaximum", e);
  }
  getIridescenceThicknessTexture() {
    return this.getRef("iridescenceThicknessTexture");
  }
  getIridescenceThicknessTextureInfo() {
    return this.getRef("iridescenceThicknessTexture")
      ? this.getRef("iridescenceThicknessTextureInfo")
      : null;
  }
  setIridescenceThicknessTexture(e) {
    return this.setRef("iridescenceThicknessTexture", e, { channels: er });
  }
}
tr.EXTENSION_NAME = Rt;
const sr = Rt;
class rr extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = sr),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createIridescence() {
    return new tr(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[sr]) {
          const n = this.createIridescence();
          e.materials[s].setExtension(sr, n);
          const i = t.extensions[sr];
          if (
            (void 0 !== i.iridescenceFactor &&
              n.setIridescenceFactor(i.iridescenceFactor),
            void 0 !== i.iridescenceIor &&
              n.setIridescenceIOR(i.iridescenceIor),
            void 0 !== i.iridescenceThicknessMinimum &&
              n.setIridescenceThicknessMinimum(i.iridescenceThicknessMinimum),
            void 0 !== i.iridescenceThicknessMaximum &&
              n.setIridescenceThicknessMaximum(i.iridescenceThicknessMaximum),
            void 0 !== i.iridescenceTexture)
          ) {
            const t = i.iridescenceTexture,
              s = e.textures[r[t.index].source];
            n.setIridescenceTexture(s),
              e.setTextureInfo(n.getIridescenceTextureInfo(), t);
          }
          if (void 0 !== i.iridescenceThicknessTexture) {
            const t = i.iridescenceThicknessTexture,
              s = e.textures[r[t.index].source];
            n.setIridescenceThicknessTexture(s),
              e.setTextureInfo(n.getIridescenceThicknessTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(sr);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[sr] = {});
            if (
              (r.getIridescenceFactor() > 0 &&
                (o.iridescenceFactor = r.getIridescenceFactor()),
              1.3 !== r.getIridescenceIOR() &&
                (o.iridescenceIor = r.getIridescenceIOR()),
              100 !== r.getIridescenceThicknessMinimum() &&
                (o.iridescenceThicknessMinimum =
                  r.getIridescenceThicknessMinimum()),
              400 !== r.getIridescenceThicknessMaximum() &&
                (o.iridescenceThicknessMaximum =
                  r.getIridescenceThicknessMaximum()),
              r.getIridescenceTexture())
            ) {
              const t = r.getIridescenceTexture(),
                s = r.getIridescenceTextureInfo();
              o.iridescenceTexture = e.createTextureInfoDef(t, s);
            }
            if (r.getIridescenceThicknessTexture()) {
              const t = r.getIridescenceThicknessTexture(),
                s = r.getIridescenceThicknessTextureInfo();
              o.iridescenceThicknessTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
rr.EXTENSION_NAME = sr;
const { R: nr, G: ir, B: or, A: ar } = U;
class cr extends we {
  init() {
    (this.extensionName = Nt),
      (this.propertyType = "PBRSpecularGlossiness"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      diffuseFactor: [1, 1, 1, 1],
      diffuseTexture: null,
      diffuseTextureInfo: new be(this.graph, "diffuseTextureInfo"),
      specularFactor: [1, 1, 1],
      glossinessFactor: 1,
      specularGlossinessTexture: null,
      specularGlossinessTextureInfo: new be(
        this.graph,
        "specularGlossinessTextureInfo"
      ),
    });
  }
  getDiffuseFactor() {
    return this.get("diffuseFactor");
  }
  setDiffuseFactor(e) {
    return this.set("diffuseFactor", e);
  }
  getDiffuseTexture() {
    return this.getRef("diffuseTexture");
  }
  getDiffuseTextureInfo() {
    return this.getRef("diffuseTexture")
      ? this.getRef("diffuseTextureInfo")
      : null;
  }
  setDiffuseTexture(e) {
    return this.setRef("diffuseTexture", e, {
      channels: nr | ir | or | ar,
      isColor: !0,
    });
  }
  getSpecularFactor() {
    return this.get("specularFactor");
  }
  setSpecularFactor(e) {
    return this.set("specularFactor", e);
  }
  getGlossinessFactor() {
    return this.get("glossinessFactor");
  }
  setGlossinessFactor(e) {
    return this.set("glossinessFactor", e);
  }
  getSpecularGlossinessTexture() {
    return this.getRef("specularGlossinessTexture");
  }
  getSpecularGlossinessTextureInfo() {
    return this.getRef("specularGlossinessTexture")
      ? this.getRef("specularGlossinessTextureInfo")
      : null;
  }
  setSpecularGlossinessTexture(e) {
    return this.setRef("specularGlossinessTexture", e, {
      channels: nr | ir | or | ar,
    });
  }
}
cr.EXTENSION_NAME = Nt;
const ur = Nt;
class hr extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = ur),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createPBRSpecularGlossiness() {
    return new cr(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[ur]) {
          const n = this.createPBRSpecularGlossiness();
          e.materials[s].setExtension(ur, n);
          const i = t.extensions[ur];
          if (
            (void 0 !== i.diffuseFactor && n.setDiffuseFactor(i.diffuseFactor),
            void 0 !== i.specularFactor &&
              n.setSpecularFactor(i.specularFactor),
            void 0 !== i.glossinessFactor &&
              n.setGlossinessFactor(i.glossinessFactor),
            void 0 !== i.diffuseTexture)
          ) {
            const t = i.diffuseTexture,
              s = e.textures[r[t.index].source];
            n.setDiffuseTexture(s),
              e.setTextureInfo(n.getDiffuseTextureInfo(), t);
          }
          if (void 0 !== i.specularGlossinessTexture) {
            const t = i.specularGlossinessTexture,
              s = e.textures[r[t.index].source];
            n.setSpecularGlossinessTexture(s),
              e.setTextureInfo(n.getSpecularGlossinessTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(ur);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[ur] = {
              diffuseFactor: r.getDiffuseFactor(),
              specularFactor: r.getSpecularFactor(),
              glossinessFactor: r.getGlossinessFactor(),
            });
            if (r.getDiffuseTexture()) {
              const t = r.getDiffuseTexture(),
                s = r.getDiffuseTextureInfo();
              o.diffuseTexture = e.createTextureInfoDef(t, s);
            }
            if (r.getSpecularGlossinessTexture()) {
              const t = r.getSpecularGlossinessTexture(),
                s = r.getSpecularGlossinessTextureInfo();
              o.specularGlossinessTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
hr.EXTENSION_NAME = ur;
const { R: fr, G: lr, B: pr, A: dr } = U;
class gr extends we {
  init() {
    (this.extensionName = At),
      (this.propertyType = "Sheen"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      sheenColorFactor: [0, 0, 0],
      sheenColorTexture: null,
      sheenColorTextureInfo: new be(this.graph, "sheenColorTextureInfo"),
      sheenRoughnessFactor: 0,
      sheenRoughnessTexture: null,
      sheenRoughnessTextureInfo: new be(
        this.graph,
        "sheenRoughnessTextureInfo"
      ),
    });
  }
  getSheenColorFactor() {
    return this.get("sheenColorFactor");
  }
  setSheenColorFactor(e) {
    return this.set("sheenColorFactor", e);
  }
  getSheenColorTexture() {
    return this.getRef("sheenColorTexture");
  }
  getSheenColorTextureInfo() {
    return this.getRef("sheenColorTexture")
      ? this.getRef("sheenColorTextureInfo")
      : null;
  }
  setSheenColorTexture(e) {
    return this.setRef("sheenColorTexture", e, {
      channels: fr | lr | pr,
      isColor: !0,
    });
  }
  getSheenRoughnessFactor() {
    return this.get("sheenRoughnessFactor");
  }
  setSheenRoughnessFactor(e) {
    return this.set("sheenRoughnessFactor", e);
  }
  getSheenRoughnessTexture() {
    return this.getRef("sheenRoughnessTexture");
  }
  getSheenRoughnessTextureInfo() {
    return this.getRef("sheenRoughnessTexture")
      ? this.getRef("sheenRoughnessTextureInfo")
      : null;
  }
  setSheenRoughnessTexture(e) {
    return this.setRef("sheenRoughnessTexture", e, { channels: dr });
  }
}
gr.EXTENSION_NAME = At;
const mr = At;
class xr extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = mr),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createSheen() {
    return new gr(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[mr]) {
          const n = this.createSheen();
          e.materials[s].setExtension(mr, n);
          const i = t.extensions[mr];
          if (
            (void 0 !== i.sheenColorFactor &&
              n.setSheenColorFactor(i.sheenColorFactor),
            void 0 !== i.sheenRoughnessFactor &&
              n.setSheenRoughnessFactor(i.sheenRoughnessFactor),
            void 0 !== i.sheenColorTexture)
          ) {
            const t = i.sheenColorTexture,
              s = e.textures[r[t.index].source];
            n.setSheenColorTexture(s),
              e.setTextureInfo(n.getSheenColorTextureInfo(), t);
          }
          if (void 0 !== i.sheenRoughnessTexture) {
            const t = i.sheenRoughnessTexture,
              s = e.textures[r[t.index].source];
            n.setSheenRoughnessTexture(s),
              e.setTextureInfo(n.getSheenRoughnessTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(mr);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[mr] = {
              sheenColorFactor: r.getSheenColorFactor(),
              sheenRoughnessFactor: r.getSheenRoughnessFactor(),
            });
            if (r.getSheenColorTexture()) {
              const t = r.getSheenColorTexture(),
                s = r.getSheenColorTextureInfo();
              o.sheenColorTexture = e.createTextureInfoDef(t, s);
            }
            if (r.getSheenRoughnessTexture()) {
              const t = r.getSheenRoughnessTexture(),
                s = r.getSheenRoughnessTextureInfo();
              o.sheenRoughnessTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
xr.EXTENSION_NAME = mr;
const { R: Tr, G: Er, B: yr, A: Ir } = U;
class Rr extends we {
  init() {
    (this.extensionName = wt),
      (this.propertyType = "Specular"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      specularFactor: 1,
      specularTexture: null,
      specularTextureInfo: new be(this.graph, "specularTextureInfo"),
      specularColorFactor: [1, 1, 1],
      specularColorTexture: null,
      specularColorTextureInfo: new be(this.graph, "specularColorTextureInfo"),
    });
  }
  getSpecularFactor() {
    return this.get("specularFactor");
  }
  setSpecularFactor(e) {
    return this.set("specularFactor", e);
  }
  getSpecularColorFactor() {
    return this.get("specularColorFactor");
  }
  setSpecularColorFactor(e) {
    return this.set("specularColorFactor", e);
  }
  getSpecularTexture() {
    return this.getRef("specularTexture");
  }
  getSpecularTextureInfo() {
    return this.getRef("specularTexture")
      ? this.getRef("specularTextureInfo")
      : null;
  }
  setSpecularTexture(e) {
    return this.setRef("specularTexture", e, { channels: Ir });
  }
  getSpecularColorTexture() {
    return this.getRef("specularColorTexture");
  }
  getSpecularColorTextureInfo() {
    return this.getRef("specularColorTexture")
      ? this.getRef("specularColorTextureInfo")
      : null;
  }
  setSpecularColorTexture(e) {
    return this.setRef("specularColorTexture", e, {
      channels: Tr | Er | yr,
      isColor: !0,
    });
  }
}
Rr.EXTENSION_NAME = wt;
const Nr = wt;
class Ar extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Nr),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createSpecular() {
    return new Rr(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[Nr]) {
          const n = this.createSpecular();
          e.materials[s].setExtension(Nr, n);
          const i = t.extensions[Nr];
          if (
            (void 0 !== i.specularFactor &&
              n.setSpecularFactor(i.specularFactor),
            void 0 !== i.specularColorFactor &&
              n.setSpecularColorFactor(i.specularColorFactor),
            void 0 !== i.specularTexture)
          ) {
            const t = i.specularTexture,
              s = e.textures[r[t.index].source];
            n.setSpecularTexture(s),
              e.setTextureInfo(n.getSpecularTextureInfo(), t);
          }
          if (void 0 !== i.specularColorTexture) {
            const t = i.specularColorTexture,
              s = e.textures[r[t.index].source];
            n.setSpecularColorTexture(s),
              e.setTextureInfo(n.getSpecularColorTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(Nr);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[Nr] = {});
            if (
              (1 !== r.getSpecularFactor() &&
                (o.specularFactor = r.getSpecularFactor()),
              re.eq(r.getSpecularColorFactor(), [1, 1, 1]) ||
                (o.specularColorFactor = r.getSpecularColorFactor()),
              r.getSpecularTexture())
            ) {
              const t = r.getSpecularTexture(),
                s = r.getSpecularTextureInfo();
              o.specularTexture = e.createTextureInfoDef(t, s);
            }
            if (r.getSpecularColorTexture()) {
              const t = r.getSpecularColorTexture(),
                s = r.getSpecularColorTextureInfo();
              o.specularColorTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
Ar.EXTENSION_NAME = Nr;
const { R: wr } = U;
class br extends we {
  init() {
    (this.extensionName = bt),
      (this.propertyType = "Transmission"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      transmissionFactor: 0,
      transmissionTexture: null,
      transmissionTextureInfo: new be(this.graph, "transmissionTextureInfo"),
    });
  }
  getTransmissionFactor() {
    return this.get("transmissionFactor");
  }
  setTransmissionFactor(e) {
    return this.set("transmissionFactor", e);
  }
  getTransmissionTexture() {
    return this.getRef("transmissionTexture");
  }
  getTransmissionTextureInfo() {
    return this.getRef("transmissionTexture")
      ? this.getRef("transmissionTextureInfo")
      : null;
  }
  setTransmissionTexture(e) {
    return this.setRef("transmissionTexture", e, { channels: wr });
  }
}
br.EXTENSION_NAME = bt;
const Mr = bt;
class Sr extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Mr),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createTransmission() {
    return new br(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[Mr]) {
          const n = this.createTransmission();
          e.materials[s].setExtension(Mr, n);
          const i = t.extensions[Mr];
          if (
            (void 0 !== i.transmissionFactor &&
              n.setTransmissionFactor(i.transmissionFactor),
            void 0 !== i.transmissionTexture)
          ) {
            const t = i.transmissionTexture,
              s = e.textures[r[t.index].source];
            n.setTransmissionTexture(s),
              e.setTextureInfo(n.getTransmissionTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(Mr);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[Mr] = {
              transmissionFactor: r.getTransmissionFactor(),
            });
            if (r.getTransmissionTexture()) {
              const t = r.getTransmissionTexture(),
                s = r.getTransmissionTextureInfo();
              o.transmissionTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
Sr.EXTENSION_NAME = Mr;
class _r extends we {
  init() {
    (this.extensionName = Mt),
      (this.propertyType = "Unlit"),
      (this.parentTypes = [O.MATERIAL]);
  }
}
_r.EXTENSION_NAME = Mt;
const Cr = Mt;
class vr extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = Cr),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createUnlit() {
    return new _r(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    return (
      (e.jsonDoc.json.materials || []).forEach((t, s) => {
        t.extensions &&
          t.extensions[Cr] &&
          e.materials[s].setExtension(Cr, this.createUnlit());
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          if (s.getExtension(Cr)) {
            const r = e.materialIndexMap.get(s),
              n = t.json.materials[r];
            (n.extensions = n.extensions || {}), (n.extensions[Cr] = {});
          }
        }),
      this
    );
  }
}
vr.EXTENSION_NAME = Cr;
class Or extends we {
  init() {
    (this.extensionName = _t),
      (this.propertyType = "Mapping"),
      (this.parentTypes = ["MappingList"]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      material: null,
      variants: new w(),
    });
  }
  getMaterial() {
    return this.getRef("material");
  }
  setMaterial(e) {
    return this.setRef("material", e);
  }
  addVariant(e) {
    return this.addRef("variants", e);
  }
  removeVariant(e) {
    return this.removeRef("variants", e);
  }
  listVariants() {
    return this.listRefs("variants");
  }
}
Or.EXTENSION_NAME = _t;
class Dr extends we {
  init() {
    (this.extensionName = _t),
      (this.propertyType = "MappingList"),
      (this.parentTypes = [O.PRIMITIVE]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { mappings: new w() });
  }
  addMapping(e) {
    return this.addRef("mappings", e);
  }
  removeMapping(e) {
    return this.removeRef("mappings", e);
  }
  listMappings() {
    return this.listRefs("mappings");
  }
}
Dr.EXTENSION_NAME = _t;
class Fr extends we {
  init() {
    (this.extensionName = _t),
      (this.propertyType = "Variant"),
      (this.parentTypes = ["MappingList"]);
  }
}
Fr.EXTENSION_NAME = _t;
const Ur = _t;
class Lr extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Ur);
  }
  createMappingList() {
    return new Dr(this.document.getGraph());
  }
  createVariant(e = "") {
    return new Fr(this.document.getGraph(), e);
  }
  createMapping() {
    return new Or(this.document.getGraph());
  }
  listVariants() {
    return Array.from(this.properties).filter((e) => e instanceof Fr);
  }
  read(e) {
    const t = e.jsonDoc;
    if (!t.json.extensions || !t.json.extensions[Ur]) return this;
    const s = (t.json.extensions[Ur].variants || []).map((e) =>
      this.createVariant().setName(e.name || "")
    );
    return (
      (t.json.meshes || []).forEach((t, r) => {
        const n = e.meshes[r];
        (t.primitives || []).forEach((t, r) => {
          if (!t.extensions || !t.extensions[Ur]) return;
          const i = this.createMappingList(),
            o = t.extensions[Ur];
          for (const t of o.mappings) {
            const r = this.createMapping();
            void 0 !== t.material && r.setMaterial(e.materials[t.material]);
            for (const e of t.variants || []) r.addVariant(s[e]);
            i.addMapping(r);
          }
          n.listPrimitives()[r].setExtension(Ur, i);
        });
      }),
      this
    );
  }
  write(e) {
    const t = e.jsonDoc,
      s = this.listVariants();
    if (!s.length) return this;
    const r = [],
      n = new Map();
    for (const t of s) n.set(t, r.length), r.push(e.createPropertyDef(t));
    for (const t of this.document.getRoot().listMeshes()) {
      const s = e.meshIndexMap.get(t);
      t.listPrimitives().forEach((t, r) => {
        const i = t.getExtension(Ur);
        if (!i) return;
        const o = e.jsonDoc.json.meshes[s].primitives[r],
          a = i.listMappings().map((t) => {
            const s = e.createPropertyDef(t),
              r = t.getMaterial();
            return (
              r && (s.material = e.materialIndexMap.get(r)),
              (s.variants = t.listVariants().map((e) => n.get(e))),
              s
            );
          });
        (o.extensions = o.extensions || {}),
          (o.extensions[Ur] = { mappings: a });
      });
    }
    return (
      (t.json.extensions = t.json.extensions || {}),
      (t.json.extensions[Ur] = { variants: r }),
      this
    );
  }
}
Lr.EXTENSION_NAME = Ur;
const { G: Br } = U;
class Pr extends we {
  init() {
    (this.extensionName = St),
      (this.propertyType = "Volume"),
      (this.parentTypes = [O.MATERIAL]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      thicknessFactor: 0,
      thicknessTexture: null,
      thicknessTextureInfo: new be(this.graph, "thicknessTexture"),
      attenuationDistance: 1 / 0,
      attenuationColor: [1, 1, 1],
    });
  }
  getThicknessFactor() {
    return this.get("thicknessFactor");
  }
  setThicknessFactor(e) {
    return this.set("thicknessFactor", e);
  }
  getThicknessTexture() {
    return this.getRef("thicknessTexture");
  }
  getThicknessTextureInfo() {
    return this.getRef("thicknessTexture")
      ? this.getRef("thicknessTextureInfo")
      : null;
  }
  setThicknessTexture(e) {
    return this.setRef("thicknessTexture", e, { channels: Br });
  }
  getAttenuationDistance() {
    return this.get("attenuationDistance");
  }
  setAttenuationDistance(e) {
    return this.set("attenuationDistance", e);
  }
  getAttenuationColor() {
    return this.get("attenuationColor");
  }
  setAttenuationColor(e) {
    return this.set("attenuationColor", e);
  }
}
Pr.EXTENSION_NAME = St;
const jr = St;
class kr extends Ge {
  constructor(...e) {
    super(...e),
      (this.extensionName = jr),
      (this.prereadTypes = [O.MESH]),
      (this.prewriteTypes = [O.MESH]);
  }
  createVolume() {
    return new Pr(this.document.getGraph());
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
  preread(e) {
    const t = e.jsonDoc,
      s = t.json.materials || [],
      r = t.json.textures || [];
    return (
      s.forEach((t, s) => {
        if (t.extensions && t.extensions[jr]) {
          const n = this.createVolume();
          e.materials[s].setExtension(jr, n);
          const i = t.extensions[jr];
          if (
            (void 0 !== i.thicknessFactor &&
              n.setThicknessFactor(i.thicknessFactor),
            void 0 !== i.attenuationDistance &&
              n.setAttenuationDistance(i.attenuationDistance),
            void 0 !== i.attenuationColor &&
              n.setAttenuationColor(i.attenuationColor),
            void 0 !== i.thicknessTexture)
          ) {
            const t = i.thicknessTexture,
              s = e.textures[r[t.index].source];
            n.setThicknessTexture(s),
              e.setTextureInfo(n.getThicknessTextureInfo(), t);
          }
        }
      }),
      this
    );
  }
  prewrite(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listMaterials()
        .forEach((s) => {
          const r = s.getExtension(jr);
          if (r) {
            const n = e.materialIndexMap.get(s),
              i = t.json.materials[n];
            i.extensions = i.extensions || {};
            const o = (i.extensions[jr] = {});
            if (
              (r.getThicknessFactor() > 0 &&
                (o.thicknessFactor = r.getThicknessFactor()),
              Number.isFinite(r.getAttenuationDistance()) &&
                (o.attenuationDistance = r.getAttenuationDistance()),
              re.eq(r.getAttenuationColor(), [1, 1, 1]) ||
                (o.attenuationColor = r.getAttenuationColor()),
              r.getThicknessTexture())
            ) {
              const t = r.getThicknessTexture(),
                s = r.getThicknessTextureInfo();
              o.thicknessTexture = e.createTextureInfoDef(t, s);
            }
          }
        }),
      this
    );
  }
}
kr.EXTENSION_NAME = jr;
const Vr = "KHR_mesh_quantization";
class Gr extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Vr);
  }
  read(e) {
    return this;
  }
  write(e) {
    return this;
  }
}
Gr.EXTENSION_NAME = Vr;
const Hr = "KHR_texture_basisu";
class zr {
  match(e) {
    return (
      171 === e[0] &&
      75 === e[1] &&
      84 === e[2] &&
      88 === e[3] &&
      32 === e[4] &&
      50 === e[5] &&
      48 === e[6] &&
      187 === e[7] &&
      13 === e[8] &&
      10 === e[9] &&
      26 === e[10] &&
      10 === e[11]
    );
  }
  getSize(e) {
    const t = ft(e);
    return [t.pixelWidth, t.pixelHeight];
  }
  getChannels(e) {
    const t = ft(e).dataFormatDescriptor[0];
    if (163 === t.colorModel)
      return 2 !== t.samples.length || 15 & ~t.samples[1].channelType ? 3 : 4;
    if (166 === t.colorModel)
      return 3 == (15 & t.samples[0].channelType) ? 4 : 3;
    throw new Error(`Unexpected KTX2 colorModel, "${t.colorModel}".`);
  }
  getVRAMByteLength(e) {
    const t = ft(e),
      s = this.getChannels(e) > 3;
    let r = 0;
    for (let e = 0; e < t.levels.length; e++) {
      const n = t.levels[e];
      if (n.uncompressedByteLength) r += n.uncompressedByteLength;
      else {
        r +=
          (Math.max(1, Math.floor(t.pixelWidth / Math.pow(2, e))) / 4) *
          (Math.max(1, Math.floor(t.pixelHeight / Math.pow(2, e))) / 4) *
          (s ? 16 : 8);
      }
    }
    return r;
  }
}
class Xr extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Hr), (this.prereadTypes = [O.TEXTURE]);
  }
  static register() {
    K.registerFormat("image/ktx2", new zr());
  }
  preread(e) {
    return (
      e.jsonDoc.json.textures.forEach((e) => {
        if (e.extensions && e.extensions[Hr]) {
          const t = e.extensions[Hr];
          e.source = t.source;
        }
      }),
      this
    );
  }
  read(e) {
    return this;
  }
  write(e) {
    const t = e.jsonDoc;
    return (
      this.document
        .getRoot()
        .listTextures()
        .forEach((s) => {
          if ("image/ktx2" === s.getMimeType()) {
            const r = e.imageIndexMap.get(s);
            t.json.textures.forEach((e) => {
              e.source === r &&
                ((e.extensions = e.extensions || {}),
                (e.extensions[Hr] = { source: e.source }),
                delete e.source);
            });
          }
        }),
      this
    );
  }
}
Xr.EXTENSION_NAME = Hr;
class qr extends we {
  init() {
    (this.extensionName = Ct),
      (this.propertyType = "Transform"),
      (this.parentTypes = [O.TEXTURE_INFO]);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), {
      offset: [0, 0],
      rotation: 0,
      scale: [1, 1],
      texCoord: null,
    });
  }
  getOffset() {
    return this.get("offset");
  }
  setOffset(e) {
    return this.set("offset", e);
  }
  getRotation() {
    return this.get("rotation");
  }
  setRotation(e) {
    return this.set("rotation", e);
  }
  getScale() {
    return this.get("scale");
  }
  setScale(e) {
    return this.set("scale", e);
  }
  getTexCoord() {
    return this.get("texCoord");
  }
  setTexCoord(e) {
    return this.set("texCoord", e);
  }
}
qr.EXTENSION_NAME = Ct;
const Yr = Ct;
class Wr extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Yr);
  }
  createTransform() {
    return new qr(this.document.getGraph());
  }
  read(e) {
    for (const [t, s] of Array.from(e.textureInfos.entries())) {
      if (!s.extensions || !s.extensions[Yr]) continue;
      const e = this.createTransform(),
        r = s.extensions[Yr];
      void 0 !== r.offset && e.setOffset(r.offset),
        void 0 !== r.rotation && e.setRotation(r.rotation),
        void 0 !== r.scale && e.setScale(r.scale),
        void 0 !== r.texCoord && e.setTexCoord(r.texCoord),
        t.setExtension(Yr, e);
    }
    return this;
  }
  write(e) {
    const t = Array.from(e.textureInfoDefMap.entries());
    for (const [e, s] of t) {
      const t = e.getExtension(Yr);
      if (!t) continue;
      s.extensions = s.extensions || {};
      const r = {},
        n = re.eq;
      n(t.getOffset(), [0, 0]) || (r.offset = t.getOffset()),
        0 !== t.getRotation() && (r.rotation = t.getRotation()),
        n(t.getScale(), [1, 1]) || (r.scale = t.getScale()),
        null != t.getTexCoord() && (r.texCoord = t.getTexCoord()),
        (s.extensions[Yr] = r);
    }
    return this;
  }
}
Wr.EXTENSION_NAME = Yr;
const Kr = [
  O.ROOT,
  O.SCENE,
  O.NODE,
  O.MESH,
  O.MATERIAL,
  O.TEXTURE,
  O.ANIMATION,
];
class $r extends we {
  init() {
    (this.extensionName = vt),
      (this.propertyType = "Packet"),
      (this.parentTypes = Kr);
  }
  getDefaults() {
    return Object.assign(super.getDefaults(), { context: {}, properties: {} });
  }
  getContext() {
    return this.get("context");
  }
  setContext(e) {
    return this.set("context", Lt({}, e));
  }
  listProperties() {
    return Object.keys(this.get("properties"));
  }
  getProperty(e) {
    const t = this.get("properties");
    return e in t ? t[e] : null;
  }
  setProperty(e, t) {
    this._assertContext(e);
    const s = Lt({}, this.get("properties"));
    return t ? (s[e] = t) : delete s[e], this.set("properties", s);
  }
  toJSONLD() {
    return Lt(
      { "@context": Jr(this.get("context")) },
      Jr(this.get("properties"))
    );
  }
  fromJSONLD(e) {
    const t = (e = Jr(e))["@context"];
    return (
      t && this.set("context", t),
      delete e["@context"],
      this.set("properties", e)
    );
  }
  _assertContext(e) {
    if (!(e.split(":")[0] in this.get("context")))
      throw new Error(`${vt}: Missing context for term, "${e}".`);
  }
}
function Jr(e) {
  return JSON.parse(JSON.stringify(e));
}
$r.EXTENSION_NAME = vt;
const Qr = vt;
class Zr extends Ge {
  constructor(...e) {
    super(...e), (this.extensionName = Qr);
  }
  createPacket() {
    return new $r(this.document.getGraph());
  }
  listPackets() {
    return Array.from(this.properties);
  }
  read(e) {
    var t;
    const s = null == (t = e.jsonDoc.json.extensions) ? void 0 : t[Qr];
    if (!s || !s.packets) return this;
    const r = e.jsonDoc.json,
      n = this.document.getRoot(),
      i = s.packets.map((e) => this.createPacket().fromJSONLD(e)),
      o = [
        [r.asset],
        r.scenes,
        r.nodes,
        r.meshes,
        r.materials,
        r.images,
        r.animations,
      ],
      a = [
        [n],
        n.listScenes(),
        n.listNodes(),
        n.listMeshes(),
        n.listMaterials(),
        n.listTextures(),
        n.listAnimations(),
      ];
    for (let e = 0; e < o.length; e++) {
      const t = o[e] || [];
      for (let s = 0; s < t.length; s++) {
        const r = t[s];
        if (r.extensions && r.extensions[Qr]) {
          const t = r.extensions[Qr];
          a[e][s].setExtension(Qr, i[t.packet]);
        }
      }
    }
    return this;
  }
  write(e) {
    const { json: t } = e.jsonDoc,
      s = [];
    for (const r of this.properties) {
      s.push(r.toJSONLD());
      for (const n of r.listParents()) {
        let r;
        switch (n.propertyType) {
          case O.ROOT:
            r = t.asset;
            break;
          case O.SCENE:
            r = t.scenes[e.sceneIndexMap.get(n)];
            break;
          case O.NODE:
            r = t.nodes[e.nodeIndexMap.get(n)];
            break;
          case O.MESH:
            r = t.meshes[e.meshIndexMap.get(n)];
            break;
          case O.MATERIAL:
            r = t.materials[e.materialIndexMap.get(n)];
            break;
          case O.TEXTURE:
            r = t.images[e.imageIndexMap.get(n)];
            break;
          case O.ANIMATION:
            r = t.animations[e.animationIndexMap.get(n)];
            break;
          default:
            (r = null),
              this.document
                .getLogger()
                .warn(
                  `[${Qr}]: Unsupported parent property, "${n.propertyType}"`
                );
        }
        r &&
          ((r.extensions = r.extensions || {}),
          (r.extensions[Qr] = { packet: s.length - 1 }));
      }
    }
    return (
      s.length > 0 &&
        ((t.extensions = t.extensions || {}),
        (t.extensions[Qr] = { packets: s })),
      this
    );
  }
}
Zr.EXTENSION_NAME = Qr;
const en = [
  ys,
  As,
  Cs,
  Ls,
  Hs,
  qs,
  Ks,
  Qs,
  rr,
  hr,
  Ar,
  xr,
  Sr,
  vr,
  Lr,
  kr,
  Gr,
  Xr,
  Wr,
  Zr,
];
function tn() {
  var e = [
    "forEach",
    "3314432QiBkZg",
    "4031328gkJSmS",
    "textures",
    "TEXTURE",
    "parentTypes",
    "181693LXRkDT",
    "MATERIAL",
    "2687002TgBuri",
    "dispose",
    "MESH",
    "3pDcvEo",
    "260LzQKlQ",
    "2992232jxVMqB",
    "14mixPCb",
    "addTexture",
    "values",
    "init",
    "GenericExtension",
    "propertyType",
    "extensionName",
    "6354295IbdlXg",
    "358542siIgmU",
  ];
  return (tn = function () {
    return e;
  })();
}
var sn = rn;
function rn(e, t) {
  var s = tn();
  return (rn = function (e, t) {
    return s[(e -= 426)];
  })(e, t);
}
!(function (e, t) {
  for (var s = rn, r = tn(); ; )
    try {
      if (
        805541 ===
        -parseInt(s(443)) / 1 +
          (parseInt(s(445)) / 2) * (parseInt(s(448)) / 3) +
          parseInt(s(438)) / 4 +
          parseInt(s(435)) / 5 +
          -parseInt(s(439)) / 6 +
          (-parseInt(s(428)) / 7) * (parseInt(s(427)) / 8) +
          (-parseInt(s(436)) / 9) * (parseInt(s(426)) / 10)
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
class nn extends we {
  [sn(429)](e, t, s, r = 4369) {
    var n = sn;
    this.setRef(e, s, { channels: r }), (this[n(440)][e] = [t, s]);
  }
  dispose() {
    var e = sn;
    Object[e(430)](this.textures)[e(437)](([t, s]) => {
      var r = e;
      t?.[r(446)]();
    }),
      super[e(446)]();
  }
  constructor(e, t, s) {
    var r = sn;
    super(e, t),
      (this[r(442)] = [O[r(444)], O[r(447)], O.NODE, O.SCENE, O[r(441)]]),
      (this[r(433)] = r(432)),
      (this[r(440)] = {}),
      (this[r(434)] = s);
  }
  [sn(431)]() {}
}
const on = an;
function an(e, t) {
  const s = cn();
  return (an = function (e, t) {
    return s[(e -= 208)];
  })(e, t);
}
function cn() {
  const e = [
    "nodes",
    "textures",
    "imageIndexMap",
    "index",
    "jsonDoc",
    "json",
    "textureChannels",
    "1802185GPlzQj",
    "getRoot",
    "2715097QynbNz",
    "8LGTmwz",
    "nodeIndexMap",
    "register",
    "setTextureInfo",
    "GLTF Pipeline: source texture not found for texture info",
    "scene",
    "materialIndexMap",
    "number",
    "meshes",
    "get",
    "1741941sZqiyc",
    "listNodes",
    "write",
    "addTexture",
    "entries",
    "extensionName",
    "getExtras",
    "listMeshes",
    "source",
    "578552VRGAhp",
    "scenes",
    "16629FnCkqH",
    "meshIndexMap",
    "6WwdZeK",
    "setExtras",
    "5628920vtBPsb",
    "setExtension",
    "20uRlAzn",
    "listMaterials",
    "read",
    "214415kRzEbK",
    "214KBRxbm",
    "materials",
    "getGraph",
    "getExtension",
    "document",
    "forEach",
    "listScenes",
    "extensions",
  ];
  return (cn = function () {
    return e;
  })();
}
!(function (e, t) {
  const s = an,
    r = cn();
  for (;;)
    try {
      if (
        332539 ===
        -parseInt(s(215)) / 1 +
          (-parseInt(s(216)) / 2) * (-parseInt(s(255)) / 3) +
          (-parseInt(s(253)) / 4) * (parseInt(s(212)) / 5) +
          (parseInt(s(208)) / 6) * (-parseInt(s(233)) / 7) +
          (parseInt(s(234)) / 8) * (parseInt(s(244)) / 9) +
          parseInt(s(210)) / 10 +
          parseInt(s(231)) / 11
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
class un extends Ge {
  constructor() {
    super(...arguments), (this.textureChannels = {});
  }
  [on(214)](e) {
    const t = on,
      s = e[t(228)],
      r = s[t(229)][t(217)] || [],
      n = s[t(229)][t(225)] || [];
    n[t(221)]((s, r) => {
      const n = t;
      if (s[n(223)] && s.extensions[this[n(249)]]) {
        const t = new nn(this.document[n(218)](), "", this[n(249)]);
        e[n(225)][r][n(211)](this[n(249)], t);
        const i = s[n(223)][this[n(249)]];
        t[n(209)](i);
      }
    }),
      r[t(221)]((s, r) => {
        const i = t;
        if (s[i(223)] && s[i(223)][this.extensionName]) {
          const t = new nn(this[i(220)][i(218)](), "", this.extensionName);
          e[i(217)][r][i(211)](this[i(249)], t);
          const o = { ...s[i(223)][this[i(249)]] };
          for (const [s, r] of Object[i(248)](o))
            if (typeof r?.[i(227)] === i(241)) {
              const a = r,
                c = n[a[i(227)]]?.source;
              if ("number" != typeof c) {
                console.warn(i(238), a);
                continue;
              }
              const u = e[i(225)][c],
                h = new be(this.document[i(218)]()),
                f = this[i(230)][s] ?? 4369;
              t[i(247)](s, h, u, f), e[i(237)](h, a), delete o[s];
            }
          t[i(209)](o);
        }
      });
    (s.json[t(242)] || []).forEach((s, r) => {
      const n = t;
      if (s[n(223)] && s[n(223)][this[n(249)]]) {
        const t = new nn(this[n(220)][n(218)](), "", this[n(249)]);
        e[n(242)][r][n(211)](this.extensionName, t);
        const i = s[n(223)][this[n(249)]];
        t[n(209)](i);
      }
    });
    (s[t(229)][t(224)] || [])[t(221)]((s, r) => {
      const n = t;
      if (s[n(223)] && s[n(223)][this[n(249)]]) {
        const t = new nn(this[n(220)][n(218)](), "", this[n(249)]);
        e[n(224)][r][n(211)](this[n(249)], t);
        const i = s[n(223)][this[n(249)]];
        t.setExtras(i);
      }
    });
    return (
      (s[t(229)][t(254)] || []).forEach((s, r) => {
        const n = t;
        if (s[n(223)] && s[n(223)][this.extensionName]) {
          const t = new nn(this[n(220)][n(218)](), "", this[n(249)]);
          e.scenes[r][n(211)](this[n(249)], t);
          const i = s[n(223)][this[n(249)]];
          t[n(209)](i);
        }
      }),
      this
    );
  }
  [on(246)](e) {
    const t = on,
      s = e.jsonDoc;
    return (
      this[t(220)]
        [t(232)]()
        [t(213)]()
        [t(221)]((r) => {
          const n = t,
            i = r.getExtension(this[n(249)]);
          if (i) {
            const t = e[n(240)].get(r),
              o = s.json[n(217)][t];
            o.extensions = o[n(223)] || {};
            const a = { ...i[n(250)]() };
            for (const [t, s] of Object.entries(i.textures)) {
              const r = s[0],
                n = s[1];
              n && (a[t] = e.createTextureInfoDef(n, r));
            }
            o[n(223)][this[n(249)]] = a;
          }
        }),
      this[t(220)]
        .getRoot()
        [t(251)]()
        [t(221)]((r) => {
          const n = t,
            i = r.getExtension(this[n(249)]);
          if (i) {
            const t = e[n(256)][n(243)](r),
              o = s[n(229)][n(242)][t];
            (o[n(223)] = o[n(223)] || {}),
              (o[n(223)][this.extensionName] = i[n(250)]());
          }
        }),
      this[t(220)]
        [t(232)]()
        [t(245)]()
        [t(221)]((r) => {
          const n = t,
            i = r.getExtension(this[n(249)]);
          if (i) {
            const t = e[n(235)][n(243)](r),
              o = s[n(229)][n(224)][t];
            (o[n(223)] = o[n(223)] || {}),
              (o[n(223)][this[n(249)]] = i.getExtras());
          }
        }),
      this[t(220)]
        [t(232)]()
        .listTextures()
        [t(221)]((r) => {
          const n = t,
            i = r[n(219)](this.extensionName);
          if (i) {
            const t = e[n(226)][n(243)](r);
            s[n(229)].textures?.[n(221)]((e, r) => {
              const o = n;
              if (e[o(252)] == t) {
                const e = s.json[o(225)][r];
                (e[o(223)] = e[o(223)] || {}),
                  (e[o(223)][this[o(249)]] = i[o(250)]());
              }
            });
          }
        }),
      this[t(220)]
        .getRoot()
        [t(222)]()
        [t(221)]((r) => {
          const n = t,
            i = r[n(219)](this[n(249)]);
          if (i) {
            const t = e.jsonDoc.json[n(239)] || 0,
              r = s[n(229)][n(254)][t];
            if (!r) return;
            (r[n(223)] = r[n(223)] || {}),
              (r[n(223)][this[n(249)]] = i[n(250)]());
          }
        }),
      this
    );
  }
  static register() {
    Ge[on(236)]();
  }
}
function hn(e, t) {
  var s = fn();
  return (hn = function (e, t) {
    return s[(e -= 378)];
  })(e, t);
}
function fn() {
  var e = [
    "2050642VtOpIL",
    "1kzxlEs",
    "1863200btieQP",
    "2767974YKxhRm",
    "6DfDJkq",
    "10RSnGnB",
    "4037210qimSkJ",
    "4531887CbSwDn",
    "1163687ThasCd",
    "6654376ZTTBum",
  ];
  return (fn = function () {
    return e;
  })();
}
!(function (e, t) {
  for (var s = hn, r = fn(); ; )
    try {
      if (
        512724 ===
        (-parseInt(s(384)) / 1) * (-parseInt(s(383)) / 2) +
          parseInt(s(386)) / 3 +
          -parseInt(s(385)) / 4 +
          (-parseInt(s(379)) / 5) * (parseInt(s(387)) / 6) +
          parseInt(s(381)) / 7 +
          -parseInt(s(382)) / 8 +
          (parseInt(s(380)) / 9) * (parseInt(s(378)) / 10)
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
var ln = Nn;
!(function (e, t) {
  for (var s = Nn, r = gn(); ; )
    try {
      if (
        955103 ===
        -parseInt(s(195)) / 1 +
          parseInt(s(199)) / 2 +
          (parseInt(s(188)) / 3) * (parseInt(s(183)) / 4) +
          -parseInt(s(193)) / 5 +
          -parseInt(s(187)) / 6 +
          (parseInt(s(198)) / 7) * (parseInt(s(194)) / 8) +
          (-parseInt(s(190)) / 9) * (-parseInt(s(182)) / 10)
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
class pn extends un {
  constructor() {
    var e = Nn;
    super(...arguments),
      (this[e(197)] = pn.EXTENSION_NAME),
      (this[e(202)] = {
        clearcoatTexture: U.R | U.G | U.B | U.A,
        clearcoatRoughnessTexture: U.R | U.G | U.B | U.A,
        clearcoatNormalTexture: U.R | U.G | U.B | U.A,
        clearCoatTintTexture: U.R | U.G | U.B | U.A,
      });
  }
}
pn.EXTENSION_NAME = ln(184);
class dn extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this.extensionName = dn.EXTENSION_NAME),
      (this[e(202)] = {
        sheenColorTexture: U.R | U.G | U.B | U.A,
        sheenRoughnessTexture: U.R | U.G | U.B | U.A,
      });
  }
}
function gn() {
  var e = [
    "EXTENSION_NAME",
    "textureChannels",
    "KHR_materials_specular",
    "KHR_materials_lightMap2",
    "25550feWnWR",
    "668ZxbGdt",
    "KHR_materials_clearcoat2",
    "KHR_materials_detailMap",
    "KHR_materials_iridescence2",
    "3315666SkRBBf",
    "14907RTAvyG",
    "KHR_materials_subsurface",
    "18ZvbpSt",
    "KHR_materials_brdf",
    "KHR_texture_transform2",
    "1503790hSNgdt",
    "16oikQuh",
    "987887KCaBRg",
    "EXT_mesh_babylon",
    "extensionName",
    "4436187WaJgzE",
    "1387888UKFrkc",
    "KHR_materials_anisotropy2",
  ];
  return (gn = function () {
    return e;
  })();
}
dn.EXTENSION_NAME = "KHR_materials_sheen2";
class mn extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this[e(197)] = mn.EXTENSION_NAME),
      (this.textureChannels = { lightMapTexture: U.R | U.G | U.B | U.A });
  }
}
mn[ln(201)] = ln(181);
class xn extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this[e(197)] = xn.EXTENSION_NAME),
      (this.textureChannels = { detailMapTexture: U.R | U.G | U.B | U.A });
  }
}
xn.EXTENSION_NAME = ln(185);
class Tn extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this[e(197)] = Tn[e(201)]),
      (this.textureChannels = {
        specularTexture: U.R | U.G | U.B | U.A,
        specularColorTexture: U.R | U.G | U.B | U.A,
      });
  }
}
Tn[ln(201)] = ln(203);
class En extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this.extensionName = En[e(201)]),
      (this[e(202)] = {
        subsurfaceTranslucencyColorTexture: U.R | U.G | U.B | U.A,
        subsurfaceThicknessTexture: U.R | U.G | U.B | U.A,
        subsurfaceRefractionTexture: U.R | U.G | U.B | U.A,
        subsurfaceRefractionIntensityTexture: U.R | U.G | U.B | U.A,
        subsurfaceTranslucencyIntensityTexture: U.R | U.G | U.B | U.A,
      });
  }
}
En[ln(201)] = ln(189);
class yn extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this[e(197)] = yn[e(201)]),
      (this[e(202)] = {
        iridescenceTexture: U.R | U.G | U.B | U.A,
        iridescenceThicknessTexture: U.R | U.G | U.B | U.A,
      });
  }
}
yn[ln(201)] = ln(186);
class In extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this[e(197)] = In[e(201)]),
      (this[e(202)] = { anisotropyTexture: U.R | U.G | U.B | U.A });
  }
}
In[ln(201)] = ln(200);
class Rn extends un {
  constructor() {
    var e = ln;
    super(...arguments), (this[e(197)] = Rn[e(201)]);
  }
}
function Nn(e, t) {
  var s = gn();
  return (Nn = function (e, t) {
    return s[(e -= 181)];
  })(e, t);
}
Rn.EXTENSION_NAME = ln(191);
class An extends un {
  constructor() {
    var e = ln;
    super(...arguments), (this[e(197)] = An.EXTENSION_NAME);
  }
}
An[ln(201)] = "KHR_materials_emissive_strength2";
class wn extends un {
  constructor() {
    var e = ln;
    super(...arguments),
      (this[e(197)] = wn.EXTENSION_NAME),
      (this[e(202)] = {
        opacityTexture: U.R | U.G | U.B | U.A,
        reflectivityTexture: U.R | U.G | U.B | U.A,
        microSurfaceTexture: U.R | U.G | U.B | U.A,
        roughnessTexture: U.R | U.G | U.B | U.A,
      });
  }
}
wn[ln(201)] = "KHR_materials_babylon";
class bn extends un {
  constructor() {
    var e = ln;
    super(...arguments), (this[e(197)] = bn[e(201)]);
  }
}
bn[ln(201)] = ln(192);
class Mn extends un {
  constructor() {
    var e = ln;
    super(...arguments), (this[e(197)] = Mn[e(201)]);
  }
}
Mn[ln(201)] = ln(196);
class Sn extends un {
  constructor() {
    var e = ln;
    super(...arguments), (this[e(197)] = Sn[e(201)]);
  }
}
function _n() {
  const e = [
    "5153088pTCwCS",
    "46JnCihJ",
    "46918gRMyxm",
    "3ZBCeNU",
    "102kPsQPZ",
    "29410vcLybR",
    "9990575JVHfiG",
    "468528QVLayr",
    "444483FJADVT",
    "31030880IkMsvj",
  ];
  return (_n = function () {
    return e;
  })();
}
function Cn(e, t) {
  const s = _n();
  return (Cn = function (e, t) {
    return s[(e -= 336)];
  })(e, t);
}
(Sn[ln(201)] = "ViewerConfigExport"),
  (function (e, t) {
    const s = Cn,
      r = _n();
    for (;;)
      try {
        if (
          899470 ===
          (-parseInt(s(345)) / 1) * (-parseInt(s(344)) / 2) +
            (parseInt(s(336)) / 3) * (parseInt(s(343)) / 4) +
            (parseInt(s(338)) / 5) * (parseInt(s(337)) / 6) +
            parseInt(s(339)) / 7 +
            parseInt(s(340)) / 8 +
            parseInt(s(341)) / 9 +
            -parseInt(s(342)) / 10
        )
          break;
        r.push(r.shift());
      } catch (e) {
        r.push(r.shift());
      }
  })();
const vn = [pn, dn, mn, xn, Tn, En, yn, In, Rn, An, wn, bn, Mn, Sn];
function On() {
  const e = [
    "registerDependencies",
    "2030998EMCREl",
    "7621632XAIdpp",
    "setEncoderOptions",
    "6475480cPwuBJ",
    "setRequired",
    "8678528nzTtXy",
    "registerExtensions",
    "882838XEvjDv",
    "EDGEBREAKER",
    "encoderConfig",
    "9763348uxdKBt",
    "3694408RwRonG",
    "3dAxdZi",
  ];
  return (On = function () {
    return e;
  })();
}
function Dn(e, t) {
  const s = On();
  return (Dn = function (e, t) {
    return s[(e -= 390)];
  })(e, t);
}
!(function (e, t) {
  const s = Dn,
    r = On();
  for (;;)
    try {
      if (
        705153 ===
        parseInt(s(390)) / 1 +
          (parseInt(s(397)) / 2) * (-parseInt(s(395)) / 3) +
          parseInt(s(394)) / 4 +
          -parseInt(s(400)) / 5 +
          -parseInt(s(398)) / 6 +
          parseInt(s(393)) / 7 +
          parseInt(s(402)) / 8
      )
        break;
      r.push(r.shift());
    } catch (e) {
      r.push(r.shift());
    }
})();
var compressArrayBuffer = async function (e, t) {
  const s = Dn;
  let r = new E();
  (r.decoderConfig = { type: "js" }),
    (r[s(392)] = { type: "js" }),
    r.setDecoderPath(t);
  let n = await r.initEncoder(),
    i = await r.initDecoder();
  const o = new ot()
      [s(403)]([...en, Ut, ...vn])
      [s(396)]({ "draco3d.encoder": n, "draco3d.decoder": i }),
    a = await o.readBinary(new Uint8Array(e));
  return (
    a
      .createExtension(ys)
      [s(401)](!0)
      [s(399)]({ method: ys.EncoderMethod[s(391)], encodeSpeed: 1 }),
    o.writeBinary(a)
  );
};
